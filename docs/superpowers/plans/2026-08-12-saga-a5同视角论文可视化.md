# SAGA 与 A5 同视角论文可视化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 生成 CoreView 377、386、394 上 SAGA-Canonical B4 与 A5 在相同 60% 目标响应下的同视角真实编辑主文图及客观精选图。

**Architecture:** 扩展既有 real-edit 权重路由和正式渲染器，使其支持 SAGA bank 与逐方法/部位编辑强度；新增一个编排器先做全测试视角强度扫描，再按确定规则选强度、渲染最终帧、排序精选视角并合成 B 布局。所有选择和资产指纹写入机器可读报告。

**Tech Stack:** Python 3.10、NumPy、PyTorch/CUDA、Pillow、imageio、pytest、现有 3DGS renderer。

---

## 文件结构

- Modify: `utils/semantic_real_editing.py`：增加 SAGA B4 的正式权重解析。
- Modify: `tools/render_semantic_real_editing_paper_suite.py`：接收 SAGA bank、方法和逐方法/部位强度 JSON。
- Create: `tools/make_saga_a5_same_view_paper_visual.py`：强度匹配、客观排名、统一裁剪、图版合成和 provenance 汇总。
- Create: `tools/run_saga_a5_same_view_paper_visual.sh`：三对象固定输入映射与顺序执行。
- Modify: `tests/test_semantic_real_editing.py`：SAGA B4 权重测试。
- Modify: `tests/test_render_semantic_real_editing_paper_suite.py`：SAGA CLI 和强度映射测试。
- Create: `tests/test_saga_a5_same_view_paper_visual.py`：匹配、排序和 B 布局合成测试。

### Task 1: SAGA B4 权重路由

**Files:**
- Modify: `tests/test_semantic_real_editing.py`
- Modify: `utils/semantic_real_editing.py`

- [ ] **Step 1: 写失败测试**

在 `_banks()` 之外构造包含 `soft_edit_weights`、`confidence` 与 `semantic_margin` 的 SAGA bank，断言 `resolve_method_weights(..., saga_bank=saga, method="saga", part="hair", threshold=0.5)` 返回 `soft * confidence * margin` 经阈值门控后的 B4 权重；缺 bank 或字段时抛出明确错误。

- [ ] **Step 2: 运行测试确认失败**

Run: `/opt/miniconda3/envs/ictrl/bin/python -m pytest tests/test_semantic_real_editing.py -q`

Expected: FAIL，原因是 `saga_bank` 或 `saga` 方法尚未支持。

- [ ] **Step 3: 实现最小权重解析**

将 `REAL_EDIT_METHODS` 扩展为 `("raw_hard", "voting", "a5", "a7", "saga")`。为 `resolve_method_weights` 增加可选 `saga_bank`；SAGA 路径读取 `soft_edit_weights[:, part_index]`，乘以逐点 `confidence` 和 `semantic_margin`，并使用冻结阈值保留非零权重。验证所有数组点数和有限性。

- [ ] **Step 4: 运行测试确认通过**

Run: `/opt/miniconda3/envs/ictrl/bin/python -m pytest tests/test_semantic_real_editing.py -q`

Expected: PASS。

### Task 2: 正式渲染器接受 SAGA 和逐项强度

**Files:**
- Modify: `tests/test_render_semantic_real_editing_paper_suite.py`
- Modify: `tools/render_semantic_real_editing_paper_suite.py`

- [ ] **Step 1: 写失败测试**

增加三类测试：`--saga-bank` 能解析；请求 `saga` 方法但缺 bank 时失败；`resolve_strength_for_item` 能从 JSON 映射 `{"saga":{"hair":0.65},"a5":{"hair":0.55}}` 为每个 method/part 返回不同强度，并在无映射时回退全局强度。

- [ ] **Step 2: 运行测试确认失败**

Run: `/opt/miniconda3/envs/ictrl/bin/python -m pytest tests/test_render_semantic_real_editing_paper_suite.py -q`

Expected: FAIL，原因是 CLI 和 helper 尚不存在。

- [ ] **Step 3: 实现 CLI 与数据流**

新增 `--saga-bank`、`--method-part-strengths`。加载并校验 SAGA bank 点数，将其传入 `resolve_method_weights`。若提供强度映射，每个 matrix item 只渲染映射强度；否则保持原多强度行为，确保既有实验兼容。summary 写入 SAGA bank 路径/sha256 和强度映射。

- [ ] **Step 4: 运行相关测试**

Run: `/opt/miniconda3/envs/ictrl/bin/python -m pytest tests/test_semantic_real_editing.py tests/test_render_semantic_real_editing_paper_suite.py -q`

Expected: PASS。

### Task 3: 强度匹配、客观排序与图版纯函数

**Files:**
- Create: `tests/test_saga_a5_same_view_paper_visual.py`
- Create: `tools/make_saga_a5_same_view_paper_visual.py`

- [ ] **Step 1: 写失败测试**

覆盖以下接口：

```python
select_matched_strength(rows, method="saga", part="hair", reference_method="voting", retention=0.6)
rank_objective_views(rows, parts=("hair", "shoes"), methods=("saga", "a5"))
compose_b_layout(rows, output_png, output_pdf, fixed_view="c22_f000420")
```

测试强度选择最接近目标响应且可达；排序先平均泄漏、再平均 IoU、最后视角名；目标响应为零的视角被排除；图版是三行五列且同时生成非空 PNG/PDF。

- [ ] **Step 2: 运行测试确认失败**

Run: `/opt/miniconda3/envs/ictrl/bin/python -m pytest tests/test_saga_a5_same_view_paper_visual.py -q`

Expected: FAIL，原因是新模块尚不存在。

- [ ] **Step 3: 实现纯函数**

实现 CSV 读写、按 pooled target response 匹配强度、按 `outer_delta_sum / target_delta_sum` 与屏幕目标响应覆盖率生成排名、从输入 RGB 非黑背景范围计算统一裁剪，以及带英文论文标签的 B 布局 PNG/PDF。固定 subject 顺序为 `377,386,394`，固定列顺序为 `Input, Hair/SAGA, Hair/Ours, Shoes/SAGA, Shoes/Ours`。

- [ ] **Step 4: 运行测试确认通过**

Run: `/opt/miniconda3/envs/ictrl/bin/python -m pytest tests/test_saga_a5_same_view_paper_visual.py -q`

Expected: PASS。

### Task 4: 三对象编排和输入校验

**Files:**
- Modify: `tools/make_saga_a5_same_view_paper_visual.py`
- Create: `tools/run_saga_a5_same_view_paper_visual.sh`
- Modify: `tests/test_saga_a5_same_view_paper_visual.py`

- [ ] **Step 1: 写失败测试**

测试编排器 dry-run manifest 包含 3 个对象、9 个固定测试视角、hair/shoes、SAGA/A5/voting、固定视角 `c22_f000420` 和输出根目录。测试缺失固定视角、点数不一致或不可达 60% 时失败。

- [ ] **Step 2: 运行测试确认失败**

Run: `/opt/miniconda3/envs/ictrl/bin/python -m pytest tests/test_saga_a5_same_view_paper_visual.py -q`

Expected: FAIL，原因是编排入口尚不完整。

- [ ] **Step 3: 实现两阶段编排**

Shell 脚本固定映射三个 subject 的 source root、42k checkpoint、raw/voting/A5/SAGA bank、LOSO config 和 method freeze。Python 编排器：

1. 调用正式渲染器以 `0.1..1.0`、步长 0.1 扫描 voting/SAGA/A5 的 recolor hair/shoes；
2. 匹配 60% 后以 `0.02` 局部步长细化相邻区间；
3. 写 `method_part_strengths.json`；
4. 用最终强度渲染 9 视角；
5. 写逐视角指标、排名、fixed_main、objectively_selected、summary 和 README。

- [ ] **Step 4: dry-run 验证命令和所有输入**

Run: `DRY_RUN=1 bash tools/run_saga_a5_same_view_paper_visual.sh`

Expected: exit 0，并打印三个对象的完整命令和固定顺序。

- [ ] **Step 5: 运行全部专项测试**

Run: `/opt/miniconda3/envs/ictrl/bin/python -m pytest tests/test_semantic_real_editing.py tests/test_render_semantic_real_editing_paper_suite.py tests/test_saga_a5_same_view_paper_visual.py -q`

Expected: PASS。

### Task 5: 正式批量渲染与验收

**Files:**
- Generate: `exp/acceptdata/saga_a5_same_view_paper_visual_20260812/`

- [ ] **Step 1: 运行正式队列**

Run: `bash tools/run_saga_a5_same_view_paper_visual.sh`

Expected: 三对象阶段一/细化/最终渲染全部结束，根目录生成 `COMPLETE`。

- [ ] **Step 2: 校验产物和数值**

Run: `/opt/miniconda3/envs/ictrl/bin/python tools/make_saga_a5_same_view_paper_visual.py --verify-only --output-root exp/acceptdata/saga_a5_same_view_paper_visual_20260812`

Expected: 3 个对象各 9 个视角、2 方法、2 部位；目标保留率接近 0.60；固定图视角/顺序正确；精选视角等于各排名第一名；无 NaN、空图或尺寸不一致。

- [ ] **Step 3: 视觉检查**

使用图像查看器检查 `fixed_main/saga_a5_same_view_fixed.png` 与 `objectively_selected/saga_a5_same_view_objectively_selected.png`，确认人物非空、无裁剪截断、标签不重叠、SAGA/A5 差异可见。

- [ ] **Step 4: 最终回归**

Run: `/opt/miniconda3/envs/ictrl/bin/python -m pytest tests/test_semantic_real_editing.py tests/test_render_semantic_real_editing_paper_suite.py tests/test_saga_a5_same_view_paper_visual.py tests/test_saga_canonical.py -q`

Expected: PASS。

- [ ] **Step 5: 记录完成状态**

将实际强度、实际保留率、精选视角、图路径和公平性声明写入输出根目录 `README.md` 与 `summary.json`，只提交源码、测试和计划，不提交大型实验输出。
