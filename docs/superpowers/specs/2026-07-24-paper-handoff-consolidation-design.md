# Paper Handoff Consolidation Design

## Goal

Update `docs/当前项目论文创新点与快速投稿补强交接_20260718.md` so that it remains a useful historical handoff while also reflecting all formal experiments completed from 2026-07-22 through 2026-07-24.

## Editing Strategy

Preserve the original July 18 sections as historical context. Add a short status notice near the top and append a dated consolidated update instead of rewriting old claims in place. The appended update becomes the authoritative current status when it conflicts with earlier sections.

## Evidence Included

The update will record:

- five-subject A5 leave-one-subject-out evaluation and subject-level bootstrap statistics;
- repaired independent B0-B4 baseline asset routing;
- A5 primary-method freeze and A6 extension-only role;
- A0-A6 component ablation and A5 micro-ablation;
- five-subject recolor, removal, and texture editing experiments;
- matched-strength burden and coverage limitations;
- continuous-pose temporal evaluation;
- corrected pooled-burden, 80% cross-subject coverage, and matched-retention temporal tables.

Only completed artifacts with explicit output paths and frozen provenance will be reported as results. Earlier mixed-protocol, mean-of-ratios, or fixed-strength-only numbers remain historical diagnostics and will not be promoted to final paper claims.

## Analysis Structure

The appended section will separate:

1. completed evidence;
2. evidence that supports the proposed innovation;
3. negative results and claim boundaries;
4. current publication readiness;
5. the shortest remaining P0/P1/P2 path to submission.

The main interpretation will be that A5 provides statistically supported spatial leakage control for persistent semantic editing, while current evidence does not support improved temporal stability or universal part coverage.

## Next-Step Priorities

The document will recommend, in order:

1. regenerate the three real-editing task tables with pooled burden and the same cross-subject coverage rule used by the temporal table;
2. add fixed-strength temporal flicker alongside adaptive matched-retention flicker to separate semantic drift from strength compensation;
3. complete fair external baselines or a clearly scoped capability comparison;
4. prepare final qualitative figures, failure cases, complexity reporting, and manuscript assets;
5. freeze the method and stop selecting changes from observed test results.

## Verification

Before completion:

- cross-check every reported number against its generated CSV or JSON artifact;
- verify all referenced paths exist;
- scan the edited Markdown for contradictory current claims, placeholders, and malformed tables;
- commit only the consolidation spec and requested handoff document, preserving unrelated worktree changes.
