# A7 时序可靠性校准实验记录（2026-07-28）

## 1. 当前结论

A7 的 Task 1-9 代码与协议已经完成，377 短序列 canary 已实际运行。结果分为两层：

- **工程与协议集成冒烟通过**：证据数组可重复，候选 ID/索引可重复，A5 在加入 A7 前后逐元素一致，A7 每帧使用同一静态 bank，统一 renderer/common support/strength/provenance 均生效。
- **短 canary 不具备候选资格**：两相机、每相机四帧最多只能形成 6 次累计 consecutive-visible pair support，低于冻结合同 `min_pair_support=8`。因此 24 个候选全部 invalid，A7 权重为零，不能把本次渲染解释为 A7 效果结果。

当前决策：**不读取 c21/c22/c23，不运行五主体，不形成 A7 优于 A5 的结论。下一步只允许进入 Task 11 的 CoreView_377 正式 evidence/validation。**

## 2. 运行环境与代码状态

- 分支：`feat/strict-semantic-paper-protocol`
- GPU：NVIDIA GeForce RTX 4090，24564 MiB
- 开跑前 GPU：0 MiB，利用率 1%
- Python：`/opt/miniconda3/envs/ictrl/bin/python`
- 记录时间：`2026-07-28T03:48:30Z` / `2026-07-28T11:48:30+0800`
- A7 实现提交：
  - `6873426` 方法：定义 A7 时序可靠性冻结合同
  - `269a822` 方法：实现高斯时序足迹证据累计
  - `d6026b6` 方法：实现 A7 可靠性衰减与稳定载体重分配
  - `2d1a432` 方法：扩展 A7 语义资产格式与溯源校验
  - `3abca77` 工具：构建 A7 跨姿态时序可靠性证据
  - `e0420e8` 工具：生成并筛选 A7 静态候选语义资产
  - `a4d59ae` 评测：接入 A7 统一空间编辑与时序协议
  - `b2bf619` 协议：实现 A7 五主体留一法配置选择
  - `ed712cb` 实验：编排 A7 证据验证与冻结评测队列

## 3. 冻结输入

| 输入 | SHA-256 / 逻辑 fingerprint |
|---|---|
| config | `e22b8f18ac10c5c1dd4462e8bbdf8c09d2e781f574c0b7129fb35cb11ea45acb` |
| checkpoint `ckpt42000.pth` | `2d5050c6a452d489e71da19a34c462bb30e64962f8662171c7196a5891e1a597` |
| A5 bank | `49ba86b05c4f87eaa8b98ef47822c7083a31fdf050a35bd8cf3a88843f8a45d3` |
| voting bank | `df7608ac25b7b9f9ed1750280512307333b2e35e44253a2c0d9d2b98b2f6cb6b` |
| A5 freeze 文件 SHA-256 | `28c59e9d98ff2954c5aee9a5e75efa1386e55dcfed38e99a10f6555d451997ae` |
| A5 freeze 逻辑 fingerprint | `de87d5af88b782d7921ae2a2728dddf9a0b396fcdb45dc945c124a481c564968` |
| A7 contract 文件 SHA-256 | `c50271e4927dde66bea786f9399875ba3259d93606bc72a23fab4827ad7b3570` |
| A7 contract 逻辑 fingerprint | `5d6337809636daa6f5d0fa8bfee7f261d417f7b32fc3ba5964f6dbc730c61168` |

## 4. 队列 dry-run

已执行：

```bash
bash tools/run_a7_temporal_reliability_queue.sh --stage validation --dry-run
```

dry-run 固定输出 GPU 顺序 `377,386,387,393,394`，每主体正式 evidence 预算为 456 samples（`c01,c05,c09,c13 / 0:570:5`），每候选 temporal validation 预算为 912 method-frame samples（`c17-c20 / 0:570:5 / A5+A7`）。dry-run 未创建状态或正式输出目录。

未携带 `--allow-post-validation` 请求 `retrospective-c21` 时返回码为 2，输出：

```text
post-validation stages require --allow-post-validation
```

## 5. 377 短 evidence canary

协议：`c01,c05 / frames 0,5,10,15 / hair,face,upper,lower,shoes,skin`。

| 项目 | 结果 |
|---|---:|
| sample count | 8 |
| point count | 2120 |
| visible 非零 entries | 6114 |
| consecutive-visible pair 非零 entries | 5934 |
| max visible count | 8 |
| max pair support | 6 |
| NaN/Inf | 0 |
| ratio/rate/flicker 范围 | 全部在 `[0,1]`；std 最大 0.5 |

在同一提交 `ed712cb` 下，使用相同输入和协议、不同临时输出路径重复两次，内部 evidence fingerprint 均为：

```text
8ecf89fc67583883acb5b5b0a3dd927fe8eb629ef57b89753f3a22c191b1e619
```

两次所有 `temporal_*` 数组逐元素一致。NPZ 文件 SHA-256 不同是因为文件保留 `generated_at_utc` 和命令文本；稳定性判据使用内部内容/provenance fingerprint。早期正式 canary evidence 的 fingerprint `4749edf3...` 来自提交 `3abca77`，跨提交差异由 `git_commit` provenance 引起，不作为同一冻结命令的复现比较。

## 6. 候选生成与停止原因

- 候选数：24
- 唯一 candidate ID：24
- valid candidate：0
- validation shortlist：空
- 两次 `candidate_index.json` 字节级一致，SHA-256：`f65d88aba879d11b6551f235b9211ede09007316681effa2ff4ca911322c5026`
- 集成候选：`e11d51249b48`
- candidate config fingerprint：`e11d51249b48bc1164108b6b77c3eed1ca4518ebca7167166f8feb8dcdd6a7d9`
- bank 逻辑 fingerprint：`bdeabcd8ea47063b27110e1d61d3878449f7d73423628aae5d3de8fca203f50c`

候选 `e11d51249b48` 的权重 shape 为 `[2120,6]`，全部 finite，范围为 `[0,0]`，无 cap saturation。六个 part 的 `remaining_deficit = target_floor - restored_target_mass` 均可复算，总 deficit 为 `115.78792924032724`。

invalid 原因为所有 part 的 evidence support coverage 不足及 target deficit。直接根因不是数值错误，而是短协议的最大 pair support 只有 6，小于合同门槛 8。该候选仅用于验证 A7 bank 的加载、fingerprint 和统一评测链路。

## 7. c17 四帧统一渲染

运行范围：`c17 / frames 0,5,10,15 / six parts / A5+A7 / edit strength 1.0`，另运行一次 A5-only 对照。

- A5+A7：48 metric rows
- A5-only：24 metric rows
- 联合运行中的 24 条 A5 rows 与 A5-only **字节级/逐字段一致**
- A5 temporal summary 与 A5-only 完全一致
- 重复 A5+A7 命令后，`metrics.csv` 字节级一致，SHA-256：`ddc3b1ded068965960333185fad4aee03d20d70df4c0d01a40ba1bdc0aa8efca`
- 去除非确定性的 `elapsed_seconds` 后，summary 完全一致，规范化 SHA-256：`f354ec347ada538d896e30f4ec228de97045657390095b28b5cec00178867120`
- 每个 part 的 adaptive strength 在整段序列中固定为一个值；face 为 0.25，其余 part 为 0.5
- `canonical_selection_fixed_across_frames=true`
- `common_support_across_methods=true`
- `shared_rasterizer_across_methods=true`
- `uses_test_parser_for_edit_selection=false`

由于集成候选权重全零，A7 的 response/flicker 为零。这不是“消除闪烁”的有效结果，而是无动作候选的预期输出。

## 8. Canary 验收表

| 验收项 | 状态 | 证据 |
|---|---|---|
| 无 NaN/Inf/shape mismatch | 通过 | evidence 全 finite；bank `[2120,6]` |
| A5 新旧输出完全一致 | 通过 | 24 rows 与 temporal summary 完全一致 |
| A7 每帧相同 bank fingerprint | 通过 | `bdeabcd8...`，canonical selection 固定 |
| visible/pair support 非空 | 通过 | 6114 / 5934 |
| water-filling 无越界，target mass 可复算 | 通过 | 权重 `[0,0]`，六 part deficit 可复算 |
| A5/A7 同 renderer/mask/strength/common support | 通过 | summary provenance 与统一命令 |
| 形成有效 A7 候选 | **未通过** | max pair 6 `<` min pair 8，24/24 invalid |

## 9. 后续许可边界

1. 可以执行 Task 11：仅 CoreView_377 的正式 `c01,c05,c09,c13 / 0:570:5` evidence、24 候选生成及 `c17-c20` validation。
2. 正式 evidence 必须先出现至少一个 valid candidate，才允许渲染 A7 效果；不得再用零权重候选作为结果。
3. 不允许读取 c21，不允许运行 c22/c23，不允许五主体 LOSO，不允许扩大参数网格。
4. 若 377 正式 evidence 仍无 valid candidate，停止并审计 support accumulation、target floor 与 redistribution capacity。
5. 所有大体积 NPZ/render 输出保留在 `exp/acceptdata/a7_temporal_reliability_v1/`，不进入 Git 提交。
