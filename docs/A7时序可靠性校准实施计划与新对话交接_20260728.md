# A7 时序可靠性校准实施计划与新对话交接

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不重训 Avatar 主干、不引入逐帧状态的前提下，把冻结 A5 扩展为静态 canonical A7 语义资产，降低姿态变化造成的 screen-space outer/boundary flicker，同时守住 A5 的空间泄漏、覆盖率、语义精度和重建质量。

**Architecture:** A7 先在固定 calibration camera/frame 上累计每个 Gaussian/part 的可见性、target/outer footprint 波动和 boundary crossing，再对 A5 `soft_edit_weights` 做静态可靠性衰减，并通过确定性的 target-preserving water-filling 把缺失 target mass 重分配给稳定 carrier。所有超参数只在 donor subjects 的 c17-c20 validation 上按 LOSO 选择；c21 仅作回顾性测试，c22-c23 和未触碰主体用于冻结确认。

**Tech Stack:** Python 3、NumPy、PyTorch/CUDA、现有 Gaussian renderer、JSON/NPZ、argparse、pytest、Bash。

---

## Task 11 A7c R1.3-G exact global aggregate feasibility oracle（2026-08-05，CERTIFIED_FEASIBLE）

R1.3-G 按冻结预注册执行 constructive endpoint replay。它不训练 predictor，也不修改
R1.3-P、carrier set、A5 bank 或 promotion threshold；仅从 R1.3-P 保存的 24 个
`boundary_conditioned.feasible_lower` 中减去固定 `0.00002` replay margin，然后对每个
`fold x fit camera held block` 重新求解一次真实 renderer-constrained HiGHS LP，保存 gate
数组和证书，再由独立 auditor 从磁盘 gate 与冻结 renderer stream 重算正式指标。

冻结执行于北京时间 `2026-08-05 10:00:42` 开始。首轮 24/24 replay 已成功，但 audit
因漏验 probe source fingerprint 而 fail closed；这是 audit plumbing error，不是数值或
feasibility failure。提交 `7a014de` 补齐 probe 重验，提交 `6487e79` 修复 error-state
restart 条件后，在不改变 contract、margin、阈值、carrier 或 evidence 的情况下确定性
重放；最终于北京时间 `2026-08-05 10:04:41` 以唯一根终态 `.completed` 结束。

24-record exact aggregate audit 为：

```text
metric                         observed        promotion gate        result
outer mean gain                +1.573653%      >= +0.500000%         PASS
outer positive blocks          24/24 (100%)    >= 90%                PASS
outer q10 gain                 +0.500000%      >= 0                  PASS
outer worst-block gain         +0.500000%      >= -0.500000%         PASS
boundary mean gain             +3.446923%      >= +0.500000%         PASS
boundary positive blocks       24/24 (100%)    >= 90%                PASS
boundary q10 gain              +2.341608%      >= 0                  PASS
boundary worst-block gain      +1.779011%      >= -0.500000%         PASS
minimum target response         0.994766        >= 0.99               PASS
maximum soft-IoU drop           0.001043        <= 0.005              PASS
maximum adjacent gate change    0.015000        <= 0.02               PASS
improves over R1.1-F1           true                                  PASS
```

24 个 LP 全部 optimal，最大重算 primal residual 为 `1.818989e-12`。实际 boundary
request 范围为 `1.779011%-5.870001%`；重算后的最差 outer/boundary gain 分别为
`0.500000%/1.779011%`。所有 sample/carrier order、source fingerprint、held-mask、
NaN isolation、A5 topology floor、coverage/frozen-parts/weight guards 和三类 eligibility
检查通过；未打开 c17-c20 或 c21-c23。

R1.3-G 的严格结论是：**冻结 lower carrier/action space 存在通过当前 24-record aggregate
promotion protocol 的真实 gate witness**。因此 R1.3-P 的主要瓶颈不是 aggregate action
capacity，而是 runtime predictor/observability 无法从当前 ray-context feature 恢复
renderer-beneficial、pose-dependent 的 carrier allocation。该结果不代表当前部署时序已经
达到论文标准，也不能把 held oracle gate 直接用作跨 fold teacher。

下一步仅允许单独预注册 renderer-visible/view-pose interaction successor（暂记
R1.4-VP）：每个 fold 只能用本 fold fit blocks 生成 oracle supervision，held blocks 只作
audit，禁止进入 normalization、超参数选择、early stopping 或 checkpoint selection。
该路线必须复现 R1.3-G 的 aggregate gain，同时保持真实 target/soft-IoU/jump/topology
guards；它不是 2026-07-31 已被五主体 interaction 诊断关闭的 pose-only A7b。在此之前
不进入 Task 12，不打开 c17-c23。

```text
design commit = 2095d1e
plan commit = 7d2a5c7
implementation commits = 6a163ba, 46e21df, 4c33858, 528d4e5, e84cc3a,
                         7a014de, 6487e79
contract SHA256 = 0ed0d588ab4a89abfa50d3213a84dc4e055ecd2d800c1bfc7bc154d3bf927bbb
output root = exp/acceptdata/a7c_r1_3g_exact_aggregate_oracle_377_v1
records SHA256 = 97b59b5ab0b9f0b0c473748f9beb9af184de9de7acccc13dc1c96794f9340594
audit SHA256 = 69426279d5bdbc44c5b8f9a353e4175eafa27e6c7b9f22811316586f07db615e
summary SHA256 = d84d345be3e15b2a833ea75d03f40fcd0473fe2da567ce0c5ee4b5d58422b830
oracle verdict = CERTIFIED_FEASIBLE
deployment_eligible = false
teacher_eligible = false
paper_test_eligible = false
```

## 0. 冻结边界与执行纪律

实施前必须先阅读：

```text
docs/superpowers/specs/2026-07-28-a7-temporal-reliability-calibration-design.md
configs/semantic/frozen_a5_main_method_v1.json
docs/正式论文失败案例与时序诊断_20260727.md
```

不可违反的边界：

```text
A5 = voting posterior + footprint evidence calibration，保持冻结，不覆盖原 bank。
A7 = A5 + visibility-aware temporal reliability calibration
         + target-preserving stable-carrier redistribution。
A7 只生成一份静态 canonical soft_edit_weights，不使用上一帧，不做视频后处理。
c21 已被观察，只能用于 retrospective evaluation，不能选超参数。
c22-c23 在冻结 LOSO 配置和 fingerprint 后才允许打开结果。
五个已有主体 377/386/387/393/394 都不能充当真正 untouched confirmation。
任何 held-out subject 的 A7 配置只能由其余四主体的 validation 结果选择。
```

统一数据协议：

```text
evidence cameras: c01,c05,c09,c13
evidence frames:  0:570:5
validation cameras: c17,c18,c19,c20
validation frames:  0:570:5
retrospective test: c21, 0:570:1
frozen test:        c22,c23, 0:570:1，资源不足时只能在开跑前统一声明 stride
parts: hair,face,upper,lower,shoes,skin
subjects: 377,386,387,393,394
```

统一输出根目录：

```text
exp/acceptdata/a7_temporal_reliability_v1/
  canary_377/
  evidence/{subject}/
  candidates/{subject}/
  validation/{subject}/{candidate_id}/
  loso/{held_out_subject}/
  frozen_test/{subject}/{camera}/
  ablation/{subject}/
  aggregate/
  queue/
```

每个正式输出必须记录：git commit、A5 freeze fingerprint、输入 bank SHA-256、checkpoint SHA-256、协议相机/帧、候选参数、随机种子、命令行、生成时间和输出 fingerprint。

---

### Task 1: 建立 A7 配置合同和冻结保护测试

**Files:**
- Create: `configs/semantic/frozen_a7_temporal_reliable_v1.json`
- Create: `tests/test_frozen_a7_temporal_method.py`
- Modify: `utils/frozen_semantic_method.py`
- Modify: `tools/validate_frozen_semantic_method.py`

- [ ] 先写失败测试，要求 A7 合同包含下列固定字段：

```json
{
  "schema_version": 1,
  "freeze_id": "a7_temporal_reliable_v1",
  "base_method": "A5",
  "output_field": "soft_edit_weights",
  "runtime_state": false,
  "retrain_avatar": false,
  "evidence_cameras": ["c01", "c05", "c09", "c13"],
  "evidence_frame_start": 0,
  "evidence_frame_end": 570,
  "evidence_frame_stride": 5,
  "validation_cameras": ["c17", "c18", "c19", "c20"],
  "validation_frame_stride": 5,
  "retrospective_test_cameras": ["c21"],
  "frozen_test_cameras": ["c22", "c23"],
  "parts": ["hair", "face", "upper", "lower", "shoes", "skin"]
}
```

- [ ] 添加失败测试：A7 的 base A5 fingerprint 与 `frozen_a5_main_method_v1.json` 不匹配时拒绝验证。
- [ ] 添加失败测试：A7 合同如果允许 per-frame state、修改 checkpoint 或包含 c21 calibration/validation camera，必须拒绝。
- [ ] 运行并确认先失败：

```bash
python -m pytest tests/test_frozen_a7_temporal_method.py -q
```

预期：因 A7 schema/validator 尚未实现而失败，不允许通过放宽断言规避。

- [ ] 扩展 `utils/frozen_semantic_method.py`，增加 `validate_a7_temporal_contract()`，并复用现有确定性 SHA-256 fingerprint 逻辑。
- [ ] 扩展 validator CLI，使以下命令输出 `base_method=A5`、`runtime_state=false` 和两个 freeze fingerprint：

```bash
python tools/validate_frozen_semantic_method.py \
  --config configs/semantic/frozen_a7_temporal_reliable_v1.json \
  --base-config configs/semantic/frozen_a5_main_method_v1.json
```

- [ ] 运行测试和静态检查：

```bash
python -m pytest tests/test_frozen_a7_temporal_method.py tests/test_frozen_semantic_method.py -q
git diff --check
```

- [ ] 独立提交：

```bash
git add configs/semantic/frozen_a7_temporal_reliable_v1.json \
  utils/frozen_semantic_method.py \
  tools/validate_frozen_semantic_method.py \
  tests/test_frozen_a7_temporal_method.py
git commit -m "方法：定义A7时序可靠性冻结合同"
```

---

### Task 2: 实现纯 NumPy 时序证据累计器

**Files:**
- Create: `utils/temporal_reliability_calibration.py`
- Create: `tests/test_temporal_reliability_calibration.py`

- [ ] 先写合成序列测试，至少覆盖：持续可见、间歇不可见、target/outer 比例波动、boundary 状态切换、零支持、NaN 输入和帧顺序错误。
- [ ] 固定公开接口，不让 GPU/render 逻辑进入此模块：

```python
def accumulate_temporal_footprint_frame(
    state,
    *,
    frame_index: int,
    visible: np.ndarray,
    target_ratio: np.ndarray,
    outer_ratio: np.ndarray,
    boundary_state: np.ndarray,
) -> None:
    """Accumulate one [N, C] frame into deterministic float64 running state."""


def finalize_temporal_footprint_evidence(state) -> dict[str, np.ndarray]:
    """Return float32 evidence arrays and integer support arrays with shape [N, C]."""
```

- [ ] 断言最终输出恰好包含：

```text
temporal_visible_count                 int32 [N,C]
temporal_consecutive_visible_count     int32 [N,C]
temporal_target_ratio_mean             float32 [N,C]
temporal_target_ratio_std              float32 [N,C]
temporal_target_flicker                float32 [N,C]
temporal_outer_ratio_mean              float32 [N,C]
temporal_outer_ratio_std               float32 [N,C]
temporal_outer_flicker                 float32 [N,C]
temporal_boundary_crossing_rate        float32 [N,C]
temporal_visibility_transition_rate    float32 [N,C]
```

- [ ] 指标定义写死在测试中：

```text
target/outer flicker = consecutive both-visible pair 上 |ratio_t-ratio_{t-1}| 的均值
boundary crossing = consecutive both-visible pair 上 state_t != state_{t-1} 的比例
visibility transition = 所有相邻采样帧上 visible_t != visible_{t-1} 的比例
std = visible frames 上的 population std，ddof=0
无有效 pair 时 flicker/crossing 为 0，同时通过 pair count 暴露低支持，不能填 NaN
```

- [ ] 运行并确认先失败：

```bash
python -m pytest tests/test_temporal_reliability_calibration.py -q
```

- [ ] 用 Welford/在线累计实现，不缓存全部帧；内部 float64，落盘 float32；对输入 shape、finite、frame 单调递增做显式校验。
- [ ] 对相同输入重复两次，断言所有数组逐元素完全一致。
- [ ] 运行：

```bash
python -m pytest tests/test_temporal_reliability_calibration.py -q
git diff --check
```

- [ ] 独立提交：

```bash
git add utils/temporal_reliability_calibration.py tests/test_temporal_reliability_calibration.py
git commit -m "方法：实现高斯时序足迹证据累计"
```

---

### Task 3: 实现可靠性衰减与 target-preserving 重分配

**Files:**
- Modify: `utils/temporal_reliability_calibration.py`
- Modify: `tests/test_temporal_reliability_calibration.py`

- [ ] 先写可靠性公式测试：

```python
def compute_temporal_reliability(
    *,
    consecutive_visible_count,
    temporal_outer_flicker,
    temporal_boundary_crossing_rate,
    temporal_target_flicker,
    lambda_outer: float,
    lambda_boundary: float,
    lambda_target: float,
    min_pair_support: int,
) -> tuple[np.ndarray, dict]:
    ...
```

必须满足：

```text
R = support_gate
    * exp(-lambda_outer * outer_flicker)
    * exp(-lambda_boundary * boundary_crossing_rate)
    * exp(-lambda_target * target_flicker)
R in [0,1]
pair support < min_pair_support 时 support_gate=0
lambda 全为 0 且支持充足时 R=1
```

- [ ] 先写 water-filling 失败测试，固定接口：

```python
def calibrate_a7_soft_edit_weights(
    *,
    a5_weights,
    semantic_probs,
    temporal_target_ratio_mean,
    temporal_outer_ratio_mean,
    temporal_reliability,
    consecutive_visible_count,
    rho: float,
    min_pair_support: int,
    max_weight_scale_from_posterior: float,
) -> tuple[np.ndarray, dict]:
    ...
```

- [ ] 测试以下不变量：

```text
输入输出 shape 均为 [N,C]，float32，finite，范围 [0,1]
w_damped = a5_weights * temporal_reliability
A5 target mass = sum(a5_weights * temporal_target_ratio_mean)
目标 floor = rho * A5 target mass
只给 support 充足、target_ratio>outer_ratio 的 carrier 重分配
候选排序稳定：posterior、target mean、R、support 降序，最后 Gaussian index 升序
单点上限由 posterior ceiling 与 1.0 的较小值决定
有足够 capacity 时达到 floor；不足时记录 remaining_deficit，不伪造覆盖率
不允许任何 A7 weight 超过 ceiling
不能改变未处理 part
相同输入得到 bitwise-identical 输出和 summary
```

- [ ] summary 每个 part 至少记录：

```text
a5_target_mass
damped_target_mass
target_floor
restored_target_mass
remaining_deficit
redistributed_gaussian_count
cap_saturated_count
stable_candidate_count
weight_l1_from_a5
```

- [ ] 写一个合成反例：所有 carrier 都不稳定时，A7 只能报告 deficit，不能把 outer-dominant Gaussian 提升为 target carrier。
- [ ] 运行并确认先失败，再实现：

```bash
python -m pytest tests/test_temporal_reliability_calibration.py -q
```

- [ ] 完成后运行 property-style 随机种子测试，至少 20 个固定种子验证范围、确定性、target floor/cap 约束。
- [ ] 独立提交：

```bash
git add utils/temporal_reliability_calibration.py tests/test_temporal_reliability_calibration.py
git commit -m "方法：实现A7可靠性衰减与稳定载体重分配"
```

---

### Task 4: 扩展 part-label bank schema 和溯源

**Files:**
- Modify: `utils/part_label_bank.py`
- Modify: `tests/test_part_label_bank.py`
- Create: `tests/test_a7_part_label_bank.py`

- [ ] 先写失败 round-trip 测试，要求 A7 bank 保存/加载 Task 2 的十个 evidence arrays、`temporal_reliability` 和新的 `soft_edit_weights`。
- [ ] A7 bank 必须另外保存这些标量字符串数组或 manifest 字段：

```text
method_id=A7
base_method=A5
base_bank_sha256
base_method_freeze_fingerprint
a7_contract_fingerprint
evidence_protocol_fingerprint
candidate_config_fingerprint
output_bank_fingerprint
```

- [ ] schema 校验必须拒绝：point/part shape 不一致、负 count、非 finite ratio、R 超出 `[0,1]`、缺失 base fingerprint、把 A5 bank 原地覆盖的相同输出路径。
- [ ] `resolve_soft_edit_weights()` 继续把 A7 解析为静态 `soft_edit_weights`；不得增加 per-frame 参数。
- [ ] 保存前计算 fingerprint，保存后重新加载并复算；不一致立即失败。
- [ ] 运行：

```bash
python -m pytest tests/test_part_label_bank.py tests/test_a7_part_label_bank.py -q
git diff --check
```

- [ ] 独立提交：

```bash
git add utils/part_label_bank.py tests/test_part_label_bank.py tests/test_a7_part_label_bank.py
git commit -m "方法：扩展A7语义资产格式与溯源校验"
```

---

### Task 5: 构建 GPU 时序证据 CLI

**Files:**
- Create: `tools/build_temporal_reliability_evidence.py`
- Create: `tests/test_build_temporal_reliability_evidence.py`
- Reuse: `tools/calibrate_evidence_soft_edit_weights.py`

- [ ] 先写 parser/协议失败测试，要求 CLI 至少支持：

```text
--config
--checkpoint
--a5-bank
--method-freeze
--a7-contract
--cameras
--frame-start
--frame-end
--frame-stride
--parts
--output
--resume
--dry-run
```

- [ ] 测试默认正式模式只接受 `c01,c05,c09,c13` 和 `0:570:5`；其他协议必须显式 `--allow-canary-protocol`，且输出标记 `formal_protocol=false`。
- [ ] 复用现有 `build_footprint_evidence_record`/renderer，不复制 parser mask 裁剪逻辑；A5、A7 的 footprint 必须走同一个渲染路径。
- [ ] 每个 frame/camera 生成 `[N,C]` 的 visible、target ratio、outer ratio、boundary state，送入 Task 2 在线累计器。
- [ ] boundary state 固定编码并写入 manifest：

```text
0 = not visible
1 = target-dominant
2 = allowed-boundary
3 = outer-dominant
```

- [ ] resume 使用 completed `(camera, frame)` ledger；恢复前验证 checkpoint、A5 bank、protocol fingerprints 全部一致。
- [ ] `--dry-run` 只解析数据、列出预计样本数/显存设备/输出路径，不写正式 evidence。
- [ ] 先运行 CPU parser 测试：

```bash
python -m pytest tests/test_build_temporal_reliability_evidence.py -q
python tools/build_temporal_reliability_evidence.py --help
```

- [ ] 做 377 短 canary，禁止读取 c21：

```bash
SOURCE_ROOT=exp/acceptdata/coreview377_multisubject_strict_20260721
CONFIG="$SOURCE_ROOT/assets/test/.hydra/config.yaml"
CHECKPOINT="$SOURCE_ROOT/semantic_train_strict/ckpt42000.pth"
A5_BANK=exp/acceptdata/frozen_a5_five_subject_main_20260723/CoreView_377/banks/footprint_evidence_target/part_label_bank.npz
test -f "$CONFIG" && test -f "$CHECKPOINT" && test -f "$A5_BANK"

CUDA_VISIBLE_DEVICES=0 python tools/build_temporal_reliability_evidence.py \
  --config "$CONFIG" \
  --checkpoint "$CHECKPOINT" \
  --a5-bank "$A5_BANK" \
  --method-freeze configs/semantic/frozen_a5_main_method_v1.json \
  --a7-contract configs/semantic/frozen_a7_temporal_reliable_v1.json \
  --cameras c01,c05 \
  --frame-start 0 --frame-end 20 --frame-stride 5 \
  --parts hair,face,upper,lower,shoes,skin \
  --allow-canary-protocol \
  --output exp/acceptdata/a7_temporal_reliability_v1/canary_377/evidence.npz
```

- [ ] Canary 验收：无 NaN/Inf；point count 与 A5 bank 一致；可见/pair counts 非零；target/outer ratio 在 `[0,1]`；重复运行 fingerprint 一致。
- [ ] 独立提交：

```bash
git add tools/build_temporal_reliability_evidence.py tests/test_build_temporal_reliability_evidence.py
git commit -m "工具：构建A7跨姿态时序可靠性证据"
```

---

### Task 6: 生成 A7 候选 bank 和证据代理排序

**Files:**
- Create: `tools/calibrate_temporal_reliable_a7_weights.py`
- Create: `tests/test_calibrate_temporal_reliable_a7_weights.py`

- [ ] 先写失败测试，要求候选网格严格为：

```text
lambda_outer:    0.25,0.50,1.00
lambda_boundary: 0.25,0.50
lambda_target:   0.00,0.25
rho:             0.90,0.95
min_pair_support: 合同中的固定整数
```

总候选为 24 个；candidate id 必须由规范化参数 JSON 的 SHA-256 前 12 位产生，不能依赖枚举顺序。

- [ ] CLI 输入 A5 bank、evidence、A7 contract，输出每个候选独立 bank、`candidate_summary.json` 和总 `candidate_index.json`。
- [ ] 证据代理至少汇总：target mass ratio、outer mass ratio、boundary crossing weighted mass、低支持权重、L1 distance、remaining deficit、cap saturation。
- [ ] 明确 invalid candidate 条件：任一 part 有非 finite、target floor 无法满足且 deficit 超过 A5 target mass 的 2%、或 evidence support coverage 低于合同阈值。
- [ ] 对 valid candidates 先按 proxy temporal burden 排序，但 proxy 只能做 shortlist，不能替代 c17-c20 render validation。
- [ ] 每个 donor subject 最多保留 4 个 GPU validation candidates；所有 candidate 的完整 index 仍保留。
- [ ] 运行：

```bash
python -m pytest tests/test_calibrate_temporal_reliable_a7_weights.py -q
python tools/calibrate_temporal_reliable_a7_weights.py --help
git diff --check
```

- [ ] 用 377 canary evidence 生成候选，检查 24 个 ID 唯一、相同输入重复运行 fingerprints 一致。
- [ ] 独立提交：

```bash
git add tools/calibrate_temporal_reliable_a7_weights.py \
  tests/test_calibrate_temporal_reliable_a7_weights.py
git commit -m "工具：生成并筛选A7静态候选语义资产"
```

---

### Task 7: 让统一 evaluator/renderers 正式识别 A7

**Files:**
- Modify: `tools/render_semantic_temporal_stability.py`
- Modify: `tools/evaluate_semantic_editing_paper_protocol.py`
- Modify: `tools/render_semantic_real_editing_paper_suite.py`
- Modify: `tests/test_render_semantic_temporal_stability.py`
- Modify: `tests/test_semantic_editing_paper_protocol.py`
- Modify: `tests/test_render_semantic_real_editing_paper_suite.py`
- Create: `tests/test_a7_method_integration.py`

- [ ] 先写失败测试：method `A7` 必须从 A7 bank 的静态 `soft_edit_weights` 读取；每帧权重 fingerprint 相同；A5 输出在加入 A7 前后逐元素一致。
- [ ] 所有 renderer 强制同时加载 A5 freeze 与 A7 contract，验证 base fingerprint 和 bank fingerprint。
- [ ] Temporal renderer 增加统一输出：

```text
fixed_strength_outer_flicker
fixed_strength_boundary_flicker
adaptive_matched_retention_outer_flicker
adaptive_matched_retention_boundary_flicker
visibility_aware_response_flicker
selection_area_cv
selection_centroid_std
visibility_transition_rate
```

- [ ] fixed-strength 的 A5/A7 必须使用同一 edit strength；adaptive matched-retention 使用同一 target retention 定义，并单独报告 strength 序列，不能混入 fixed 主结论。
- [ ] Spatial evaluator 输出 pooled outer/boundary/selection burden、coverage、macro mIoU、micro IoU、Boundary F1/IoU、soft IoU。
- [ ] Real-edit suite 对 recolor/removal/texture 使用同一 coverage-constrained pooled burden 协议。
- [ ] 所有 summary 写入 `canonical_selection_fixed_across_frames=true`、method fingerprints、common support 和 protocol provenance。
- [ ] 运行：

```bash
python -m pytest \
  tests/test_render_semantic_temporal_stability.py \
  tests/test_semantic_editing_paper_protocol.py \
  tests/test_render_semantic_real_editing_paper_suite.py \
  tests/test_a7_method_integration.py -q
git diff --check
```

- [ ] 独立提交：

```bash
git add tools/render_semantic_temporal_stability.py \
  tools/evaluate_semantic_editing_paper_protocol.py \
  tools/render_semantic_real_editing_paper_suite.py \
  tests/test_render_semantic_temporal_stability.py \
  tests/test_semantic_editing_paper_protocol.py \
  tests/test_render_semantic_real_editing_paper_suite.py \
  tests/test_a7_method_integration.py
git commit -m "评测：接入A7统一空间编辑与时序协议"
```

---

### Task 8: 实现 LOSO 选择器和硬门槛

**Files:**
- Create: `tools/select_loso_a7_temporal_config.py`
- Create: `tests/test_select_loso_a7_temporal_config.py`

- [ ] 先写合成五主体报告测试。held-out subject 的任何 validation 文件一旦出现在输入中必须报错，不能静默忽略。
- [ ] 每个 donor、每个候选必须逐项通过：

```text
formal eligible parts >= A5
matched target coverage >= A5 - 0.02
pooled outer burden <= 1.02 * A5
pooled boundary burden <= 1.02 * A5
macro mIoU >= A5 - 0.01
micro IoU >= A5 - 0.005
```

- [ ] 任一 donor 失败则 candidate 整体不 eligible；禁止 subject/part/camera fallback。
- [ ] eligible 候选按下列确定性优先级选择：

```text
1. donor-subject-equal mean fixed outer + fixed boundary flicker 最小
2. pooled spatial burden 更低
3. 与 A5 的 weight L1 distance 更小
4. lambda_outer + lambda_boundary + lambda_target 更小
5. candidate_id 字典序
```

- [ ] 输出 `selected_config.json`、`eligibility_matrix.csv`、`selection_trace.json`；包含四 donor 名单、held-out、候选 fingerprint、A5/A7 contract fingerprints。
- [ ] 测试无 eligible candidate 时 exit non-zero，并明确返回 `A7_REJECTED_FOR_HELD_OUT_SUBJECT`，不得自动选择最接近者。
- [ ] 运行：

```bash
python -m pytest tests/test_select_loso_a7_temporal_config.py -q
python tools/select_loso_a7_temporal_config.py --help
git diff --check
```

- [ ] 独立提交：

```bash
git add tools/select_loso_a7_temporal_config.py tests/test_select_loso_a7_temporal_config.py
git commit -m "协议：实现A7五主体留一法配置选择"
```

---

### Task 9: 编写可恢复队列和防泄漏 dry-run

**Files:**
- Create: `tools/run_a7_temporal_reliability_queue.sh`
- Create: `tests/test_run_a7_temporal_reliability_queue.py`

- [ ] 先写 shell 文本测试，要求队列固定包含 377/386/387/393/394、正式 cameras/frames、A5/A7 freeze、独立输出和 `set -euo pipefail`。
- [ ] 队列分 stage，默认只允许执行到 validation：

```text
canary
evidence
candidates
validation
loso-freeze
retrospective-c21
frozen-c22-c23
paper-tables
```

- [ ] `retrospective-c21` 必须要求所有五个 `selected_config.json` 存在且 fingerprint 冻结；`frozen-c22-c23` 还要求 `aggregate/loso_freeze_manifest.json` 只读确认。
- [ ] 为每个 job 写 `.running/.done/.failed` 状态、开始/结束 UTC/BJT、PID、command、日志和输出 fingerprint；重启时只跳过 fingerprint 一致的 `.done`。
- [ ] `--dry-run` 打印 GPU job 顺序、预计 render sample 数和所有输入输出，不启动 CUDA。
- [ ] 禁止队列修改训练 checkpoint 或调用 `train.py`；文本测试明确断言不存在训练命令。
- [ ] 运行：

```bash
python -m pytest tests/test_run_a7_temporal_reliability_queue.py -q
bash -n tools/run_a7_temporal_reliability_queue.sh
bash tools/run_a7_temporal_reliability_queue.sh --stage validation --dry-run
git diff --check
```

- [ ] 独立提交：

```bash
git add tools/run_a7_temporal_reliability_queue.sh tests/test_run_a7_temporal_reliability_queue.py
git commit -m "实验：编排A7证据验证与冻结评测队列"
```

---

### Task 10: 执行 377 短 canary 与停止审计

**Files:**
- Create: `exp/acceptdata/a7_temporal_reliability_v1/canary_377/` outputs
- Create: `docs/A7时序可靠性校准实验记录_20260728.md`

- [ ] 记录开跑前 GPU、输入 checkpoint/bank、commit 和 fingerprint；先执行 queue dry-run。
- [ ] 运行 `c01,c05`、frames `0:20:5` evidence canary，生成候选，只渲染 A5 与 1 个 A7 候选的 `c17`、frames `0:20:5`。
- [ ] 对同一个命令重复一次并核对 evidence、candidate bank、summary fingerprints 一致。
- [ ] Canary 必须同时满足才继续：

```text
无 NaN/Inf/shape mismatch
A5 新旧输出完全一致
A7 每帧使用相同 bank fingerprint
visible/pair support 非空
water-filling 无越界，summary target mass 可复算
A5/A7 使用相同 renderer、mask/parser、strength 和 common support
```

- [ ] 任一失败：停止，不跑全主体；在实验记录写根因、修复 commit 和复测结果。
- [ ] 通过后提交代码/记录，不提交大体积 render/NPZ：

```bash
git add docs/A7时序可靠性校准实验记录_20260728.md
git commit -m "实验：完成A7短序列一致性冒烟验证"
```

---

### Task 11: 执行 377 全 validation canary（已执行，promotion gate 未通过）

**Files:**
- Output: `exp/acceptdata/a7_temporal_reliability_v1/evidence/377/`
- Output: `exp/acceptdata/a7_temporal_reliability_v1/candidates/377/`
- Output: `exp/acceptdata/a7_temporal_reliability_v1/validation/377/`
- Modify: `docs/A7时序可靠性校准实施计划与新对话交接_20260728.md`

- [x] 构建 377 正式 `c01,c05,c09,c13 / 0:570:5` evidence。
- [x] 生成 24 candidates，按 proxy 保留最多 4 个。
- [x] 在 `c17-c20 / 0:570:5` 对 A5 和最多 4 个 A7 candidates 统一评测 temporal、spatial、semantic、coverage。
- [x] 只根据 validation 判断，不打开 c21。
- [x] 检查进入五主体的最低门槛。结果：`0/4` 候选通过；所有候选 fixed outer 和 fixed boundary flicker 均小幅上升。
- [x] 因无候选通过而停止；未扩大网格、未接触 c21、未转 A7b。下一步审计 evidence 到 screen-space flicker 的代理失配与 redistribution capacity。
- [x] 已在本节补入所有候选表、淘汰原因、资源用时和下一阶段决策。

#### Task 11 正式执行记录（2026-07-28）

执行分支与冻结状态：

```text
branch = feat/strict-semantic-paper-protocol
start commit = 4574ce3e2c228ebe05bf1cda1fe15ab6aec96783
spatial evaluator fix commit = 6059226
GPU = NVIDIA GeForce RTX 4090, GPU0
start BJT = 2026-07-28 12:52
GPU evaluation end BJT = 2026-07-28 14:39:05
initial ETA BJT = 2026-07-28 15:30
```

本任务没有调用 `train.py`，没有修改 checkpoint。执行内容是在冻结 `ckpt42000.pth` 上构建 A7 evidence/bank，并运行 validation render/evaluator。

正式 evidence：

```text
protocol = c01,c05,c09,c13 / frames 0:570:5
formal_protocol = true
sample_count = 456
point_count = 2120
visible nonzero entries = 10104
consecutive-visible pair nonzero entries = 9582
max pair support = 452
per-part Gaussian count with pair support >= 8 = 1391
all temporal arrays finite = true
internal output fingerprint = 6abbbc14169986960c246095bd9129785ae45b54f4962416df6e59fc94f00a66
evidence NPZ SHA-256 = 215077c40f2cf8dd0f4c45e82e160aeb07c5503f13095ee0b7eeeea71e7f488f
elapsed = 14m38.6s
```

候选生成：

```text
candidate_count = 24
valid_candidate_count = 24
validation_shortlist_count = 4
candidate_index SHA-256 = 1304e2d87b716a3e27d0bb172ba5759c45908d6b47c7d5d6ce0d990ca41a2e25
candidate generation elapsed = 1.7s
shortlist = 108035060b77,e0e10bd7dca8,70f3ea602d0b,e11d51249b48
```

四个候选均完成 `c17-c20 / 0:570:5`。每个相机输出 1368 行 temporal metrics，共 16 个相机 summary、21888 行 temporal metrics。四候选 temporal renderer 累计耗时 `4661.0s`；spatial evaluator 每候选读取 8 条 validation records。所有候选中 A5 的 c17-c20 temporal summary 逐元素一致，A5 freeze fingerprint 始终为 `de87d5af...`。

候选门槛表如下。Temporal 列为四相机、除空响应 face 外的 part-equal fixed flicker 相对 A5 变化；spatial burden 为六 part-equal pooled burden 相对 A5 变化。

| candidate | fixed outer | fixed boundary | coverage | spatial outer | spatial boundary | macro mIoU delta | micro IoU delta | 淘汰原因 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| `108035060b77` | +0.668% | +0.278% | 0.6042 | -0.172% | -0.715% | -0.004181 | -0.003728 | coverage + temporal |
| `e0e10bd7dca8` | +0.142% | +0.171% | 0.6250 | -0.372% | -0.249% | +0.002772 | -0.002417 | temporal |
| `70f3ea602d0b` | +0.142% | +0.171% | 0.6250 | -0.372% | -0.249% | +0.002772 | -0.002417 | temporal |
| `e11d51249b48` | +0.175% | +0.148% | 0.6250 | -0.440% | -0.294% | +0.002773 | -0.002414 | temporal |

Task 8 空间/语义硬门槛结果：

- `108035060b77` 的 coverage 从 A5 `0.6250` 降到 `0.6042`，低于 `A5 - 0.02 = 0.6050`，其余 spatial/semantic gates 通过。
- `e0e10bd7dca8`、`70f3ea602d0b`、`e11d51249b48` 均通过 formal eligible parts、coverage、pooled outer/boundary burden、macro mIoU 和 micro IoU 六项 gate。
- 四个候选均未通过 Task 11 temporal promotion gate：没有候选做到 fixed outer 或 fixed boundary 至少一项下降。

当前有效与无效路线：

1. **有效**：正式多相机长序列 evidence 解决了短 canary 的 support 不足，24/24 候选均能形成有限、可复算、target-preserving 的静态 A7 bank。
2. **有效**：A7 静态校准能在 3 个候选上保持 coverage/semantic guards，并轻微降低 pooled spatial outer/boundary burden。
3. **无效**：当前 proxy temporal burden 排序和 reliability damping/redistribution 没有转化为 screen-space fixed flicker 改善；四个 shortlist 候选均小幅恶化 outer 与 boundary flicker。
4. **观察**：`e0e10bd7dca8` 与 `70f3ea602d0b` 的正式结果等价，说明当前 shortlist 中 `lambda_target=0.25` 未改变最终 screen-space selection。

Task 11 决策：

```text
final eligible candidate count = 0
do not enter Task 12
do not open c21/c22/c23
do not expand the grid
do not start A7b
keep A5 as the frozen primary method
next audit = evidence proxy vs rendered flicker attribution, especially shoes/skin and redistribution carrier capacity
```

执行期间首次进入真实 spatial evaluator 时发现固定路径遗漏 `selected` 定义。根因是新增 spatial guard 汇总没有复用 fixed threshold/projected selection。已用失败测试复现并在 `6059226` 修复，相关回归 `45 passed`，随后四个 spatial evaluator 全部重跑完成。

```bash
git add docs/A7时序可靠性校准实施计划与新对话交接_20260728.md
git commit -m "实验：完成A7单主体正式验证筛选"
```

#### Task 11 v2 renderer-aligned 修复实验（2026-07-29，已完成，promotion gate 未通过）

本轮对应实现提交 `039cc57`，输出根目录为
`exp/acceptdata/a7_renderer_aligned_v2_canary_377/`。实验使用真实 rasterizer
的累计 opacity/transmittance 梯度贡献构造 target、outer、boundary evidence；
预冻结 `face/upper/shoes/skin`，只校准 `hair/lower`；固定 selection threshold
为 `0.2` 并禁止 A5 topology 阈值穿越。A7 contract fingerprint 为
`0ad527f29a52a51da4db634afe0aa03b8cb404b6af7db578aad85a7b37d58368`。

运行状态：

```text
start = 2026-07-29 09:57:01 BJT
evidence complete = 2026-07-29 10:00:15 BJT
run complete = 2026-07-29 10:41:58 BJT
candidate_count = 24
valid_candidate_count = 24
validation_shortlist = 7b31afe47a46,f5df51f21895
temporal validation = c17-c20, frame 0:570:5, 114 frames/camera
spatial validation = 8 fixed validation records/candidate
GPU after completion = 0 MiB
```

Temporal 为四相机、除空响应 face 外的五 part-equal 聚合；spatial burden 为六
part-equal pooled burden。相对变化为 A7 相对冻结 A5，负值更好。

| candidate | rho | fixed outer | fixed boundary | adaptive outer | adaptive boundary | spatial outer | spatial boundary | mean soft IoU delta | promotion |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `7b31afe47a46` | 0.95 | +1.412% | -2.182% | +1.411% | -2.185% | +6.314% | +1.662% | -0.069851 | fail |
| `f5df51f21895` | 0.90 | +1.111% | -1.692% | +1.114% | -1.688% | +6.730% | +2.315% | -0.079020 | fail |

硬约束核验：

- `face/upper/shoes/skin` 的 A5/A7 temporal rows 在 selected count、weight sum、
  selection fingerprint 和全部 edit response 列上逐元素一致，`mismatch_count=0`。
- `soft_edit_weights >= 0.2` 的 A5/A7 topology 完全一致，向下和向上阈值穿越均为
  `0`；hard macro mIoU、micro IoU、boundary IoU/F1 均与 A5 完全相同。
- 两候选均保住 renderer target mass floor；`f5df51f21895` 的 hair/lower fixed
  target response 分别为 A5 的 `98.40%/98.41%`，adaptive 后为
  `100.96%/99.62%`。
- soft spatial quality 明显退化：mean soft IoU 从 A5 `0.543859` 降至
  `0.474008/0.464840`，说明 topology 不变并不等于 soft mass 分布不退化。

本轮出现一个重要的“绝对量改善、归一化门槛失败”现象。以 fixed outer edit
response 为例：

| candidate/part | outer mean | adjacent absolute change | normalized outer flicker |
|---|---:|---:|---:|
| `7b31afe47a46` hair | -29.12% | -23.41% | +8.06% |
| `7b31afe47a46` lower | -57.15% | -51.43% | +13.35% |
| `f5df51f21895` hair | -33.93% | -29.76% | +6.32% |
| `f5df51f21895` lower | -57.95% | -53.52% | +10.53% |

因此 renderer-aligned evidence 确实减少了 outer 的绝对编辑量和绝对逐帧变化，
也让 hair/lower 的 normalized boundary flicker 分别下降约 `2.36%-3.74%` 和
`9.24%-11.18%`；但 outer mean 的下降速度快于 adjacent change，导致协议定义的
`adjacent_change / mean_response` 反而上升。

根因判断：

1. renderer-aligned evidence 输入已对齐真实渲染贡献，但候选 proxy 仍是逐 Gaussian
   的 `weight * (outer_flicker + boundary_crossing)` 加权平均，不是最终合成后的
   part-level `adjacent_change / mean_response`，因此没有控制分母收缩。
2. target floor 恢复把质量集中到少量 carrier：`7b31afe47a46` 的 hair/lower
   分别重分配 `6/21` 个 carrier，总 weight L1 改变量 `89.30`；这种大幅 soft mass
   重排保持了 hard topology，却使 lower matched-retention spatial outer burden
   增加 `69.94%`，并拖低 soft IoU。
3. evidence proxy 预测整体 outer mass ratio 为 `0.7441/0.7399`，它正确预测了
   绝对 outer response 会下降，却不能预测 normalized temporal flicker 和
   matched-retention spatial burden；当前剩余问题已经从 evidence 来源转移到
   candidate objective 与 redistribution 约束。

当前决策：

```text
final eligible candidate count = 0
renderer-aligned evidence = partially effective
frozen-part and A5 topology guards = effective
current reliability-weighted redistribution objective = not promotion-safe
keep A5 as frozen primary method
do not enter Task 12
do not open c21/c22/c23
do not expand the hyperparameter grid on the same objective
```

#### Task 11 v3 rendered-objective + bounded retention（2026-07-29，已完成）

本轮按 v2 根因分析启动单一因果修复，不扩展旧 24 组合 grid：evidence 额外保存
c01/c05/c09/c13 的逐帧 renderer target/outer/boundary contribution sequence，候选
生成直接汇总 part-level `adjacent_change / mean_response`；同时所有 A7 权重均不得
超过 A5，只比较两个预注册策略：

```text
bounded_damping_005:
  A5 weight lower ratio = 0.95
  rho = 0.95
  no target restoration

bounded_retention_010:
  A5 weight lower ratio = 0.90
  rho = 0.95
  restore only toward original A5 weights
  maximum part L1 change = 12.0
```

冻结协议与实现证据：

```text
design/plan commit = d87ee9a
contract commit = 92a4fc7
sequence evidence commit = 7c587af
bounded calibration commit = 3f3065a
rendered objective commit = 8f74018
candidate CLI commit = b96f12a
runner commit = 0acb777
float tolerance fix commit = 3236926
A7 v3 contract fingerprint = 816c975183a868c97ab156d866d1411d03df1265e9ad98f885cf50f3d637e508
focused regression = 78 passed
CUDA canary sequence shape = [1,2120,6] float16
CUDA canary finite = true
CUDA canary nonzero contribution count = 6420
```

正式队列：

```text
output = exp/acceptdata/a7_renderer_objective_v3_canary_377/
runner = run_377_v3_validation.sh
formal evidence start = 2026-07-29 16:51:20 BJT
evidence complete = 2026-07-29 16:54:33 BJT, elapsed 193s
validation restart = 2026-07-29 17:05:15 BJT
detached runner PID = 798554
registered validation = c17-c20 / 0:570:5 / two candidates
actual completion = 2026-07-29 17:46:11 BJT
c21/c22/c23 = unopened
```

首次候选生成被固定绝对 `1e-6` target deficit 容差误判为 0 个 valid；实际剩余
deficit 仅为 `1.3e-6` 至 `5.8e-6`。已增加相对 A5 target mass 的 scale-aware
浮点容差并补 2 个红绿回归测试。重生成后 `candidate_count=2`、
`valid_candidate_count=2`；`bounded_damping_005` 通过 evidence screen，
`bounded_retention_010` 保留真实 hair target ratio `0.949454` 的 screen miss 记录，
但按预注册双策略对照继续进入 c17-c20 validation。

最终 promotion gate 不变。若两个候选仍未同时降低 fixed outer/boundary flicker，
或六 part-equal spatial outer/boundary burden 恶化超过 2%，继续保持 A5，禁止进入
Task 12。

正式结果如下。Temporal 为 c17-c20、除空响应 face 外五 part-equal fixed 指标；
spatial 为六 part-equal matched-retention burden。负值表示 A7 低于 A5。

| candidate | fixed outer | fixed boundary | adaptive outer | adaptive boundary | spatial outer | spatial boundary | mean soft IoU delta | promotion |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| `bounded_damping_005` | +0.001% | +0.000% | +0.001% | -0.003% | +0.001% | -0.000% | -0.010261 | fail |
| `bounded_retention_010` | -0.045% | -0.076% | -0.042% | -0.076% | -0.013% | -0.052% | -0.010458 | pass |

`bounded_retention_010` 的 active part 结果：hair fixed outer/boundary 分别下降
`0.418%/0.454%`，lower 分别下降 `0.278%/0.046%`。Outer 在 4/4 个 validation
cameras 均下降；boundary 在 c17/c18/c19 下降，c20 上升 `0.100%`，四机位聚合仍
下降。Visibility-aware response flicker 下降 `0.390%`，selection area CV 下降
`0.385%`。

空间与冻结约束：

- 六 part-equal pooled outer/boundary burden 分别下降 `0.013%/0.052%`，通过
  相对恶化不超过 2% 的 gate。
- macro mIoU、micro IoU、boundary IoU/F1 与 A5 完全相同；所有 part coverage
  与 A5 完全相同。
- `face/upper/shoes/skin` temporal rows 逐元素一致，mismatch count 为 `0`；
  threshold 0.2 topology 上下穿越均为 `0`；所有 A7 soft weights 均不超过 A5。
- hair/lower fixed target response 为 A5 的 `95.586%/95.320%`，adaptive 后为
  `100.359%/100.087%`。
- mean soft IoU 从 `0.543859` 降到 `0.533401`，相对下降 `1.923%`。该项不是
  当前 Task 11 预注册 hard promotion gate，但必须作为 Task 12 的残余风险保留，
  不得写成无代价改善。

本轮验证了 rendered-sequence objective 的预测价值：damping-only evidence
预测 normalized outer 基本不变，validation 也没有改善；bounded retention evidence
预测 normalized outer/boundary 小幅下降，validation 同方向通过。说明 v2 的主要
问题确实是 greedy redistribution，而不是 renderer-aligned evidence 本身。

Task 11 v3 决策：

```text
final eligible candidate count = 1
selected CoreView377 candidate = bounded_retention_010
Task 11 promotion gate = pass
allow entry to Task 12 five-subject evidence/validation/LOSO
keep A5 as frozen primary method until Task 12-14 pass
do not open c21/c22/c23 yet
do not claim temporal reliability solved: aggregate margin is small and c20 boundary regresses
candidate_index SHA-256 = a1a2b1b8eab6263440f8f2a24def1d1e42eae0a6a45f77546cede369d6361fc3
evidence NPZ SHA-256 = 17142db0063bb63b84b8f0a777e9ca9ec21c1d4084608aa791f79e8e595ab965
```

#### Task 11 v4 sparse robust capacity（2026-07-29，已完成）

v4 不再使用统一 weight floor。它复用 v3 的冻结 renderer contribution sequence，
只允许 A5 selected 且 support 合格的 hair/lower carrier 下调 `5%` 或 `10%`，并以
最差 camera normalized flicker、camera mean 和绝对 adjacent change 的组合目标做
确定性稀疏 coordinate search。每个 camera 的 target mean 必须保留至少 `98%`；
最多改变 A5 selected carrier 的 `20%`。

实现与冻结证据：

```text
design/plan commit = 00b766b
contract commit = 4e98bca
sparse optimizer commit = 38174ed
candidate CLI commit = f1b91ca
runner commit = 366ecf9
A7 v4 contract fingerprint = 4167896b7e714decd707a6718111d36c6688ca4527bd24f361c6816fa1f2dc36
focused regression = 85 passed
candidate id = sparse_robust_loco_v4
candidate config fingerprint = 3c540ec549b68ea1ffdcf874974177bc3af187a6f0a5d4e122e98e3c8f0250fb
output bank fingerprint = ccc61421e0cbecb4dbd21c1292f65057b22e0560dbc4bac2169fa97192feb01a
candidate_index SHA-256 = 7fd201ec15977e4c7435bd57b68531c20cd59758d9d114c59c112d05a503b309
```

四折 leave-one-evidence-camera-out capacity gate：

| held-out evidence camera | part-equal outer ratio | part-equal boundary ratio | part-equal target ratio | pass |
|---:|---:|---:|---:|---|
| 0 | 0.987791 | 0.990479 | 0.995706 | yes |
| 1 | 0.989631 | 0.985527 | 0.996390 | yes |
| 2 | 0.985811 | 0.991409 | 0.990862 | yes |
| 3 | 0.983256 | 0.988174 | 0.997203 | yes |

最终全 evidence camera 候选只修改 hair `15` 个、lower `25` 个 carrier。Hair 的
最差 per-camera outer/boundary ratio 为 `0.993954/0.992433`，最低 target ratio
为 `0.980152`；lower 为 `0.984063/0.984074`，最低 target ratio 为 `0.998866`。

正式队列：

```text
output = exp/acceptdata/a7_sparse_robust_v4_canary_377/
runner = run_377_v4_validation.sh
formal start = 2026-07-29 23:04:09 BJT
detached runner PID = 803636
validation = c17-c20 / 0:570:5 / one frozen candidate
actual completion = 2026-07-29 23:25:11 BJT
c21/c22/c23 = unopened
```

v4 正式 promotion 除原 spatial/coverage/topology gate 外，额外要求 hair/lower
part-equal fixed outer 与 boundary 至少下降 `0.5%`、四个 validation cameras 两项
都下降，并要求 hair/lower 各自 soft IoU 绝对下降不超过 `0.01`。任一项失败则不
进入 Task 12，也不自动启动 A7b。

正式验证结果：

| gate/metric | A7 v4 相对 A5 | 要求 | 结果 |
|---|---:|---:|---|
| hair/lower part-equal fixed outer flicker | -0.648% | <= -0.5% | pass |
| hair/lower part-equal fixed boundary flicker | -0.706% | <= -0.5% | pass |
| adaptive outer flicker | -0.649% | diagnostic | improve |
| adaptive boundary flicker | -0.699% | diagnostic | improve |
| validation camera direction | 4/4 两项下降 | 4/4 | pass |
| six-part-equal spatial outer burden | +0.042% | <= +2% | pass |
| six-part-equal spatial boundary burden | +0.054% | <= +2% | pass |
| hair soft IoU absolute drop | 0.015644 | <= 0.01 | **fail** |
| lower soft IoU absolute drop | 0.005498 | <= 0.01 | pass |
| macro/micro IoU | 0/0 delta | within tolerance | pass |
| coverage/topology/frozen parts | exact | exact | pass |

各 validation camera 的 hair/lower part-equal fixed temporal 变化：

| camera | outer | boundary |
|---|---:|---:|
| c17 | -0.996% | -0.683% |
| c18 | -0.510% | -0.589% |
| c19 | -0.601% | -0.727% |
| c20 | -0.501% | -0.801% |

各 active part 四机位聚合：hair outer/boundary 分别下降 `0.151%/0.219%`，lower
分别下降 `1.102%/1.219%`。Hair/lower fixed target response 分别保留
`97.911%/99.911%`。绝对 outer adjacent change 分别下降 `2.603%/4.873%`，绝对
boundary adjacent change 分别下降 `1.805%/3.662%`。

残余问题：v4 的 sparse robust objective 已经解决了 v3 的统一缩放和 c20 boundary
反向问题，但 hair 的 15 个时序高风险 carrier 同时承担较多 target-side soft mass，
使 hair soft IoU 从 `0.682936` 降到 `0.667292`。此外 visibility-aware response
flicker 上升 `0.689%`，说明当前 objective 还没有直接约束 visibility response。

Task 11 v4 决策：

```text
temporal reliability gate = pass
spatial burden / semantic / coverage / topology gates = pass
per-part soft IoU gate = fail (hair)
final promotion = fail
do not enter Task 12
do not open c21/c22/c23
next repair = add per-carrier target/inside soft-mass cost and visibility-response cost
keep sparse LOCO architecture; do not return to dense scaling or expand the action grid
```

#### Task 11 v5 dual evidence constrained repair（2026-07-30，已完成）

v5 保留 v4 的 sparse coordinate architecture，但修正 v4 objective 与论文 spatial/
visibility 指标不一致的问题。同一次 renderer backward 现在同时导出：

```text
edit-sensitivity weighted target / outer / boundary contribution sequence
unweighted alpha/transmittance target / outer / boundary contribution sequence
per-frame per-part target pixel count
```

未加权 sequence 用于直接构造 evidence soft IoU：

```text
inside = selection_target_sequence @ weights
outside = selection_outer_sequence @ weights
soft IoU = inside / (target_pixel_count + outside)
```

冻结 repair policy：

```text
hair initial = exact A5
lower initial = v4 sparse bank
LOCO fold seed = 在该 fold 的 training cameras 上重新生成 v4 lower seed，禁止 held-out 泄漏
coordinate actions = 5% / 10% only
maximum lower changed fraction = 20%
maximum hair compensation count = 3, only when active temporal gain is insufficient
minimum per-camera target response = 0.99
maximum per-camera evidence soft IoU drop = 0.005
maximum per-camera visibility response ratio = 1.0
selection topology / frozen parts / weight <= A5 = exact hard constraints
```

正式 audit 使用此前未打开的 `c21/c22/c23`，且必须在 contract、evidence、candidate
index、candidate bank 的 SHA-256 freeze manifest 写出后才开始。Spatial audit 使用协议
`test` split，而不复用 c17-c20 validation assets。

实现前聚焦基线为 `21 passed`；v5 实现后的扩大聚焦回归为 `44 passed`。Renderer
evidence dry-run 为 `456` samples、每 sample `6` 次 backward，双 evidence 不增加
backward 次数。

v5 capacity 于北京时间 `2026-07-30 10:17:49` 结束。最终全 evidence candidate
通过全部约束，outer/boundary 分别下降 `0.852%/0.805%`，hair 保持 exact A5，lower
evidence soft IoU 改善；但 LOCO 只有 `3/4` 通过。Held-out evidence camera 0 的
lower visibility response ratio 为 `1.000369`，超过冻结上限 `1.0`，因此 candidate
被拒绝，freeze manifest 未生成，`c21/c22/c23` 未打开。

逐 carrier 回退确认主要责任点是 lower carrier `2012`：训练 fold 将其从
`0.999758` 下调到 `0.899782`；仅恢复该 carrier 即可把 held-out visibility ratio
从 `1.000369` 降到 `0.999955`。问题不是 support/coverage，而是 construction gate
贴线、没有为跨视角 target-response 相位变化预留余量。

#### Task 11 v5.1 visibility construction margin（2026-07-30，已完成，promotion gate 未通过）

v5.1 复用 v5 双 evidence，不重新 renderer backward。唯一方法变化是分离训练与
正式 visibility gate：

```text
training/construction visibility response ratio <= 0.9995
LOCO held-out/formal audit visibility response ratio <= 1.0
construction 0.9995 只约束实际发生 move 的 part；exact A5 part 不制造无意义 move
source evidence SHA-256 = 3e04bd3c49dfa32c6dd74ca3313669536df85083e5c3dccd9b198acc5a10a141
coordinate actions = 5% / 10% unchanged
hair/lower policy, target, soft IoU, topology, frozen parts = unchanged
```

真实 evidence 的冻结前 capacity 复核结果：

| held-out evidence camera | outer | boundary | visibility ratio | pass |
|---:|---:|---:|---:|---|
| 0 | -0.996% | -0.707% | 0.997459 | yes |
| 1 | -1.144% | -0.640% | 0.998905 | yes |
| 2 | -0.714% | -0.668% | 0.998477 | yes |
| 3 | -0.836% | -0.636% | 0.996350 | yes |

最终全 evidence candidate outer/boundary 分别下降 `0.874%/0.787%`，visibility
最大 ratio 为 `0.999318`，target response 最低保留 `99.661%`，soft IoU 继续改善，
hair change count 为 `0`，lower change count 为 `25`。冻结前 capacity 为 `4/4 pass`。
正式 runner 会在 audit 前复核 contract/evidence/v4 bank/candidate bank 指纹，拒绝半成品
temporal/spatial 输出；只有 c21-c23 时序门槛与 test spatial/coverage/IoU 门槛全部通过，
`promotion_summary.json` 才返回 pass，并在其后写入 `.done`。

v5.1 正式诊断于北京时间 `2026-07-30 12:21:26` 完成。Outer/boundary 在
`c21-c23` 上分别下降 `0.786%/0.511%`，三个相机两项均同向改善；spatial burden、
coverage、hair/lower soft IoU、macro/micro IoU、topology 全部通过。唯一失败为
`c22/lower visibility response ratio = 1.000077`。原始相邻 target 波动下降
`0.276%`，但 mean target response 同时下降 `0.284%`，归一化后发生极小回退。
因此 v5.1 promotion 为 fail，不能进入 Task 12。

#### Task 11 v5.2 dual construction margin（2026-07-30，已完成，development gate 未通过）

v5.2 不扩大 action grid，不增加 hair carrier，不修改 topology/coverage/spatial
协议。新增独立 construction target gate，并进一步收紧 construction visibility：

```text
training target response ratio >= 0.9975
training visibility response ratio <= 0.9990
held-out/diagnostic target response ratio >= 0.99
held-out/diagnostic visibility response ratio <= 1.0
hair = exact A5
lower = v4 start, 5%/10% actions, maximum changed fraction 20%
```

真实 evidence 的冻结前反事实 capacity 为 `4/4 pass`：outer/boundary 分别下降
`0.893%/0.796%`，visibility 最大 `0.998865`，target response 最低 `0.997631`，
hair change count `0`，lower change count `25`。相对 v5.1，优化器恢复 lower
carrier `424/2102`，改用 `1786/2050`，其余 23 个 carrier 保持不变。

由于 `c21-c23` 已在 v5.1 打开，v5.2 的 377 运行只作为 development：`c17-c20`
为 validation，`c21-c23` 为 retrospective diagnostic，固定写
`paper_test_eligible=false`。新的论文级 test 必须在 v5.2 算法冻结后由五主体 LOSO
的未开封 held-out subject 承担。

v5.2 于北京时间 `2026-07-30 15:22:06` 完成。c17-c20 validation 的
outer/boundary 分别下降 `0.607%/0.647%`，target response 最低 `0.997914`；但
visibility 在 `c17/c18/c20` 分别为 `1.000338/1.002253/1.000812`，未通过。
c21-c23 retrospective 的 outer/boundary 分别下降 `0.776%/0.576%`，target
response 最低 `0.997716`；c22 visibility 已修复到 `0.999822`，但 c21/c23
仍为 `1.001881/1.000199`。Spatial、coverage、soft-IoU、macro/micro IoU 和
topology 均通过。结论是四相机 evidence 无法覆盖 c17-c20 的 target/visibility
相位，继续收紧同一四相机阈值无效。

#### Task 11 v5.3 eight-camera construction（2026-07-30，已完成，development gate 未通过）

v5.3 重新生成 renderer-aligned dual evidence，将 construction 相机扩展为：

```text
c01 c05 c09 c13 c17 c18 c19 c20
frames 0:570:5，共 912 samples
evidence SHA-256 = 8b655f48fad664ba308f51d3291971382d7f9037fc7d69e38fca37907efd77f4
evidence contract fingerprint = 9f4322fe3a7ac2812908289d65964f4d6d74c39d048e8707dd90fe938f04b368
```

不扩大 action grid，不恢复 dense scaling，不增加 hair move；hair exact A5，lower
从 v4 开始，5%/10% actions、maximum changed fraction 20%、topology、coverage、
frozen parts、weight `<=A5` 均不变。八相机 feasibility sweep 冻结以下门槛：

```text
full construction outer/boundary gain >= 0.5%
construction target response ratio >= 0.995
construction visibility response ratio <= 0.998
LOCO held-out outer/boundary 均同向下降
LOCO held-out visibility <= 1.0
minimum_loco_held_out_temporal_gain = 0.0
retrospective target >= 0.99, visibility <= 1.0
paper_test_eligible = false
```

`minimum_loco_held_out_temporal_gain=0.0` 只取消单个 held-out 相机重复承担总体
`0.5%` effect-size 门槛；full construction 仍同时要求 outer/boundary 至少下降
`0.5%`。正式 capacity 预检为 `8/8 pass`：整体 outer/boundary 分别下降
`0.731%/0.685%`，lower target 最低 `0.995096`，construction visibility 最大
`0.997276`，evidence soft-IoU 改善 `0.0783%`，hair change count `0`，lower
change count `25`。八个 held-out 相机两项均同向下降，最紧 visibility 出现在 c20，
ratio 为 `0.998721`，仍低于 held-out 上限 `1.0`。

c17-c20 已成为 development construction，相应不能再称 validation；c21-c23 因
v5.1 已打开，只能继续作为 retrospective diagnostic。v5.3 的 377 结果即使通过，
也只能冻结算法并进入全新五主体 LOSO，不能直接作为论文 promotion test。

正式 development runner 于北京时间 `2026-07-30 20:16:44` 启动，绑定实现提交
`73fd92ff6754e95a3ef1c32371b46b4c27eafad2`。八折 capacity 于 `20:18:39`
完成，耗时 `115s`，随后进入 c21 retrospective 渲染。按同协议历史实测，预计于
北京时间 `2026-07-30 20:35` 左右完成 c21-c23、spatial test 和最终汇总。

实际于北京时间 `2026-07-30 20:34:45` 完成。八相机 construction 为 `8/8 pass`；
c21-c23 outer 下降 `0.515%`，boundary 下降 `0.487%`，三相机两项均同向，target、
visibility、soft-IoU、spatial、coverage、macro/micro IoU 全部通过。唯一失败为
retrospective boundary 未达到 `0.5%`，差 `0.0132` 个百分点。时间块复核显示
boundary 为 `16/18` 块改善，但 outer 仅 `9/18` 块改善，说明完整序列平均值仍掩盖
局部 temporal regression。

#### Task 11 v5.4 camera-time stability selection（2026-07-31，capacity 判拒，未启动 runner）

v5.4 将每个 construction 相机的 114 帧固定分成 6 个连续块，执行
`8 camera folds × 6 time folds = 48` 个嵌套折。每折排除完整 held-out camera，并从
其余相机排除同一个时间块；support 不允许跨被删除时间块形成伪 adjacent pair。
最终 lower carrier 必须至少在 `36/48` 折被选中，且不允许 full-data greedy 追加。
c21-c23 不参与 candidate ranking、threshold、pass/fail 或 runner success。

冻结 gates：

```text
aggregate outer/boundary gain >= 0.75%
lower-only outer/boundary gain >= 1.5%
positive block fraction >= 44/48
10th-percentile block gain >= 0
worst block regression <= 0.5%
target >= 0.995, visibility <= 0.998, evidence soft-IoU drop <= 0.005
hair exact A5, 5%/10% actions, maximum changed fraction 20%
```

第一次正式 capacity 暴露两个实现问题：19 帧 held-out block 被错误施加完整相机
target/visibility gate，以及 fold 直接复用全局 v4 seed。前者按设计修正为 block 只检查
temporal direction；后者恢复为 fold-local v4 seed。两项均通过 TDD 后重新运行正式
capacity。

第二次正式 capacity 于北京时间 `2026-07-31 00:02:21` 输出。结果仍被拒绝：

```text
fold pass = 14/48
36/48 consensus carrier count = 6
aggregate outer/boundary gain = 0.462% / 0.238%
lower-only outer/boundary gain = 0.924% / 0.475%
outer positive block fraction = 47/48
boundary positive block fraction = 40/48
outer/boundary 10th-percentile block gain = 0.241% / -0.297%
worst outer/boundary block regression = 0.986% / 1.294%
target minimum = 0.999188
visibility maximum = 1.000423
```

因此当前静态 lower carrier 在严格 stability consensus 下 capacity 不足。按照预注册
防过拟合协议，本轮没有降低 `36/48`、没有放宽 block gates、没有增加 hair、没有扩大
action grid，也没有启动会确定性失败的正式 runner。下一步不能继续在 CoreView377
c21-c23 上微调；应先讨论静态 bank 架构是否需要改为跨主体共享的 carrier prior 或
pose-conditioned A7b，并重新写设计与 untouched-subject 协议。

---

### Task 12: 执行五主体 evidence、validation 和 LOSO 冻结

**Files:**
- Output: `exp/acceptdata/a7_temporal_reliability_v1/evidence/{377,386,387,393,394}/`
- Output: `exp/acceptdata/a7_temporal_reliability_v1/validation/{subject}/`
- Output: `exp/acceptdata/a7_temporal_reliability_v1/loso/{held_out_subject}/`
- Modify: `docs/A7时序可靠性校准实验记录_20260728.md`

- [ ] 逐主体运行 evidence 和最多 4 candidate validation；一个 GPU 顺序执行，支持断点恢复。
- [ ] 每完成一个主体即核对 evidence coverage、candidate count、summary complete 和 fingerprints，不等全部结束再发现缺项。
- [ ] 分别对 377/386/387/393/394 做五次 LOSO：每次只读另外四主体 c17-c20 validation。
- [ ] 生成 `aggregate/loso_freeze_manifest.json`，记录五个 held-out 的 selected/rejected 状态和完整 fingerprint。
- [ ] 冻结后把 manifest 设为只读副本，并在实验记录抄录 SHA-256；后续任何修改都必须产生新的 protocol version，不能覆盖 v1。
- [ ] 如果任一 held-out 无 eligible candidate：该主体 A7 判拒；不得为凑五主体主表采用自身 validation 或 A5/A7 混合协议。
- [ ] 汇总当前能否进入 retrospective c21：只有五个 held-out 都得到合规选型时才跑正式五主体 A7 c21；否则保留 A5 主方法并记录 A7 失败。
- [ ] 提交记录：

```bash
git add docs/A7时序可靠性校准实验记录_20260728.md
git commit -m "实验：冻结A7五主体留一法配置"
```

---

### Task 13: 回顾性 c21 测试与统计显著性

**Files:**
- Output: `exp/acceptdata/a7_temporal_reliability_v1/frozen_test/{subject}/c21/`
- Create: `tools/summarize_a7_temporal_reliability.py`
- Create: `tests/test_summarize_a7_temporal_reliability.py`
- Modify: `docs/A7时序可靠性校准实验记录_20260728.md`

- [ ] 先为汇总器写失败测试：subject-equal macro 与 pooled micro 分开；frame/part 不得伪装为独立 subject 样本。
- [ ] 固定读取 Task 12 manifest 中每个 held-out 的唯一配置，在 c21 `0:570:1` 渲染 Voting/A5/A7。
- [ ] 输出每主体、每 part、总体的 fixed/adaptive temporal 指标，以及 spatial/coverage guards。
- [ ] 显著性以 subject 为配对单位，报告 A7-A5 的 bootstrap 95% CI 和 exact paired sign/permutation test；五主体小样本不能只报 p 值。
- [ ] 明确标注 c21 为 `retrospective_test=true`，不得称 unseen/untouched。
- [ ] A7 进入下一阶段的回顾性门槛：

```text
fixed outer flicker A7-A5 显著下降
fixed boundary flicker A7-A5 显著下降
至少 4/5 主体两项都改善
formal eligible parts 不减少
real-edit pooled burden 相对恶化 <=2%
macro mIoU 下降 <=0.01
micro IoU 下降 <=0.005
```

- [ ] 如果不通过：A7 不升级为主方法，停止正式宣传；保留消融/负结果，不用 c21 反向改参数。
- [ ] 运行测试并提交：

```bash
python -m pytest tests/test_summarize_a7_temporal_reliability.py -q
git diff --check
git add tools/summarize_a7_temporal_reliability.py \
  tests/test_summarize_a7_temporal_reliability.py \
  docs/A7时序可靠性校准实验记录_20260728.md
git commit -m "实验：汇总A7五主体回顾性时序结果"
```

---

### Task 14: 冻结 c22-c23 与未触碰主体确认

**Files:**
- Output: `exp/acceptdata/a7_temporal_reliability_v1/frozen_test/{subject}/{c22,c23}/`
- Modify: `docs/A7时序可靠性校准实验记录_20260728.md`

- [ ] 在打开任何 c22/c23 指标前，把 camera、frame stride、LOSO manifest、汇总脚本 commit 和输出 schema 写入实验记录并提交。
- [ ] 对五主体按冻结配置运行 c22-c23；资源不足时使用预先提交的统一 stride，不能看结果后改变。
- [ ] 选择一个从未参与当前方法开发、阈值选择或结果查看的新主体；先记录 subject、数据准备、camera split、checkpoint/bank、可用性检查和预注册门槛。
- [ ] 新主体 A7 配置只能使用五主体 validation 冻结出的全局规则，不能用新主体 validation 选 candidate；若论文采用 LOSO 扩展，则需在测试前写清唯一映射规则。
- [ ] untouched confirmation 最低要求：fixed outer 与 boundary flicker 都低于 A5，同时 coverage/spatial/semantic guards 均通过。
- [ ] 若新主体不通过，论文不得使用“泛化时序稳定性已解决”的强表述；A5 仍为主方法，A7 只能作为探索或受限扩展。
- [ ] 提交冻结确认记录：

```bash
git add docs/A7时序可靠性校准实验记录_20260728.md
git commit -m "实验：完成A7冻结视角与未触碰主体确认"
```

---

### Task 15: 完成 A7 消融、正式主表、视频和失败案例

**Files:**
- Modify: `tools/run_a7_temporal_reliability_queue.sh`
- Modify: `tools/summarize_a7_temporal_reliability.py`
- Create: `docs/A7论文正式结果与结论_20260728.md`
- Modify: `docs/当前项目论文创新点与快速投稿补强交接_20260718.md`

- [ ] 只在 Task 13/14 promotion gates 通过后，把 A7 作为候选主方法生成正式表；否则 A5 保持主方法。
- [ ] 固定消融：

```text
A5   = frozen spatial method
A7-V = visibility normalization/support gate only
A7-T = temporal reliability damping without redistribution
A7-R = redistribution with target/outer mean evidence but no flicker penalty
A7   = full temporal reliability + redistribution
```

- [ ] 正式表包括：五主体/新主体 temporal、semantic、matched-retention spatial、coverage-constrained real editing、reconstruction、complexity。
- [ ] Reconstruction 用同一未编辑渲染结果核对 PSNR/SSIM/LPIPS；理论上 A7 不改变未编辑 Avatar，差异应在浮点/no-practical-effect 范围。
- [ ] Complexity 报告 evidence 建设时间、candidate validation 时间、bank 大小和运行时额外开销；A7 推理阶段应无额外网络、无上一帧缓存。
- [ ] 连续视频并排显示 Voting/A5/A7，同步相机、帧、strength、颜色和裁剪，叠加 part/subject/camera，不加入时域后处理。
- [ ] 失败案例必须保留：face/skin/shoes、CoreView_377 lower、coverage 不足 part、A7 仍闪烁或重分配 capacity 不足场景。
- [ ] 论文 claim 仅在证据支持时使用：

```text
We extend evidence-calibrated persistent semantic assets with
visibility-aware temporal reliability calibration, rebalancing canonical
edit carriers to reduce pose-induced screen-space flicker while preserving
matched-retention spatial leakage and coverage.
```

- [ ] 最终验证：

```bash
python -m pytest \
  tests/test_temporal_reliability_calibration.py \
  tests/test_a7_part_label_bank.py \
  tests/test_build_temporal_reliability_evidence.py \
  tests/test_calibrate_temporal_reliable_a7_weights.py \
  tests/test_a7_method_integration.py \
  tests/test_select_loso_a7_temporal_config.py \
  tests/test_run_a7_temporal_reliability_queue.py \
  tests/test_summarize_a7_temporal_reliability.py -q
bash -n tools/run_a7_temporal_reliability_queue.sh
python tools/validate_frozen_semantic_method.py \
  --config configs/semantic/frozen_a7_temporal_reliable_v1.json \
  --base-config configs/semantic/frozen_a5_main_method_v1.json
git diff --check
```

- [ ] 最终中文提交：

```bash
git add docs/A7论文正式结果与结论_20260728.md \
  docs/当前项目论文创新点与快速投稿补强交接_20260718.md
git commit -m "论文：补齐A7时序可靠性正式实验与结论"
```

---

## A7b 启动条件

本计划不得自动实现 pose-conditioned A7b。只有以下四项同时成立，才另开设计讨论：

```text
1. 静态 A7 已通过 spatial/coverage/semantic guards；
2. 静态 A7 在 c17-c20 validation 仍无法降低 fixed flicker；
3. evidence 明确显示 stable carrier capacity 不足，而非指标/实现错误；
4. 团队接受重训、额外消融和重新准备 untouched test 的成本。
```

A7b 必须另写设计和协议，不能利用 c21/c22-c23 test 结果选网络结构或超参数。

---

## 新对话开场指令

在新对话中直接使用下面这段指令：

```text
请在 /remote-home/ming/3dgs-avatar-release-main 当前分支上执行 A7 实施计划：
docs/A7时序可靠性校准实施计划与新对话交接_20260728.md

先完整读取该计划和：
docs/superpowers/specs/2026-07-28-a7-temporal-reliability-calibration-design.md
configs/semantic/frozen_a5_main_method_v1.json

使用 executing-plans 或 subagent-driven-development 技能，严格按任务顺序和 TDD 执行。
保持 A5 冻结，不覆盖用户现有修改，不使用 c21/c22/c23 调参。
先完成 Task 1-9 的代码、测试和 dry-run；通过后再执行 Task 10 的 377 短 canary。
每个任务独立中文提交，并汇报测试证据、commit、GPU 进程和预计北京时间。
遇到 promotion gate 失败时停止后续正式测试，先报告原因，不自动转 A7b。
```

## 完成定义

A7 开发不能以“代码能运行”作为完成。只有以下全部成立才算论文级闭环：

```text
实现和证据可复现；
五主体 LOSO 无泄漏；
c21 只作 retrospective；
c22-c23 和新主体在冻结后确认；
fixed outer/boundary flicker 均显著改善；
至少 4/5 回顾性主体两项改善；
coverage、spatial burden、semantic、reconstruction guards 全部通过；
消融能分离 visibility、temporal damping、redistribution 的贡献；
视频、失败案例、复杂度和统计报告齐全；
所有配置、bank、报告都有可核验 fingerprint。
```
