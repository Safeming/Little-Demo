# 多视角清晰度采样根因与 v165 实验计划

日期：2026-04-25

## 当前结论

v164b 验证了显式绑定路线是可行的，但还没有解决清晰度根因。

v164b best 出现在 1500：

```text
FG_LPIPS = 0.160253
L1_fg    = 0.048948
SSIM_fg  = 0.852758
```

它明显优于 v160a、v163c、v158a：

```text
v158a FG_LPIPS 0.168829
v160a FG_LPIPS 0.166306
v163c FG_LPIPS 0.170153
v164b FG_LPIPS 0.160253
```

但仍然明显弱于 v142e 和 mono baseline：

```text
v142e train FG_LPIPS    0.139722
baseline render LPIPS   0.021162
v142e render LPIPS      0.023021
v164b render LPIPS      0.025965
```

机制日志说明：v164b 的几何硬化开关已经打开，但 hardest contour 没有真正被 boundary/head 强接管。

```text
support          ~= 0.916
owner_takeover   ~= 0.36 - 0.53
boundary_takeover~= 0.02 - 0.04
boundary_focus   ~= 0.01 - 0.03
sharpen_gate     ~= 0.99
```

所以它不是“显式绑定失败”，而是“显式绑定能稳定出完整人，但多视角监督仍把边界和高频平均软了”。

## 三个主因

### 1. 多视角采样稀释 / 训练不够

当前训练集约为：

```text
570 frames * 20 train cameras = 11400 samples
```

旧的随机采样逻辑每个 iteration 只取一个 camera-frame：

```python
data_stack = list(range(len(scene.train_dataset)))
data_idx = data_stack.pop(randint(0, len(data_stack)-1))
data = scene.train_dataset[data_idx]
```

因此 15k iterations 的真实样本曝光约为：

```text
15000 / 11400 = 1.32 次 / 样本
```

即使累计到约 24k render exposure，也只有：

```text
24000 / 11400 = 2.1 次 / 样本
```

这和 mono 15k 的约 26 次 / 样本不是一个训练密度。多视角下人能出来，但手臂、腰侧、衣服褶皱、脸部、鞋边等高频区域没有被充分拟合。

### 2. SMPL / 相机小对齐误差 + 显式绑定偏软

前面抽查过，整体对齐合理，但 768 分辨率下仍有约 5-16px 的局部中心偏差。这个量级足够把边界、手臂、腰侧和衣服褶皱平均掉。

v164b 的 sharpen gate 已开，但 boundary takeover 很低，说明几何硬化开始工作，hardest contour 仍没有足够强的、稳定的梯度接管。

### 3. 多视角颜色 / 曝光不一致

20 个训练相机亮度、白平衡、颜色质量不同。统一 texture MLP 会倾向解释平均颜色，质量差或偏色视角抽得太多时，会进一步压低纹理高频。

## 方案判断

直接把每个样本训练到 25 次需要：

```text
11400 * 25 = 285000 render exposure
```

如果使用 K=4 multi-view gradient accumulation：

```text
285000 / 4 = 71250 optimizer steps
```

这在当前 3 秒左右 / render 的成本下太重，不适合作为第一步。

更合理的路线是分阶段验证：

```text
v165: frame-balanced + camera-quality weighted, K=1
v166: same-frame K=2 gradient accumulation
v167: same-frame K=4 late finetune
```

v165 的目的不是一次性达到 baseline，而是先验证“采样根因”是否成立。它保持每步一个 render/backward，成本和现有训练一致。

## v165 采样策略

v165 使用：

```text
先均匀/轮转抽 frame
再在该 frame 内按 camera quality weight 抽 camera
每个 camera 设置 min_prob / max_prob，避免低权重相机被饿死
```

相机权重：

```yaml
camera_sampling_weights:
  "1":  1.52
  "2":  0.54
  "3":  0.90
  "4":  0.60
  "5":  1.44
  "6":  1.09
  "7":  1.21
  "8":  1.24
  "9":  1.32
  "10": 1.41
  "11": 0.80
  "12": 0.89
  "13": 1.05
  "14": 1.19
  "15": 0.82
  "16": 0.87
  "17": 0.77
  "18": 0.68
  "19": 0.95
  "20": 0.74
```

概率约束：

```text
min_prob = 0.025
max_prob = 0.100
```

这样既让优质相机提供更多清晰监督，又不完全牺牲相机 20 等接近 held-out view 的泛化视角。

## 预期

v165 可改善：

```text
多视角采样稀释
高质量视角曝光不足
低质量相机拖平均
部分边界/纹理梯度不稳定
```

v165 不能彻底解决：

```text
SMPL/相机局部错位
严重颜色/曝光差异
boundary head 接管不足
```

如果 v165 有明显收益，再进入 K=2/K=4 same-frame gradient accumulation。否则不应继续在 K accumulation 上耗算力，应优先做 per-camera color/exposure correction 或对齐误差建模。

## v165 训练记录

已实现代码：

```text
train.py 新增配置化 sampler:
  opt.train_sample_mode: frame_balanced_camera_weighted
  opt.train_sample_camera_weights
  opt.train_sample_camera_min_prob
  opt.train_sample_camera_max_prob
```

默认训练逻辑不变。只有新配置显式启用 `frame_balanced_camera_weighted` 时，才替换旧的随机 camera-frame 抽样。

新增配置：

```text
configs/option/stageA_377_multiview_explicit_hq_v165_v164b_framebalanced_camquality_v1.yaml
```

新增启动脚本：

```text
tools/start_377_stageA2_multiview_explicit_rootfix_resume_v165_v164b_framebalanced_camquality.sh
```

验证：

```text
python -m py_compile train.py 通过
1 iter smoke 通过
sampler 识别:
  frames=570
  samples=11400
  cameras=1..20
```

正式训练：

```text
实验名:
  v165_v164b_framebalanced_camquality_screen6k

exp:
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v165_v164b_framebalanced_camquality_screen6k

start_checkpoint:
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v164b_v164a_texture_restore_contourlock_screen2k/best_ckpt.pth

log:
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/logs/v165_20260425_115553_framebalanced_camquality.log

pid:
  wrapper 3253321
  train   3253326

启动时间:
  2026-04-25 11:55:53 CST

预计结束:
  2026-04-25 17:20 CST 左右
  当前早期速度约 3.2s/iter，实际可能在 17:05-17:35 CST 区间
```

当前启动日志确认：

```text
[TrainSampler] mode=frame_balanced_camera_weighted frames=570 samples=11400 cameras=['1'...'20'] min_prob=0.0250 max_prob=0.1000
Training progress: 10/6000, early speed ~= 3.28s/it
```

评估点：

```text
1000, 2000, 3000, 4000, 5000, 6000
```

验收口径：

```text
首先看 best FG_LPIPS 是否优于 v164b 0.160253
同时看 render LPIPS 是否优于 v164b_render 0.025965
肉眼重点看 c22_f000210 / c22_f000240:
  手臂边界
  肩膀外沿
  腰侧
  衣服褶皱
  鞋边
```

## 2026-04-26 补记：v165 之后的实验结果

### v165 结果：采样方向有弱信号，但不能作为主线继续堆

v165 使用 `frame_balanced_camera_weighted`，从 v164b best 继续训练。best 出现在 1000：

```text
v164b best:
  FG_LPIPS = 0.160253
  L1_fg    = 0.048948
  SSIM_fg  = 0.852758

v165 best@1000:
  FG_LPIPS = 0.159906
  L1_fg    = 0.050364
  SSIM_fg  = 0.855845
```

继续训练没有带来稳定收益：

```text
v165 @2000 FG_LPIPS = 0.165815
v165 @3000 FG_LPIPS = 0.169767
```

机制上，v165 的 owner 输出和 sharpen gate 都打开了，但 hardest contour 没有真正被强力接管：

```text
iter=3000
support           = 0.9172
owner_takeover    = 0.6458
boundary_focus    = 0.0193
boundary_takeover = 0.0330
boundary_color    = 0.00961
sharpen_gate_mean = 0.992890
```

结论：

```text
frame-balanced + camera-quality weighted 证明“采样稀释”是因素之一，
但它不是当前最主要瓶颈。

v165 只能把 v164b 从 0.160253 小幅推到 0.159906，
且后续训练迅速变差，说明继续堆 K=2/K=4 accumulation 的性价比不高。

当前更关键的问题仍是：
  1. boundary/head 接管不足；
  2. trunk/HF/contour 残差没有稳定进入主输出；
  3. 多视角对齐/光照误差仍会把边界平均软。
```

因此 v165 之后没有继续走“大算力 accumulation 主线”，而是转向更直接的清晰度瓶颈排查。

### v166 / v166b / v166c：photometric correction 与 uncertainty 验证

v166 目标是验证多视角曝光/颜色不一致是否是当前主因。结果不成立，甚至明显恶化：

```text
v166 photometric + contour uncertainty + owner cap:
  best@500
  FG_LPIPS = 0.190714
  L1_fg    = 0.051581
  SSIM_fg  = 0.821382

v166b light photo / light uncertainty / owner window:
  best@500
  FG_LPIPS = 0.190205
  L1_fg    = 0.048842
  SSIM_fg  = 0.827329
```

v166c 退回 identity continuation，只保留较保守延续，恢复到 v165 附近：

```text
v166c identity continuation:
  best@2000
  FG_LPIPS = 0.159078
  L1_fg    = 0.050535
  SSIM_fg  = 0.863225
```

机制观察：

```text
v166 / v166b 的 photometric 和 uncertainty 在当前显式绑定输出仍偏软时介入，
容易把颜色/边界误差解释成可校正项，反而削弱真实纹理与轮廓梯度。

v166c 能恢复，是因为它基本回到保守 continuation；
这说明光照校正方向不是完全错误，但不适合作为当前阶段的主药。
必须先让主干输出高频/轮廓残差有足够幅度，再考虑 per-camera correction。
```

### 采样结论更新

原始判断里，“多视角采样稀释 / 训练不够”是第一主因。v165-v166 之后需要修正为：

```text
采样稀释是必要修复项，但不是单独足以逼近 baseline 的根因。

当模型主路径本身不能输出足够高频/硬边界时，
更均衡或更高质量的采样只会给它更好的监督，
但不会自动让软输出头变成硬输出头。

所以采样策略保留为辅助配置：
  frame-balanced camera weighted 可以继续用于后续长训；
  但下一主线必须修 trunk/HF/contour 输出幅度与路由。
```

## 2026-04-26 补记：从 v142e 回到 trunk/HF/contour 根因

### 为什么回到 v142e 附近

v142e 仍是显式绑定清晰度锚点之一：

```text
v142e:
  FG_LPIPS = 0.139722
  L1_fg    = 0.042726
  SSIM_fg  = 0.879750
```

v164b / v165 虽然证明显式绑定路线能稳定训练，但离 v142e 仍差很远：

```text
v164b FG_LPIPS = 0.160253
v165  FG_LPIPS = 0.159906
v142e FG_LPIPS = 0.139722
```

这说明从 v164/v165 继续修采样、光照和 uncertainty，更多是在偏软底座上补；真正高价值方向是回到 v142e 清晰锚点，排查为什么后续 trunk/HF/boundary 改造没有真正把高频主路径做强。

### v167a / v168a / v169a：主干高频重锚定

```text
v167a trunkhf_localcolor_reanchor:
  best@82900
  FG_LPIPS = 0.140530
  L1_fg    = 0.042753
  SSIM_fg  = 0.880634

v168a isolated_trunkhf:
  best@83400
  FG_LPIPS = 0.139709
  L1_fg    = 0.043080
  SSIM_fg  = 0.880857

v169a hardcontour_boundaryactuator:
  best@84900
  FG_LPIPS = 0.139954
  L1_fg    = 0.042581
  SSIM_fg  = 0.880252
```

结论：

```text
v168a 基本回到并略好于 v142e 的 FG_LPIPS，
说明 trunk/HF 隔离训练是有效方向。

v169a 引入 hard contour actuator 后 L1_fg 变好，
但 FG_LPIPS 没同步变好，说明轮廓误差有局部改善，
但 boundary 输出幅度仍不足以带来肉眼硬边界突破。
```

v169a 机制日志的关键问题：

```text
boundary_focus    ~= 0.014 - 0.016
boundary_takeover ~= 0.008
boundary_color    ~= 0.0002
sharpen_gate_mean = 0
```

也就是说，hard contour 路由有了，但真正输出的 boundary color 几乎为零。

### v170 批处理：tag focus / residual / photometric 的横向验证

v170 批处理结果：

```text
v170a tagfocus_boundaryactivate:
  FG_LPIPS = 0.139949
  L1_fg    = 0.043635
  SSIM_fg  = 0.879389

v170b tagfocus_boundarylocalout:
  FG_LPIPS = 0.140514
  L1_fg    = 0.043185
  SSIM_fg  = 0.879476

v170c residualonly_contour:
  FG_LPIPS = 0.139496
  L1_fg    = 0.042802
  SSIM_fg  = 0.881312

v170d tagfocus_strongresidual:
  FG_LPIPS = 0.139748
  L1_fg    = 0.043206
  SSIM_fg  = 0.879919

v170e tagfocus_photo_contour:
  FG_LPIPS = 0.139228
  L1_fg    = 0.043631
  SSIM_fg  = 0.880144

v170f framebalanced_photo_texturerestore:
  FG_LPIPS = 0.139253
  L1_fg    = 0.043154
  SSIM_fg  = 0.881344
```

v170e / v170f 是当前指标上最好的显式绑定版本，但视觉对比仍明显软于 mono baseline。它们相比 v142e 只有小幅提升，不是根因突破。

v170 的机制结论：

```text
tag focus 可以把 hardest contour 子集稳定到约 0.08 - 0.10；
residual-only / photometric 分支能带来轻微 LPIPS 收益；
但 boundary/head 的直接输出幅度仍偏小。

典型现象：
  boundary_focus 已经不再是 0.01 级别；
  boundary_takeover 只有约 0.05 - 0.06；
  boundary_color 仍约 0.001；
  sharpen_gate 仍为 0。
```

因此 v170 后的判断是：

```text
问题不再是“找不到 hardest contour”，
而是“找到了 hardest contour，但输出头没有足够幅度把它画硬”。
```

## 当前主线：v171a direct trunk / contour amplitude

v171a 已启动，用于验证“主干输出高频/轮廓残差真正有幅度”这个根因。

```text
实验名:
  v171a_v170e_directtrunk_contouramp_screen2500

start_checkpoint:
  v170e best_ckpt.pth

启动时间:
  2026-04-26 09:44:06 CST

预计结束:
  2026-04-26 11:15 - 11:35 CST
```

关键改动：

```text
1. 解除 direct trunk output head / HF head / local color / owner head / boundary head 的训练；
2. 加 gate max cap，避免 gate 全开导致 v166c 式 washout；
3. 保留 photometric correction 和 contour uncertainty 作为辅助，不作为主药；
4. 清空并重打 boundary tag，topk_ratio=0.10；
5. 冻结几何、densify、opacity、scaling、features，只看 texture/head/contour 残差能否变强。
```

500 步早期结果：

```text
v171a @500:
  FG_LPIPS = 0.140413
  L1       = 0.003215
  FG_PSNR  = 21.665016

早期机制:
  support           ~= 0.959 - 0.962
  owner_takeover    ~= 0.63
  boundary_focus    = 0.1000
  boundary_takeover = 0.1000
  boundary_color    ~= 0.001 - 0.004
  sharpen_gate_mean = 0
```

阶段判断：

```text
v171a 的 500 步还没有超过 v170e 的 0.139228，
但它已经把 boundary_focus / boundary_takeover 稳定推到 0.10。

真正要看的不是 500 步是否立刻破 best，
而是 1000 / 1500 / 2500 时 boundary_color 和 direct head 幅度是否继续上来，
以及渲染里 c22_f000210 / c22_f000240 的手臂边界、腰侧、衣服褶皱、鞋边是否比 v170e 更硬。
```

## 当前总判断

截至 v171a 启动后，路线判断更新为：

```text
1. 采样稀释是真问题，但 v165 证明单独修采样只能带来弱收益。
2. photometric / uncertainty 不能在软主路径之前先上，v166 已验证会恶化。
3. v142e / v168a / v170e 说明显式绑定可以做到 0.139 附近。
4. 距离 baseline 的主要差距不是“人不完整”，而是高频和边界硬度不足。
5. 当前最高价值修复是让 trunk/HF/boundary/contour 残差在主输出里有真实幅度，同时保持 selective gate。
```

后续不应再回到“单纯加权、单纯加步数、单纯调 boundary tag”的小修小补。判断一版是否值得长训，应同时满足：

```text
指标:
  FG_LPIPS 明显低于 v170e 0.139228；
  L1_fg 不明显劣化，最好接近或低于 0.0428；
  SSIM_fg 不低于 0.881 附近。

机制:
  boundary_focus 保持 0.08 - 0.10；
  boundary_takeover 不只是 0.10 的路由数字，而是 boundary_color / head_color 有真实输出幅度；
  gate 不全开，不把整个人洗平；
  trunk/HF/head 输出残差不能长期停在接近 0 的幅度。

视觉:
  c22_f000210 / c22_f000240 的手臂边界、肩膀、腰侧、衣服褶皱、鞋边必须肉眼比 v142e/v170e 更硬；
  不能新增鼓包、腰侧外扩、手臂边界漂移。
```

## 2026-04-26 补充：v172 失败结论与 v173a 安排

### v172 结果

`v172a` 是 1 step layer-sharpen 探针：

```text
v172a @86401
FG_LPIPS = 0.139242
L1_fg    = 0.043622
FG_PSNR  = 21.774441
SSIM_fg  = 0.880133
```

结论：单独打开轻量 layer-sharpen 不会立刻破坏 `v170e`。

`v172b` 继续训练 1500 步：

```text
v172b @87900
FG_LPIPS = 0.147685
L1_fg    = 0.044183
FG_PSNR  = 21.650383
SSIM_fg  = 0.877301
```

机制和视觉结论：

```text
sharpen_gate_mean ~= 0.70，说明 sharpen 路径已经启动；
boundary_color 仍只有约 0.0024 - 0.0030；
boundary_takeover = 0.0592；
owner_takeover 被压到约 0.36；
渲染没有变清楚，并出现头顶上方竖向半透明拖影/漏色。
```

因此 `v172b` 不作为主线。它给出的结论是：

```text
1. 多视角采样稀释不是唯一问题；
2. layer-sharpen 打开不是直接解法；
3. boundary opacity/scaling residual 与 layer-sharpen 一起训会破坏 v170e anchor；
4. 下一步应先冻结 geometry/opacity/scaling，只训练 owner/boundary texture head，验证残差幅度能否安全提升。
```

### v173a 当前安排

实验配置：

```text
configs/option/stageA_377_multiview_explicit_hq_v173a_v170e_textureboundary_leakguard_v1.yaml
tools/run_377_stageA2_v173_textureboundary_leakguard.sh
```

启动点：

```text
v170e best_ckpt.pth
```

核心策略：

```text
从 v170e 回退；
不训练 geometry / opacity / scaling / boundary opacity-scaling residual；
只训练 owner head + boundary head texture MLP/gate；
保持 owner 主路径，不再像 v172b 那样压低 owner_takeover；
boundary tag 继续使用 topk_ratio=0.08；
目标是在不新增背景泄漏的前提下，让 boundary/head 纹理残差真正有幅度。
```

## 2026-04-26 晚间补充：v173-v175 结果与 v176 same-frame K=2 计划

这一段补齐 `v173a / v174a / v175a` 的结果，并把后续采样主线重新接回本文档最初的三个主因。

### v173a / v174a：继续拧 boundary/head 不是主突破口

`v173a` 从 `v170e` 回退，只训练 owner head + boundary head texture MLP/gate，冻结 geometry / opacity / scaling，目标是在不引入 `v172b` 漏色的前提下提高 boundary/head 残差幅度。

结果：

```text
v173a textureboundary_leakguard:
  best iter = 86900
  FG_LPIPS = 0.139833
  L1_fg    = 0.043418
  FG_PSNR  = 21.769121
  SSIM_fg  = 0.881153

v174a boundarydirect_rgbcompose:
  best iter = 87900
  FG_LPIPS = 0.140044
  L1_fg    = 0.043677
  FG_PSNR  = 21.745602
  SSIM_fg  = 0.879624
```

结论：

```text
v173a 没超过 v170e 0.139228；
v174a 即使把 boundary RGB composition 的 effective gate 推到很高，
boundary_color 仍停在约 0.001 - 0.002。

这说明问题不是“boundary gate 没接进最终颜色”这么小，
而是 boundary/head 本身没有足够高频源幅度。
继续沿 v173/v174 方向堆 boundary head，不是高价值主线。
```

### v175a：高频源修复第一次带来明确突破

`v175a` 从 `v170e` best 启动，冻结 geometry / opacity / scaling / feature，只训练 `detail_high_freq_*` 和 `structured_trunk_output_head_hf_head_*`，并保留 frame-balanced camera-quality sampling 与 image-space edge/detail/luma-DOG supervision。

结果：

```text
v175a hfsource_edgesupervise:
  best iter = 88900
  FG_LPIPS = 0.134804
  L1_fg    = 0.043218
  FG_PSNR  = 21.708366
  SSIM_fg  = 0.883234
```

对比：

```text
v142e  FG_LPIPS = 0.139722
v170e  FG_LPIPS = 0.139228
v173a  FG_LPIPS = 0.139833
v174a  FG_LPIPS = 0.140044
v175a  FG_LPIPS = 0.134804
```

机制信号：

```text
support        ~= 0.960
owner_takeover ~= 0.650
hf_scale       = 0.240
luma           ~= 0.022 - 0.024
tiny           ~= 0.016 - 0.019
boundary_color ~= 0.0007 - 0.0011
boundary_focus = 0.080
```

视觉复核：

```text
对比目录:
  /remote-home/ming/3dgs-avatar-release-main/outputs/v175a_compare

v175a 的衣服褶皱、裤子暗部纹理、鞋边比 v170e / v174a 更清楚；
腰侧和腿部边缘有收紧；
但手臂外轮廓、肩膀外轮廓、头发边缘仍比 mono baseline 软，
肩膀到上臂还有轻微膨胀/平均感。
```

因此，`v175a` 的结论是：

```text
高频源/HF source 是 v170e 之后最关键的前置瓶颈。
v165 的采样修复当时收益弱，不代表采样方向错；
更准确地说，是当时主干还没有足够高频输出幅度，
采样改善没有可用通路承载。
```

### 三个主因的当前修复进度

```text
1. 多视角采样稀释 / 训练不够:
   已验证是真问题，已实现 K=1 frame-balanced + camera-quality weighted；
   但 v165 证明单独修采样收益弱。
   现在 v175a 已经修出 HF source 幅度，采样主因需要重新验证。

2. SMPL/相机小对齐误差 + 显式绑定偏软:
   还没有根治。
   v175a 缓解了纹理和局部细节软化，但手臂/肩膀 hardest contour 仍软于 baseline。

3. 多视角颜色 / 曝光不一致:
   还没有根治。
   v166 提前上 photometric correction 会恶化，说明不能在软主路径之前先做颜色校正。
   当前只是通过 camera-quality sampling 降低差视角权重，没有做真正 per-camera color/exposure model。
```

### v176a：从 v175a 进入 same-frame K=2 accumulation

现在可以重新回到本文档最初设想的 `same-frame multi-view gradient accumulation`，但前提已经变了：

```text
旧前提:
  主干软，HF source 没幅度；
  v165 单独修采样只能得到弱信号。

新前提:
  v175a 已经把 HF source 拉起来；
  需要验证更密的同帧多视角监督能否继续把清晰度往 baseline 推。
```

已落地代码：

```text
train.py:
  新增 opt.train_sample_accumulation_steps
  新增 opt.train_sample_accumulation_without_replacement
  frame_balanced_camera_weighted sampler 支持同一 frame 连续抽 K 个 camera
  每个 micro-view 的 loss 除以 K
  第 K 个 micro-view 后再 optimizer step
```

已落地配置：

```text
configs/option/stageA_377_multiview_explicit_hq_v176a_v175a_sameframe_k2_v1.yaml
tools/run_377_stageA2_v176_sameframe_k2.sh
```

实验设置：

```text
start_checkpoint:
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v175a_v170e_hfsource_edgesupervise_screen2500/best_ckpt.pth

experiment:
  v176a_v175a_sameframe_k2_hfsource_screen3000

micro iterations:
  3000

effective optimizer updates:
  1500

sampling:
  frame_balanced_camera_weighted
  train_sample_accumulation_steps = 2
  train_sample_accumulation_without_replacement = true
  camera-quality weights / min_prob / max_prob 继承 v175a

HF/loss setup:
  继承 v175a，不改 HF source / luma / edge / detail loss
```

v176a 要证实的问题：

```text
核心问题:
  v175a 让高频输出通路有幅度之后，
  剩余清晰度瓶颈是否主要来自多视角监督稀释？

如果 v176a 比 v175a 更清楚:
  说明 v165 方向成立，只是必须先修 HF source；
  下一步可以做 K=4 或 5k-8k 长训。

如果 v176a 指标略好但手臂/肩膀更软:
  说明 dense multi-view supervision 已经撞到 SMPL/相机局部对齐误差；
  下一步要做 contour uncertainty / view consistency mask，而不是继续加 K。

如果 v176a 整体变灰或高频下降:
  说明多视角颜色/曝光不一致成为主瓶颈；
  下一步才上保守 per-camera color/exposure correction。
```

v176a 验收线：

```text
指标:
  FG_LPIPS < 0.134804
  L1_fg    <= 0.0433
  SSIM_fg  >= 0.883

机制:
  hf_scale / luma / tiny 不能掉回 v170e 水平；
  owner_takeover 保持约 0.60 - 0.66；
  boundary_color 不作为主验收指标，但不能伴随背景漏色或轮廓漂移。

视觉:
  必看 c22_f000210 / c22_f000240；
  手臂边界、肩膀、腰侧、衣服褶皱、鞋边必须至少不劣于 v175a；
  最理想是衣服/鞋边继续变锐，同时手臂/肩膀不被多视角平均糊。
```

## 2026-04-27 补充：v176-v178 same-frame 采样验证结果

### v176a / v177a：K=2 与 K=4 都成立

v176a 从 v175a best 启动，验证 same-frame K=2 gradient accumulation。

```text
v175a:
  FG_LPIPS = 0.134804
  L1_fg    = 0.043218
  FG_PSNR  = 21.708366
  SSIM_fg  = 0.883234

v176a K=2:
  best iter = 91900
  FG_LPIPS = 0.133691549
  L1_fg    = 0.043266263
  FG_PSNR  = 21.710976
  SSIM_fg  = 0.883712530
```

结论：

```text
v176a 通过验收线。
在 v175a 已经修出 HF source 幅度之后，
same-frame K=2 能继续降低 FG_LPIPS，
说明“多视角采样稀释 / 同帧监督不足”确实是剩余清晰度瓶颈之一。
```

v177a 从 v176a best 启动，把 same-frame accumulation 提到 K=4。

```text
v177a K=4:
  best iter = 94900
  FG_LPIPS = 0.132785276
  L1_fg    = 0.042765995
  FG_PSNR  = 21.791283
  SSIM_fg  = 0.884231567

final@4000:
  FG_LPIPS 回退到约 0.133044
```

结论：

```text
K=4 相比 K=2 继续有效，并且 L1_fg / SSIM_fg 同步改善。
但 v177a final 比 best 略回退，说明不能无脑加长训练；
后续应提高 eval 密度，并以 best checkpoint 作为锚点继续。
```

视觉复核：

```text
对比目录:
  /remote-home/ming/3dgs-avatar-release-main/outputs/v176a_compare
  /remote-home/ming/3dgs-avatar-release-main/outputs/v177a_compare

v177a 相比 v175a/v176a:
  腰侧、裤子暗部纹理、鞋边更稳；
  肩膀、上臂、头发边缘仍比 mono baseline 软；
  没有出现明显漏色或结构崩坏。
```

### v178：9 小时批处理横向验证

v178 从 v177a best 启动，目的不是继续小修，而是横向验证三个方向：

```text
v178a: K=4 low LR stabilize
v178b: K=4 camera-quality focus
v178c: K=4 conservative photometric consistency
v178d: K=4 contour uncertainty
v178e: K=6 same-frame accumulation probe
```

全部训练与 best render 已完成：

```text
summary:
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/logs/v178_batch_20260427_000807_summary.tsv

compare:
  /remote-home/ming/3dgs-avatar-release-main/outputs/v178_compare
```

结果：

```text
v177a anchor:
  FG_LPIPS = 0.132785276
  L1_fg    = 0.042765995
  FG_PSNR  = 21.791283
  SSIM_fg  = 0.884231567

v178a K4 low LR stabilize:
  best iter = 95650
  FG_LPIPS = 0.132849038
  L1_fg    = 0.042907142
  FG_PSNR  = 21.749825
  SSIM_fg  = 0.884067893

v178b K4 camera-quality focus:
  best iter = 96400
  FG_LPIPS = 0.132657006
  L1_fg    = 0.042761434
  FG_PSNR  = 21.747021
  SSIM_fg  = 0.884293795

v178c K4 photometric consistency:
  best iter = 97900
  FG_LPIPS = 0.132780522
  L1_fg    = 0.042749531
  FG_PSNR  = 21.786301
  SSIM_fg  = 0.884639800

v178d K4 contour uncertainty:
  best iter = 95650
  FG_LPIPS = 0.133084074
  L1_fg    = 0.042886636
  FG_PSNR  = 21.771009
  SSIM_fg  = 0.884247541

v178e K6 probe:
  best iter = 97900
  FG_LPIPS = 0.132453978
  L1_fg    = 0.042850472
  FG_PSNR  = 21.778639
  SSIM_fg  = 0.884508014
```

对比关键锚点：

```text
v164b  FG_LPIPS = 0.160253
v165   FG_LPIPS = 0.159906
v170e  FG_LPIPS = 0.139228
v175a  FG_LPIPS = 0.134804
v176a  FG_LPIPS = 0.133692
v177a  FG_LPIPS = 0.132785
v178e  FG_LPIPS = 0.132454
v142e  FG_LPIPS = 0.139722
```

v178 结论：

```text
1. K=6 是当前 FG_LPIPS 最好的版本。
   这说明采样稀释仍然是有效修复方向，不能回到 K=1 随机采样。

2. camera-quality focus 有正收益。
   v178b 比 v177a 小幅更好，说明把清晰/对齐更好的相机抽多一点是合理的。

3. conservative photometric consistency 有一致性收益。
   v178c 的 L1_fg / SSIM_fg 最好，但 LPIPS 没超过 v178e；
   它更像稳定颜色/结构一致性，不是单独的清晰度突破。

4. contour uncertainty 在当前形态下是负收益。
   v178d 明显不如 v177a / v178b / v178e；
   后续不应继续沿 v178d 的 uncertainty 方案加权。
```

视觉复核：

```text
重点帧:
  c22_f000210
  c22_f000240

重点区域:
  shoulder_arm
  waist_arm
  shoes

结论:
  v178e 整体是当前最好的采样 anchor；
  v178b / v178c 与 v178e 肉眼差距很小；
  v178c 颜色和结构一致性更稳；
  v178e 的鞋边、下半身和整体 LPIPS 更好。

仍未解决:
  手臂外轮廓、肩膀边界、头发边缘仍比 mono baseline 软；
  衣服褶皱有提升，但细节幅度仍低于 baseline；
  这说明采样/HF source 已经推进到有效区间，
  但 SMPL/相机局部错位与多相机颜色差异仍在压平最难轮廓。
```

### 当前完成进度判断

按本文档最初定义的三个主因看：

```text
1. 多视角采样稀释 / 训练不够:
   已从“假设”变成“确认成立”。
   v175a -> v176a -> v177a -> v178e 连续改善：
     0.134804 -> 0.133692 -> 0.132785 -> 0.132454
   当前完成度约 70%。
   还没完成的部分是：
     是否 K=6 已到拐点；
     K=6 与 camera-quality / photometric 是否能组合；
     是否需要更高 K 或更长训。

2. SMPL/相机小对齐误差 + 显式绑定偏软:
   只缓解，没有根治。
   手臂、肩膀、头发边缘仍明显软于 baseline。
   v178d 说明简单 uncertainty 加权不是正确解法。
   当前完成度约 30%-40%。

3. 多视角颜色 / 曝光不一致:
   已确认不能太早强上 correction。
   v178c 说明在 HF source 和 K=4 之后，保守 photometric 有正面一致性收益。
   但它还没有带来 LPIPS/肉眼锐度突破。
   当前完成度约 30%。
```

总体判断：

```text
项目已经从 v164/v165 的“完整但软”，推进到 v178e 的“超过 v142e 指标、接近可用显式绑定清晰度”。
但还没有逼近 mono baseline 的肉眼锐度。

下一步不能再回到单独 boundary/head 或单独 uncertainty。
高价值方向应该组合已经被验证为正收益的三件事：
  K=6 same-frame dense supervision
  camera-quality focus
  conservative photometric consistency

同时必须用 v177a/v178e 暴露的问题做 early-stop：
如果肩膀/手臂继续变软，即使 LPIPS 小降也不能作为主线。
```

### 下一实验建议：v179 组合正信号，不继续单点小修

建议做 v179 批处理，目标是验证“采样 + 相机质量 + 保守颜色一致性”能否叠加，而不是各自单独有效。

```text
v179a:
  start = v178e best
  K=6
  继承 v178b camera-quality focus
  不开 photometric
  目的:
    验证 K=6 与 camera-quality 是否叠加；
    如果比 v178e 更好，说明优质相机加权仍能提升高频监督质量。

v179b:
  start = v178e best
  K=6
  继承 v178b camera-quality focus
  开 v178c 的 conservative photometric，但 strength 降到约 0.30-0.35
  目的:
    验证颜色一致性是否能在 K=6 下稳定 L1/SSIM，
    同时不牺牲手臂/肩膀边界。

v179c:
  start = v178c best
  K=4 或 K=6
  保持 photometric consistency
  加 camera-quality focus
  目的:
    以结构一致性最好的 v178c 为 anchor，
    验证它是否比 v178e 更适合作为后续长训底座。
```

训练控制：

```text
每个分支先跑 2000-3000 micro iterations；
test_interval = 500 或 750；
必须渲染 c22_f000210 / c22_f000240 best；
不要只看 final，要看 best。
```

v179 通过条件：

```text
指标:
  FG_LPIPS < 0.13245
  L1_fg    <= 0.04275
  SSIM_fg  >= 0.8846

视觉:
  肩膀和手臂不能比 v178e 更软；
  腰侧、衣服褶皱、鞋边至少保持 v178e；
  如果 v179b/v179c 的颜色更稳但边界变软，则 photometric 只能作为低权重辅助。
```

如果 v179 仍然只小幅提升或肉眼不变：

```text
说明 same-frame 采样和 HF source 已接近当前显式绑定主干上限。
下一阶段应转向真正的 per-camera / per-view residual 或 alignment-aware supervision，
而不是继续加 K、加 boundary loss、加 uncertainty 权重。
```

## 2026-04-27 下午补充：v179 组合正信号批处理结果

v179 按上一节计划执行，三个分支全部完成并渲染 best checkpoint。

```text
summary:
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/logs/v179_batch_20260427_100300_summary.tsv

render:
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v179a_v178e_k6_camquality_focus_screen3000_render_bestcmp
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v179b_v178e_k6_camquality_tightphoto_screen3000_render_bestcmp
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v179c_v178c_k6_camquality_tightphoto_screen3000_render_bestcmp

compare:
  /remote-home/ming/3dgs-avatar-release-main/outputs/v179_compare_auto
```

### v179 指标结果

```text
v178e anchor:
  start = v177a
  setup = K6 probe
  FG_LPIPS = 0.132453978
  L1_fg    = 0.042850472
  FG_PSNR  = 21.778639
  SSIM_fg  = 0.884508014

v179a:
  start = v178e best
  setup = K6 + camera-quality focus
  best iter = 99900
  FG_LPIPS = 0.132391959
  L1_fg    = 0.042777337
  FG_PSNR  = 21.777884
  SSIM_fg  = 0.884296000

v179b:
  start = v178e best
  setup = K6 + camera-quality focus + tight photometric
  best iter = 99400
  FG_LPIPS = 0.132202625
  L1_fg    = 0.042600427
  FG_PSNR  = 21.802261
  SSIM_fg  = 0.884419441

v179c:
  start = v178c best
  setup = K6 + camera-quality focus + tight photometric
  best iter = 100400
  FG_LPIPS = 0.132146314
  L1_fg    = 0.042667636
  FG_PSNR  = 21.797573
  SSIM_fg  = 0.884817839
```

横向对比：

```text
v142e  FG_LPIPS = 0.139722
v170e  FG_LPIPS = 0.139228
v175a  FG_LPIPS = 0.134804
v176a  FG_LPIPS = 0.133692
v177a  FG_LPIPS = 0.132785
v178e  FG_LPIPS = 0.132454
v179a  FG_LPIPS = 0.132392
v179b  FG_LPIPS = 0.132203
v179c  FG_LPIPS = 0.132146
```

v179 训练动态：

```text
v179a:
  camera-quality focus 能在 v178e K6 基础上继续小幅提升。

v179b:
  best 出现在 99400；
  后续 99900 / 100400 / 100900 有回退，
  说明该方向仍然需要看 best，不适合无脑延长。

v179c:
  从 v178c 这种 photometric consistency anchor 出发，
  再接 K6 + camera-quality + tight photometric，
  最终成为当前指标最好版本。
```

### v179 视觉复核

对比图：

```text
/remote-home/ming/3dgs-avatar-release-main/outputs/v179_compare_auto/auto_crops_render_c22_f000210.png
/remote-home/ming/3dgs-avatar-release-main/outputs/v179_compare_auto/auto_crops_render_c22_f000240.png
/remote-home/ming/3dgs-avatar-release-main/outputs/v179_compare_auto/auto_full_render_c22_f000210.png
/remote-home/ming/3dgs-avatar-release-main/outputs/v179_compare_auto/auto_full_render_c22_f000240.png
```

肉眼结论：

```text
v179c 相比 v178e / v179b 是小幅更稳，不是大跳变。

改善区域:
  腰侧轮廓更稳定；
  裤子暗部和衣服褶皱有轻微增强；
  鞋边和整体颜色一致性略好；
  没有因为 tight photometric 出现明显变灰、漏色或结构崩坏。

仍然不足:
  肩膀外轮廓仍偏软；
  上臂/前臂外边界仍不如 mono baseline 硬；
  头发边缘仍偏糊；
  衣服细节幅度仍低于 baseline。
```

因此，v179 的核心判断是：

```text
组合正信号成立：
  K6 same-frame dense supervision
  camera-quality focus
  tight / conservative photometric consistency
这三项可以叠加，并且 v178c 这种颜色/结构一致性 anchor
比单纯 v178e best-LPIPS anchor 更适合作为后续底座。

但 v179 仍然没有打穿 baseline 肉眼清晰度。
剩余问题不再是简单“采样不够”，而是 hardest contour 在多视角错位/颜色差异下仍被平均。
```

### 三个主因的 v179 后完成度

按本文档最初定义的三个主因，v179 后的状态更新为：

```text
1. 多视角采样稀释 / 训练不够:
   完成度约 80%-85%。

   已完成:
     K=1 frame-balanced / camera-quality 有弱信号；
     HF source 修复后，K=2 / K=4 / K=6 连续有效；
     K6 + camera-quality + tight photometric 已组合成功；
     v175a -> v179c 从 0.134804 推到 0.132146。

   未完成:
     不确定 K=8 是否仍有收益；
     但 v179 的收益已经变窄，继续只加 K 的性价比低。

   当前判断:
     采样主因已经基本修到位，不能再作为唯一主线。

2. SMPL / 相机小对齐误差 + 显式绑定偏软:
   完成度约 35%-45%。

   已完成:
     支撑结构完整；
     显式绑定不会崩；
     HF source 和 dense sampling 缓解了大面积软糊；
     腰侧、裤腿、鞋边已有改善。

   未完成:
     肩膀、手臂、头发这些 hardest contour 仍明显软于 baseline；
     v178d 的简单 contour uncertainty 是负收益；
     说明“粗暴降低边界权重”不是正确解决方式。

   当前判断:
     下一阶段主要要处理 alignment-aware supervision，
     让错位视角不要把最难轮廓平均掉。

3. 多视角颜色 / 曝光不一致:
   完成度约 45%-55%。

   已完成:
     v166 证明太早强上 photometric 会恶化；
     v178c 证明 HF source + K4 后，保守 photometric 有正收益；
     v179b / v179c 证明 tight photometric 可以和 K6 / camera-quality 叠加。

   未完成:
     当前 photometric 仍是 loss/supervision 侧修正，
     不是模型显式表达每个 camera 的颜色/曝光差异；
     它能提升一致性，但没有单独带来 hard contour 突破。

   当前判断:
     后续应做受限 per-camera color/exposure residual，
     而不是继续提高 photometric correction strength。
```

额外主干状态：

```text
HF source / trunk 高频输出:
  完成度约 75%-80%。
  v175a 是该瓶颈第一次被打穿，v176-v179 的收益都依赖它。

boundary/head 直接硬化:
  完成度仍只有约 30%-40%。
  boundary_focus 已能稳定在 0.08；
  但 boundary_color 仍约 0.001 量级，
  boundary_takeover 约 0.048，
  sharpen_gate 仍为 0。

这说明当前清晰度提升主要来自 HF source + dense sampling，
不是 boundary/head 真正像 baseline 一样主动画硬边界。
```

### 下一步清晰度路线

v179c 应固化为当前主线 anchor：

```text
anchor:
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v179c_v178c_k6_camquality_tightphoto_screen3000/best_ckpt.pth
```

下一步不建议：

```text
不建议直接 K8 / K10 长训；
不建议继续加 boundary loss；
不建议继续沿 v178d contour uncertainty 增强；
不建议简单提高 photometric correction strength。
```

建议进入 v180：alignment-aware hard contour supervision。

目标：

```text
从 v179c best 出发，
只针对 shoulder / arm / waist / head hairline 这些 hardest contour，
做 alignment-aware supervision：
  保留 K6 + camera-quality + tight photometric；
  在轮廓附近识别高错位/高不一致区域；
  对明显错位的 view-edge loss 降权；
  对稳定一致的 hard contour 提高 edge/detail/luma-DOG 权重；
  不重新打开 geometry / opacity / scaling。
```

v180 要证实的问题：

```text
如果 v180 让肩膀/手臂轮廓变硬，同时 FG_LPIPS / L1_fg 不退:
  说明当前最大剩余瓶颈确实是局部对齐误差导致的多视角边界平均。

如果 v180 指标持平但轮廓肉眼变硬:
  可以作为视觉清晰度主线继续做 alignment-aware。

如果 v180 指标和肉眼都不变:
  说明当前显式绑定主干已经接近上限；
  下一步要做受限 per-camera / per-view residual，
  让模型能表达相机颜色/局部视角差异。
```

v180 验收线：

```text
指标:
  FG_LPIPS <= 0.13215
  L1_fg    <= 0.04267
  SSIM_fg  >= 0.8848

视觉:
  c22_f000210 / c22_f000240 的肩膀、手臂、腰侧不能比 v179c 更软；
  至少一个 hardest contour 区域要肉眼更硬；
  不能出现漏色、局部鼓包或背景污染。
```

## 2026-04-27 下午补充：v180 alignment-aware 实验已落地并启动

v179c 之后的判断是：

```text
继续只堆 K / camera sampling / photometric 的收益已经变窄；
v179c 的主要剩余问题集中在 shoulder / arm / waist / hairline 等 hardest contour；
这些区域最容易受多视角 5-16px 级局部错位影响，被平均成软边界。
```

因此 v180 不再走 v178d 那种全局 contour uncertainty 降权。v178d 已经证明：

```text
简单把边界附近整体降权，会削弱真实硬边界监督，指标变差。
```

v180 改为局部 alignment-aware hard-contour supervision：

```text
只在 shoulder/arm 和 waist 的边界监督区域内工作；
使用 GT luma gradient 找稳定硬边界；
使用当前 image error 识别疑似错位/不一致边界；
稳定边界增强 edge/detail 监督；
高误差但不稳定的疑似错位点降低监督权重；
不打开 geometry / opacity / scaling；
不增加新的 boundary/head takeover 机制。
```

### v180 代码改动

新增到 `train.py`：

```text
_luma_gradient_magnitude()
_alignment_aware_contour_weight_mask()
```

训练时在 loss mask 侧接入：

```text
shoulder_arm_edge_loss_mask *= alignment_contour_weight
可选:
  shoulder_arm_detail_mask *= alignment_contour_weight
  shoulder_arm_l1_loss_mask *= alignment_contour_weight
  waist_edge_loss_mask *= waist_alignment_weight
  waist_detail_mask *= waist_alignment_weight
```

新增 debug：

```text
[PhotometricContour]
  align_pixels
  align_mean_w
  align_stable
  align_suppress
  align_waist_pixels
```

这些 debug 用来确认 v180 是否真的在 hardest contour 区域生效，而不是只改了配置没有影响训练。

### v180 实验配置

新增配置：

```text
configs/option/stageA_377_multiview_explicit_hq_v180a_v179c_alignaware_edge_v1.yaml
configs/option/stageA_377_multiview_explicit_hq_v180b_v179c_alignaware_edge_luma_v1.yaml
```

新增批处理脚本：

```text
tools/run_377_stageA2_v180_alignaware_batch.sh
```

启动 anchor：

```text
/remote-home/ming/3dgs-avatar-release-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v179c_v178c_k6_camquality_tightphoto_screen3000/best_ckpt.pth
```

v180a：

```text
setup:
  v179c best
  K6 same-frame
  camera-quality focus
  tight photometric
  alignment-aware edge only

目的:
  最保守地验证 alignment-aware edge weighting 是否能让肩膀/手臂/腰侧边界变硬。

关键配置:
  alignment_aware_contour_enable: true
  alignment_aware_contour_apply_waist: true
  alignment_aware_contour_apply_luma: false
  alignment_aware_contour_apply_l1: false
  alignment_aware_contour_band_width: 11
  alignment_aware_contour_stable_boost: 0.35
  alignment_aware_contour_misaligned_suppress: 0.22
  alignment_aware_contour_min_weight: 0.78
  alignment_aware_contour_max_weight: 1.35
```

v180b：

```text
setup:
  v179c best
  K6 same-frame
  camera-quality focus
  tight photometric
  alignment-aware edge + luma/detail

目的:
  验证更强的局部硬轮廓/detail reweight 是否能带来肉眼边界增强，
  同时不引入局部鼓包或纹理噪声。

关键配置:
  alignment_aware_contour_apply_luma: true
  alignment_aware_contour_band_width: 13
  alignment_aware_contour_stable_boost: 0.50
  alignment_aware_contour_misaligned_suppress: 0.30
  alignment_aware_contour_min_weight: 0.70
  alignment_aware_contour_max_weight: 1.55
```

### v180 启动记录

```text
run_id:
  20260427_154534

runner pid:
  3439072

runner log:
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/logs/v180_batch_20260427_154534.log

summary:
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/logs/v180_batch_20260427_154534_summary.tsv

current log:
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/logs/v180a_20260427_154534_alignaware_edge.log

启动时间:
  2026-04-27 15:45:34 CST

预计结束:
  2026-04-27 18:20-18:40 CST
```

启动确认：

```text
Loaded checkpoint:
  v179c best_ckpt.pth

loaded iteration:
  100400

sampler:
  frame_balanced_camera_weighted
  accumulation_steps = 6
  min_prob = 0.025
  max_prob = 0.105

trainable:
  texture HF/source 相关参数
  geometry / opacity / scaling / feature 仍保持冻结
```

早期 debug 确认 alignment-aware 逻辑生效：

```text
iter=100500
photo_strength=0.340
photo_pixels=25402
photo_scale=1.012
photo_shift=0.0044
uncertain_pixels=16652
uncertain_min_w=0.560
align_pixels=1394.8
align_mean_w=0.0192
align_stable=0.0722
align_suppress=0.0436
align_waist_pixels=342.0
```

解释：

```text
align_pixels 非 0，说明 shoulder/arm contour 区域已经被命中；
align_stable 和 align_suppress 都非 0，说明它同时在区分稳定硬边界与疑似错位点；
align_waist_pixels 非 0，说明 waist 也进入了 alignment-aware 分支。
```

### v180 要观察什么

v180 不以单纯 FG_LPIPS 小数点继续下降作为唯一成功标准。它要验证的是：

```text
1. 肩膀/手臂/腰侧边界是否比 v179c 更硬；
2. v180a 是否能在不牺牲 v179c 指标的情况下带来稳定边界收益；
3. v180b 的 luma/detail 加权是否进一步提升衣服/边界细节，还是会引入噪声或软化；
4. 如果 v180a/v180b 都无效，则说明 alignment-aware loss-mask 还不够，需要进入 per-camera / per-view residual，而不是继续堆 loss。
```

v180 结束后必须渲染并对比：

```text
baseline
v142e
v179c
v180a
v180b

重点帧:
  c22_f000210
  c22_f000240

重点区域:
  shoulder / arm
  waist side
  clothes wrinkles
  shoes edge
  hairline
```

## 2026-05-07 补充：v180 alignment-aware 结果与根因判断

v180 的目的不是继续堆采样或 boundary loss，而是验证：

```text
在 v179c 已经具备 K6 + camera-quality + tight photometric 正信号之后，
只通过 alignment-aware contour loss mask，
能否把 shoulder / arm / waist / hairline 这些 hardest contour 从多视角平均中拉硬。
```

### v180 最终可用结果

对比当前主线 anchor：

```text
v179c:
  setup    = K6 + camera-quality focus + tight photometric
  best iter= 100400
  FG_LPIPS = 0.132146314
  L1_fg    = 0.042667636
  FG_PSNR  = 21.797573
  SSIM_fg  = 0.884817839

v180a:
  setup    = v179c + alignment-aware edge only
  best iter= 100900
  FG_LPIPS = 0.132353365
  L1_fg    = 0.042650647
  FG_PSNR  = 21.787601
  SSIM_fg  = 0.884922087

v180b:
  setup    = v179c + alignment-aware edge + luma/detail
  best iter= 101400
  FG_LPIPS = 0.132490322
  L1_fg    = 0.042762368
  FG_PSNR  = 21.775658
  SSIM_fg  = 0.884576976
```

相关路径：

```text
v179c:
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v179c_v178c_k6_camquality_tightphoto_screen3000
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v179c_v178c_k6_camquality_tightphoto_screen3000_render_bestcmp

v180a:
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v180a_v179c_alignaware_edge_screen2000
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v180a_v179c_alignaware_edge_screen2000_render_bestcmp

v180b:
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v180b_v179c_alignaware_edge_luma_screen2000
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v180b_v179c_alignaware_edge_luma_screen2000_render_bestcmp

logs:
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/logs/v180a_20260427_154534_alignaware_edge.log
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/logs/v180b_20260427_154534_alignaware_edge_luma.log
  /remote-home/ming/3dgs-avatar-release-main/exp/stageA2/logs/v180b_20260427_154534_render_manual.log
```

说明：

```text
v180 batch summary 只自动写入了 v180a。
v180b 目录中已有 best_ckpt.pth / best_test_metrics.json / ckpt101900.pth，
但没有自动 render_bestcmp，因此 2026-05-07 手动补渲染了 v180b best。
```

### v180 机制观察

v180b 的 debug 证明 alignment-aware loss mask 确实命中了 hardest contour：

```text
PhotometricContour:
  align_pixels       > 0
  align_stable       > 0
  align_suppress     > 0
  align_waist_pixels > 0
```

典型日志：

```text
iter=101500
photo_strength=0.340
photo_pixels=30453.0
uncertain_pixels=17908.0
align_pixels=315.5
align_mean_w=0.0312
align_stable=0.0811
align_suppress=0.0376
align_waist_pixels=35.0
```

但 texture/head 输出机制没有发生根本变化：

```text
support           ~= 0.959 - 0.961
owner_takeover    ~= 0.646 - 0.651
boundary_focus    = 0.080
boundary_takeover = 0.048
boundary_color    ~= 0.0007 - 0.0012
sharpen_gate_mean = 0
```

结论：

```text
v180 不是“没有命中边界”，而是“命中了边界，但只改 loss mask 不能让主输出画出更硬的轮廓”。

alignment-aware edge/luma/detail reweight 是 supervision 侧修正，
它无法解决同一个 texture MLP 被 20 个相机的颜色差异、曝光差异、局部错位共同平均的问题。
```

### v180 视觉结论

已对比重点帧：

```text
c22_f000210
c22_f000240
```

v180b 相比 v179c：

```text
没有明显新增漏色或结构崩坏；
但肩膀、上臂、前臂、腰侧轮廓没有肉眼可见的硬化；
衣服褶皱和鞋边也没有超过 v179c；
整体观感基本持平或略弱。
```

因此：

```text
v179c 仍是当前最强主线 anchor。
v180a/v180b 不应作为下一阶段继续训练底座。
```

## 2026-05-07 更新：三个主因完成度

### 1. 多视角采样稀释 / 训练不够

当前完成度：

```text
80% - 85%
```

已完成：

```text
K=1 frame-balanced / camera-quality 有弱信号；
v175a 修复 HF source 后，K=2 / K=4 / K=6 连续有效；
K6 + camera-quality + tight photometric 已组合成功；
v175a -> v179c:
  FG_LPIPS 0.134804 -> 0.132146
```

未完成：

```text
不确定 K=8 是否仍有微弱收益；
但 v178e -> v179c 的增益已经很窄，
继续只加 K 或加训练时长，不是根因级路线。
```

判断：

```text
采样稀释已经基本修到当前显式绑定主干能吃下的上限。
它仍是必要配置，但不能再作为唯一主线。
```

### 2. SMPL / 相机小对齐误差 + 显式绑定偏软

当前完成度：

```text
35% - 45%
```

已完成：

```text
人形完整性稳定；
HF source + dense same-frame sampling 缓解了大面积软糊；
腰侧、裤腿、鞋边比 v164/v165 阶段明显改善；
v180 证明可以在 loss mask 里识别稳定边界和疑似错位点。
```

未完成：

```text
shoulder / arm / hairline 仍明显软于 mono baseline；
v178d 的简单 contour uncertainty 是负收益；
v180 的 alignment-aware loss mask 没带来肉眼硬边界突破；
boundary_color 仍在 0.001 量级，boundary/head 没有真实硬边界输出幅度。
```

判断：

```text
剩余问题不是“找不到 hardest contour”，
而是多视角错位下，同一个主输出仍被迫平均不同相机看到的边界。
只继续调 boundary loss、uncertainty mask、alignment-aware mask，属于低价值小修。
```

### 3. 多视角颜色 / 曝光不一致

当前完成度：

```text
45% - 55%
```

已完成：

```text
v166 证明太早强上 photometric correction 会恶化；
v178c 证明 HF source + K4 后，保守 photometric 有一致性收益；
v179b / v179c 证明 tight photometric 可以和 K6 / camera-quality 叠加；
v179c 是当前最强 anchor。
```

未完成：

```text
当前 photometric 仍是 loss/supervision 侧修正；
模型本身仍没有显式表达每个 camera 的颜色、曝光、白平衡差异；
统一 texture MLP 仍会把相机差异平均成灰、软、低幅度细节。
```

判断：

```text
颜色/曝光差异已经不能只靠 loss 权重处理。
下一步必须让模型具备受限的 per-camera / per-view 表达能力，
否则多视角差异会继续压平高频和轮廓。
```

## 2026-05-07 下一步：v181 根因级修复方向

v180 之后，不建议继续：

```text
不继续单纯加 K；
不继续单纯加 boundary/head loss；
不继续加强 contour uncertainty；
不继续把 alignment-aware mask 做得更复杂；
不继续提高 photometric correction strength。
```

这些都还是在 supervision 侧调权重，不能改变核心事实：

```text
20 个训练相机存在颜色/曝光/白平衡差异；
局部 SMPL/相机对齐存在 5-16px 级偏差；
当前模型仍用同一个主 texture 输出解释所有相机；
所以 hardest contour 和高频细节会被平均。
```

下一步应做 v181：受限 per-camera / per-view residual。

目标不是让模型记住每个相机、破坏泛化，而是给它一个很小、可正则、可关掉的通道，用来表达多视角差异，避免主 texture 被迫平均：

```text
base texture:
  继续保持 v179c 的 shared canonical texture 主路径。

camera residual:
  只表达低频颜色 / 曝光 / 白平衡差异。

view residual:
  只表达很小幅度的 view-conditioned 高频修正，
  优先作用在衣服、皮肤、鞋边等 texture/detail 区域，
  不允许直接改 geometry / opacity / scaling。
```

### v181a：per-camera affine color/exposure residual

目的：

```text
先验证多相机颜色/曝光平均是不是仍在压低清晰度。
```

设计：

```text
每个训练 camera 学一个很小的 RGB affine:
  rendered_rgb_corrected = clamp(rendered_rgb * scale_cam + shift_cam)

约束:
  scale_cam 初始为 1
  shift_cam 初始为 0
  scale/shift 强正则
  scale 范围限制，例如 0.92 - 1.08
  shift 范围限制，例如 -0.04 - 0.04

训练:
  从 v179c best 启动
  冻结 geometry / opacity / scaling / feature
  保留 K6 + camera-quality + tight photometric
  只训练 texture HF/source + camera affine
```

验收：

```text
FG_LPIPS < 0.132146
L1_fg    <= 0.04267
SSIM_fg  >= 0.8848

肉眼:
  肩膀/手臂不能更软；
  衣服褶皱、鞋边、裤腿暗部应更稳定；
  颜色不能变灰，不能出现相机特化导致 test view 崩。
```

如果 v181a 有正收益：

```text
说明多相机颜色/曝光平均仍是主要剩余瓶颈之一。
再进入 v181b。
```

如果 v181a 无收益：

```text
说明剩余瓶颈更偏局部视角/轮廓错位，
per-camera 低频颜色不足以解决，需要更直接的 view-conditioned residual。
```

### v181b：受限 view-conditioned texture residual

目的：

```text
让模型表达轻微视角相关细节，
避免同一个 canonical texture 被迫同时解释所有相机边界和高频。
```

设计：

```text
输入:
  view direction / camera embedding / surface normal 与 view direction 的关系

输出:
  小幅 RGB residual

约束:
  residual amplitude cap，例如 <= 0.02 - 0.04
  强 L2 / TV / sparsity 正则
  只在 foreground detail mask 内起作用
  不作用于背景
  不训练 geometry / opacity / scaling
  不改变 canonical base texture 的主体表达
```

关键原则：

```text
v181b 不是开一个大 view-dependent MLP 去记相机；
它必须是低幅度、受限、可正则的残差支路。
它的任务是释放主 texture 的平均压力，
让 canonical base 保持完整，
让 view residual 只吸收多视角无法共享的颜色/局部细节差异。
```

验收：

```text
指标必须超过 v179c；
test view 不能崩；
重点看 c22_f000210 / c22_f000240：
  shoulder / arm
  waist side
  hairline
  clothes wrinkles
  shoes edge
```

### v181c：如果 v181a/b 仍不够，再做 contour-side residual

只有在 v181a/b 证明 per-camera/per-view 表达有效但 hardest contour 仍软时，才进入 v181c：

```text
在 hardest contour 附近引入 very-small signed color/alpha residual，
但仍冻结真实 geometry / opacity / scaling 主参数；
只允许局部输出修正，不允许全局形变。
```

v181c 的前提：

```text
不能再用 v178d 那种简单 uncertainty 降权；
不能再用 v180 那种只改 loss mask；
必须建立在 v181a/b 已经让多视角颜色/视角差异不再压平主 texture 的基础上。
```

## 当前总判断

截至 v180：

```text
v179c 是当前最强结果；
v180a/v180b 验证了 alignment-aware loss mask 不是根解；
采样主因已基本修到位；
HF source 已经是有效通路；
剩余主要瓶颈是多相机颜色/曝光/局部视角差异仍被统一 texture 主输出平均。
```

因此下一阶段的根因级目标是：

```text
让模型拥有受限的 camera/view residual 表达能力，
把不可共享的多视角差异从 canonical texture 主路径里分离出去。
```

这比继续加 loss 或调 boundary tag 更接近清晰度不足的根源。

## 2026-05-07 晚间补充：v181a 实跑结果、失败原因与下一阶段方向

### v181a 实验记录

本次在当前工程目录重新配置数据、SMPL、tiny-cuda-nn 后，完成了 v181a 训练：

```text
project:
  /remote-home/ming/semantic-editable-avatar-main

data:
  /remote-home/ming/semantic-editable-avatar-main/data/ZJUMoCap/CoreView_377

body_models:
  /remote-home/ming/body_models

start checkpoint:
  /remote-home/ming/semantic-editable-avatar-main/exp/377_multiview_explicit_hq_rootfix_resume_v179c_v178c_k6_camquality_tightphoto_screen3000/best_ckpt.pth

v181a output:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v181a_v179c_camera_affine_screen2500

v181a render check:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v181a_v179c_camera_affine_screen2500_render_bestcmp_quick

comparison sheets:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v181a_v179c_camera_affine_screen2500_render_bestcmp_quick/inspect/v181a_gt_render_diff_contact.png
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v181a_v179c_camera_affine_screen2500_render_bestcmp_quick/inspect/v179c_vs_v181a_contact.png
```

### v181a 指标结果

v179c anchor：

```text
iteration = 100400
FG_LPIPS  = 0.132146
L1_fg     = 0.042668
PSNR_fg   = 21.797573
SSIM_fg   = 0.884818
```

v181a best：

```text
iteration = 102900
FG_LPIPS  = 0.199893
L1_fg     = 0.051276
PSNR_fg   = 20.682056
SSIM_fg   = 0.865694
```

同一批 held-out view 快速渲染 masked L1 对比：

```text
v179c quick masked L1 = 0.048674
v181a quick masked L1 = 0.055527
delta                 = +0.006853
```

结论：

```text
v181a 没有超过 v179c；
指标显著退化；
肉眼看 v181a 更偏亮 / 偏黄，前景误差更大；
轮廓、衣服褶皱、鞋边、高频细节没有获得有效改善。
```

因此：

```text
当前 v181a 不应作为下一阶段底座；
不要从这个 v181a checkpoint 继续接 v181b；
v179c 仍是当前最强 anchor。
```

### v181a 的关键机制问题

训练日志里出现了一个重要信号：

```text
[GaussianConverter] texture optimizer filter:
  trainable_nonlatent=0
  trainable_latent=0
  skipped=7
```

同时加载 v179c checkpoint 时出现大量：

```text
Resume safety: partial converter load unexpected keys:
  texture.detail_high_freq_*
  texture.structured_trunk_*
  texture.structured_trunk_output_head_*
  ...
```

这说明本次 v181a 并不是“完整 v179c texture stack + camera affine”的严格续训。
它实际发生的是：

```text
1. v179c checkpoint 中大量 texture / structured trunk / high-frequency 模块没有被当前模型结构接住；
2. v181a 计划中“训练 texture HF/source + camera affine”没有真正成立；
3. camera affine 以 identity 初始化后参与训练，但 shared texture 主体已经不是完全等价的 v179c 结构；
4. 因此 v181a 的失败不能严格证明 per-camera affine 思路无效；
5. 但它可以证明：只靠低频 camera affine、且没有 faithful v179c 结构继承时，会明显破坏当前最强结果。
```

更准确的判断：

```text
v181a 当前结果是一次“配置/结构不完全等价的 per-camera affine 试验失败”。
它不是可继续放大的方向；
它也不能作为 v181b 的有效前置。
```

这件事本身也暴露了下一阶段必须解决的工程前提：

```text
后续所有根因级实验必须从 v179c 的实际 hydra config / actual module graph 出发，
不能再用手写 option stack 近似复现 v179c。
否则实验结果会混入结构缺失和 partial load 误差，无法判断方法本身。
```

## 2026-05-07 下一阶段判断：不要小修小补，进入 v182 级重构

v181a 之后，不建议继续：

```text
不从 v181a 接 v181b；
不再只调 camera_affine_lr / strength / reg；
不继续加 boundary loss、edge loss、luma loss；
不继续加 K 或延长训练；
不继续做更复杂的 alignment-aware mask；
不继续只在 supervision 侧做 photometric / contour 小修。
```

这些都不能解决核心事实：

```text
1. 当前模型的 canonical shared texture 仍在吃多相机矛盾；
2. 局部 5-16px 错位会把高频边缘平均掉；
3. 单一前景表示很难同时解释 cloth / skin / hair / shoe 的硬边界；
4. 只改 loss mask 不会改变可表达性；
5. 只加 per-camera 低频颜色不能修局部几何 / 轮廓错位。
```

下一阶段应当从“模型表达和数据对齐”两端做大改，而不是继续调权重。

### v182a：先做 faithful resume substrate，保证后续实验可信

目的：

```text
任何 v182 / v183 实验都必须完全继承 v179c 的实际模型结构。
```

具体做法：

```text
从 v179c 的 .hydra/config.yaml 作为 base config；
只追加新模块配置，不重写 option stack；
加载 checkpoint 后要求 texture / structured_trunk / detail_high_freq key 全部可解释；
除明确新增模块外，不允许出现大批 unexpected texture keys；
训练启动日志必须显示预期 trainable modules 数量，而不是 trainable_nonlatent=0。
```

验收：

```text
用 v179c config + v179c best_ckpt 重新 render，
held-out 指标必须复现 v179c 水平；
然后只加一个 no-op 新模块，指标也必须不退。
```

这不是小修。
这是后续根因实验可信的前置条件。

### v182b：相机/人体局部对齐重建，而不是 loss mask 对齐

v180 已经证明：

```text
alignment-aware mask 能识别 hardest contour，
但不能让主输出画出更硬轮廓。
```

因此下一步不该继续 mask，而应该让数据真的对齐。

方向：

```text
建立一个独立 alignment calibration stage：
  per-camera extrinsic residual
  per-camera focal / principal point small residual
  per-frame root / SMPL pose small residual
  optional local joint residual for shoulder / elbow / wrist / waist

训练信号：
  silhouette edge distance transform
  foreground mask IoU
  image gradient / Canny edge alignment
  sparse high-confidence body-part contour

冻结：
  texture
  gaussian opacity / scaling / feature

输出：
  corrected camera / pose residual cache
  后续 texture 训练全部使用 corrected alignment
```

为什么这是大方向：

```text
如果 5-16px 局部错位是真的存在，
那么任何 shared texture 都会被迫平均边缘。
只有先把相机/人体局部对齐修掉，清晰度才有上限提升。
```

建议实验：

```text
v182b_aligncalib:
  start = v179c best
  train = camera/pose alignment residual only
  eval  = render silhouette/edge alignment, not RGB only

v182c_texture_after_align:
  start = v179c best + corrected alignment cache
  train = texture/detail only
  freeze geometry/opacity/scaling
```

验收：

```text
如果 corrected alignment 后 v179c render 的 silhouette / contour residual 明显下降，
再训练 texture 才有意义。
如果 alignment 不能改善 contour residual，
说明剩余问题更偏 representation，而不是 pose/camera。
```

### v183：从单一 shared texture 改成“冲突分解”的表示

如果 v182b 证明对齐仍不足，下一步要改表示，而不是再调 loss。

方向：

```text
shared canonical base:
  负责稳定、跨视角一致的主体颜色和低频结构。

robust detail atlas / surface grid:
  负责可共享的高频细节；
  采用 per-surface / per-anchor 显式高分辨率 detail 表示；
  不再完全依赖 MLP 从 point feature 里挤出细节。

view-conditioned residual:
  只吸收不可共享的视角相关小残差；
  不能使用大 camera-id MLP 记忆相机；
  以 normal-view relation / visibility / grazing angle 为输入；
  residual cap 强约束。

semantic layered boundary:
  cloth / skin / hair / shoes 建立独立 boundary ownership；
  让边界由正确层负责，而不是一个混合 feature 同时解释所有区域。
```

关键变化：

```text
从“一个 canonical texture 解释所有相机”
变成“base / detail / view residual / semantic layer 分工解释”。
```

这才是清晰度上限提升，不是局部调参。

### v184：鲁棒多视角 teacher-student，而不是平均所有视角

当前多视角训练默认把所有相机监督混在一起。
如果某些相机/角度在某些部位更清晰，平均训练会抹掉它。

方向：

```text
1. 为每个 surface anchor / semantic part 估计 view reliability；
2. 对每个部位选择最可靠的 camera/frame 作为 teacher signal；
3. 用 robust aggregation / median / trimmed mean 生成 canonical detail target；
4. student base texture 学可共享细节；
5. residual branch 只学 teacher 无法统一的剩余。
```

这相当于把训练从：

```text
所有视角同权平均
```

改成：

```text
按 surface / semantic / visibility 选择可靠监督，再蒸馏到共享表示
```

预期收益：

```text
衣服褶皱、鞋边、手臂边缘、头发轮廓不会再被低质量视角一起平均掉。
```

### 推荐下一步执行顺序

不要从 v181a 继续。
建议顺序：

```text
Step 1:
  固化 v179c faithful replay。
  目标是 v179c config + v179c ckpt 在当前工程可无损 render / eval。

Step 2:
  做 v182b alignment calibration。
  只优化 camera / pose residual，输出 corrected alignment cache。

Step 3:
  在 corrected alignment 上做 v182c texture/detail retrain。
  冻结 geometry/opacity/scaling，只训练 high-frequency detail。

Step 4:
  如果 v182c 仍不够，进入 v183 representation split：
    shared base
    explicit detail atlas
    small view residual
    semantic boundary layers

Step 5:
  如果多视角平均仍压细节，进入 v184 teacher-student reliable-view distillation。
```

当前最优策略：

```text
v179c 继续作为唯一 anchor；
v181a 只保留为失败记录；
下一轮从 v182 alignment / representation 重构开始。
```

## 2026-05-07 夜间复盘：实验模块完成度、有效性与排除项

这一节把前面 v165 到 v181a 的结果拆成工程分流表，目的是明确：

```text
哪些已经验证有效；
哪些只是辅助有效；
哪些可以停止投入；
哪些还没被真正验证；
下一批实验必须按什么顺序推进。
```

当前总判断：

```text
v179c 仍是唯一可用 anchor；
v181a 当前 checkpoint 不能继续接实验；
下一阶段先补 faithful resume，再做 alignment calibration，最后再做 representation split；
不要继续围绕 loss 权重、K 值、边界小项做局部试错。
```

### 模块级完成度与结论

| 模块 | 完成度 | 证据 | 当前判断 | 后续动作 |
|---|---:|---|---|---|
| 数据 / SMPL / tiny-cuda-nn 环境 | 95% | CoreView_377 已整理到当前工程；SMPL symlink 可用；tinycudann import/forward 通过 | 已完成，可支撑继续训练 | 保持，不作为实验变量 |
| v179c anchor | 90% | best_ckpt、metrics、quick render 均可用；FG_LPIPS=0.132146 | 当前最强 anchor | 所有新实验从 v179c 出发 |
| v179c faithful replay | 30% | render 通过 root override 复现；但还没有做 no-op resume/key audit | 后续实验可信性的前置缺口 | v182a 第一优先级 |
| HF source / high-frequency detail | 85% | v175a 后进入有效区间，后续 K/camera-quality 能继续叠加 | 明确有效 | 保留为主干，不回退 |
| frame-balanced / K 采样 | 85% | K=2/K=4/K=6 连续有效；v175a 到 v179c 有小幅稳定增益 | 有效但接近边际收益 | 保留 K6，不继续加 K |
| camera-quality / tight photo sampling | 75% | v178e/v179b/v179c 证明可与 K6 叠加 | 辅助有效 | 保守保留，不加大强度 |
| photometric/luma 小项 | 60% | 早期单独上容易伤害；在强 anchor 上低权重可辅助 | 只能做辅助 | 不再作为主方向 |
| contour uncertainty | 100% | v178d 类路线无稳定收益 | 已排除为主线 | 停止追加 |
| alignment-aware mask/loss | 100% | v180a/v180b 能找到困难边界，但输出清晰度不超过 v179c | 机制诊断有效，训练收益不足 | 不继续加复杂 mask |
| per-camera affine v181a | 100% | v181a FG_LPIPS=0.199893，quick masked L1 也差于 v179c | 当前实现失败，不能作为底座 | 停止从 v181a 接 v181b |
| per-camera affine 思路本身 | 40% | v181a 不是 faithful v179c 结构续训，不能严格否定思路 | 暂不作为下一主线 | 只有 v182a 通过后才可重测 |
| alignment calibration | 0% | 目前只有 mask/loss 侧对齐，没有真正修 camera/pose | 最高价值未验证项 | v182b 主实验 |
| texture after corrected alignment | 0% | 没有在 corrected camera/pose 上重训 texture | 依赖 v182b | v182c |
| representation split | 0% | 还没有 base/detail/view residual/semantic layer 分工 | 大改方向 | v182 后仍不够再做 v183 |
| reliable-view teacher-student | 0% | 还没有按 surface/part 选择最可靠视角监督 | 大改方向 | v183 后再做 v184 |

### 已验证有效的部分

下面这些不需要反复证明，应作为后续实验的固定资产：

```text
1. v179c 是当前唯一有效 anchor。
2. HF source 修复是第一个把实验推入有效区间的关键改动。
3. K/frame-balanced/camera-quality 采样有效，但收益已经变窄。
4. tight photometric 在强 anchor 上低权重有辅助价值。
5. quick render + masked L1 + FG_LPIPS 能较快筛掉坏分支。
```

后续实验默认继承这些配置，不再单独做 K7/K8、单独 photometric 加强、单独 edge/luma 加强。

### 可以排除或降级的部分

这些方向已经不值得继续作为主实验：

```text
1. 只继续加 K 或延长训练时间。
2. 只继续调 photometric / luma / boundary / edge loss 权重。
3. 只继续做 contour uncertainty 或更复杂的 alignment-aware mask。
4. 直接从当前 v181a checkpoint 接 v181b/v181c。
5. 手写一个近似 v179c option stack 再接新模块。
6. 在没有 faithful replay 的情况下判断 per-camera/view residual 是否有效。
```

排除原因不是这些模块完全没信息量，而是它们已经证明不能单独抬高清晰度上限。
当前瓶颈更像是：

```text
局部 camera/pose/SMPL 对齐误差；
多视角监督互相平均高频；
单一 shared texture 表示承载了过多冲突；
hard contour 缺少明确的语义/层级 ownership。
```

### 还没有真正验证、但值得投入的部分

后续大实验应该集中在三个未验证核心假设：

```text
假设 A：局部错位是清晰度上限瓶颈。
如果 corrected camera/pose 能降低 silhouette/edge residual，再训练 texture 后应该能提升 hardest contour。

假设 B：单一 shared texture 正在平均多视角高频冲突。
如果 base/detail/view residual 分工后局部纹理更硬，说明表示容量和冲突分解是主瓶颈。

假设 C：不是所有视角都应该同权监督同一个 surface detail。
如果 reliable-view teacher-student 能提升衣服褶皱、鞋边、头发/手臂轮廓，说明监督聚合方式是主瓶颈。
```

这三个假设都不是 loss 小修。
它们分别对应 data alignment、representation、supervision aggregation 三个根因层。

### 下一批实验的推荐顺序

#### v182a：faithful replay / no-op resume

目标：

```text
证明当前代码能完整复现 v179c 的实际 module graph 和 checkpoint state。
```

必须完成：

```text
1. 从 v179c .hydra/config.yaml 启动，而不是手写近似配置。
2. 覆盖 dataset.root_dir 到当前工程数据路径。
3. load v179c best_ckpt 后检查 unexpected/missing keys。
4. render/eval 指标接近 v179c quick render。
5. 加一个 no-op 模块或极短 no-op resume，不允许指标明显退化。
```

验收标准：

```text
如果 v182a 不通过，所有 v182b/v183/v184 都先不要跑。
因为这时任何收益/退化都可能是配置结构错配造成的。
```

#### v182b：alignment diagnostic + calibration

目标：

```text
把“alignment-aware mask 发现错位”升级成“camera/pose residual 真正修正错位”。
```

先做诊断：

```text
统计每个 camera / frame / body part 的 silhouette distance、edge residual、foreground contour error；
重点看 shoulder、arm、waist、hairline、shoe edge；
输出 hardest camera-part-frame 排名。
```

再做校准：

```text
freeze texture / gaussian feature / opacity / scaling；
只训练 per-camera extrinsic residual、intrinsic small residual、per-frame root/pose small residual；
loss 以 mask edge distance、silhouette IoU、image gradient alignment 为主；
输出 corrected alignment cache。
```

验收标准：

```text
v179c best 在 corrected alignment 下，contour/edge residual 必须下降；
如果只是 RGB 指标波动但 contour residual 不降，v182b 不算成功。
```

#### v182c：corrected alignment 上的 texture/detail retrain

目标：

```text
在对齐修正后，验证 high-frequency texture 是否能恢复被平均掉的边界和细节。
```

训练约束：

```text
start = v179c best + corrected alignment cache；
freeze geometry / opacity / scaling；
train texture / detail_high_freq / structured trunk；
保留 K6 + camera-quality + tight photo；
不加新的大 loss 项。
```

验收标准：

```text
FG_LPIPS 必须超过 v179c；
quick masked L1 不得高于 v179c；
肩膀、手臂、腰侧、鞋边、衣服褶皱至少有一组稳定更清晰。
```

#### v183：representation split

触发条件：

```text
v182b 证明对齐修正不足，或 v182c 只能小幅改善但仍明显发糊。
```

实验内容：

```text
shared base texture：负责低频一致性；
explicit detail atlas / surface grid：负责可共享高频；
small view-conditioned residual：负责不可共享视角差异；
semantic boundary layer：负责 cloth/skin/hair/shoe 的边界归属。
```

验收标准：

```text
不能只看全图 PSNR；
必须看 foreground LPIPS、masked L1、hard contour crop、跨视角一致性；
view residual 不能变成 camera-id 记忆器。
```

#### v184：reliable-view teacher-student

触发条件：

```text
v183 后仍存在多视角平均导致的细节变软。
```

实验内容：

```text
为每个 surface anchor / semantic part 估计 view reliability；
选最可靠视角作为 teacher；
用 robust aggregation 生成 canonical detail target；
student 学共享 detail；
residual 只学 teacher 无法统一的部分。
```

验收标准：

```text
衣服褶皱、鞋边、头发轮廓、手臂边界必须比 v179c 更硬；
同时不能引入明显 view flicker 或 camera-specific color shift。
```

### 决策树

下一步不要并行乱跑，应按下面 gate 推进：

```text
Gate 0:
  v182a faithful replay 是否通过？

  No:
    只修 config/checkpoint/module graph；
    不跑任何新想法。

  Yes:
    进入 Gate 1。

Gate 1:
  v182b alignment calibration 是否显著降低 contour/edge residual？

  Yes:
    跑 v182c texture/detail retrain。

  No:
    alignment 不是当前主要瓶颈；
    直接进入 v183 representation split。

Gate 2:
  v182c 是否超过 v179c 的 FG_LPIPS / masked L1 / hardest crop？

  Yes:
    v182c 成为新 anchor；
    再考虑轻量 per-view residual。

  No:
    不继续调 v182c loss；
    进入 v183。

Gate 3:
  v183 是否解决 shared texture 平均问题？

  Yes:
    固化 v183 anchor，做可靠性/泛化检查。

  No:
    进入 v184 reliable-view teacher-student。
```

### 下一轮具体实验清单

建议按这个顺序命名和执行：

```text
v182a_faithful_replay_v179c_currentroot
  只验证 v179c .hydra config + v179c best_ckpt 在当前工程无损复现。

v182a_noop_resume_v179c_exact
  极短训练或 no-op 新模块，验证不会因为 resume pipeline 退化。

v182b_align_diagnostic_v179c
  不训练，统计 camera/frame/body-part 的 contour/edge residual。

v182b_aligncalib_camera_pose_residual
  只训练 camera/pose residual，输出 corrected alignment cache。

v182c_texture_after_align_v179c
  在 corrected alignment 上训练 texture/detail，冻结 geometry。

v183a_split_base_detail_viewres_probe
  如果 v182c 不够，再做 base/detail/view residual 分工。

v184a_reliable_view_teacher_probe
  如果 v183 仍有多视角平均问题，再做 teacher-student。
```

### 当前阶段的工程结论

```text
已完成：
  数据、SMPL、tinycudann、v179c anchor、v181a 实跑和渲染对比。

有效：
  HF source、K/frame-balanced、camera-quality、保守 tight photo、v179c 作为 anchor。

辅助有效但不能当主线：
  photometric/luma、alignment-aware mask 的诊断能力。

可以排除为主线：
  继续加 K、继续调 loss 小项、contour uncertainty、直接接 v181a、非 faithful 手写 option stack。

未完成且最值得做：
  v182a faithful replay、v182b alignment calibration、v182c corrected alignment texture retrain。

后续大方向：
  v183 representation split；
  v184 reliable-view teacher-student。
```

最重要的下一步不是再跑一个训练脚本，而是先把 v182a 做到可验收。
只有 faithful replay 通过，后面的清晰度实验才有可信因果结论。

## 2026-05-08 早间补充：v182 overnight 实跑结果与结论

本轮按照 2026-05-07 夜间复盘的计划，实际挂起了一个串行 overnight 队列：

```text
main queue:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/logs/v182_overnight_20260507_234247_bjt

fill watcher:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/logs/v182_overnight_20260507_234247_bjt/fill_remaining
```

执行时间：

```text
主队列：
  2026-05-07 23:42:47 BJT start
  2026-05-08 00:56:59 BJT complete

fill watcher：
  2026-05-08 00:57:29 BJT start first fill chunk
  2026-05-08 08:18:13 BJT complete

GPU：
  结束后 4 张 4090 均空闲
```

本轮实际完成：

```text
v182a_faithful_replay_render
v182a_contour_diagnostic
v182a_noop_resume_500
v182a_noop_resume_render
v182a_noop_contour_diagnostic
v182c_texture_after_anchor_6000
v182c_texture_after_anchor_render
v182c_texture_after_anchor_contour_diagnostic
v182c_fill_chunk1_6000
v182c_fill_chunk2_6000
v182c_fill_chunk3_6000
v182c_fill_chunk4_6000
v182c_fill_chunk5_6000
v182c_fill_chunk6_6000
v182c_fill_chunk7_3000
v182c_fill_chunk8_3000
v182c_fill_final_render
v182c_fill_final_contour_diagnostic
```

注意：

```text
本轮没有实现真正的 v182b camera/pose residual calibration。
v182b 目前只完成了 render-side contour diagnostic。
因此 v182c 本质上是“faithful v179c anchor 上的 texture/detail 续训”，
不是 corrected alignment 之后的 texture retrain。
```

### v182a faithful replay 结果

v182a faithful replay render：

```text
exp:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/v182a_faithful_replay_v179c_currentroot_20260507_234247_bjt

render metrics:
  psnr  = 32.474205
  ssim  = 0.982066
  lpips = 0.025829
```

v179c quick render baseline：

```text
exp:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/v179c_render_bestcmp_quick

render metrics:
  psnr  = 32.474205
  ssim  = 0.982066
  lpips = 0.025829
```

结论：

```text
v182a faithful render 通过。
当前工程使用 v179c .hydra/config.yaml + v179c best_ckpt + 当前 data root，
可以无损复现 v179c quick render。

这说明：
  1. 当前数据路径 / SMPL / tinycudann / renderer 是可用的；
  2. v181a 的大退化不是环境问题；
  3. 后续实验必须继续从 v179c actual hydra config 出发。
```

### v182a no-op resume 结果

v182a no-op resume 配置：

```text
start:
  v179c best_ckpt.pth

train:
  500 local iterations

intended no-op:
  position_lr = 0
  feature_lr = 0
  opacity_lr = 0
  scaling_lr = 0
  rotation_lr = 0
  pose_correction_lr = 0
  rigid_lr = 0
  non_rigid_lr = 0
  texture_lr = 0
  tex_latent_lr = 0
  camera_affine_lr = 0
```

结果：

```text
exp:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v182a_noop_resume_v179c_exact_20260507_234247_bjt

best metrics:
  iteration = 100900
  psnr      = 32.385284
  ssim      = 0.982047
  lpips     = 0.025435
  psnr_fg   = 21.528034
  ssim_fg   = 0.874763
  lpips_fg  = 0.150250
  l1_fg     = 0.043889
```

相对 v179c best：

```text
v179c:
  psnr_fg   = 21.797573
  ssim_fg   = 0.884818
  lpips_fg  = 0.132146
  l1_fg     = 0.042668

v182a noop:
  psnr_fg   = 21.528034
  ssim_fg   = 0.874763
  lpips_fg  = 0.150250
  l1_fg     = 0.043889

delta:
  lpips_fg  = +0.018104 worse
  l1_fg     = +0.001221 worse
```

主队列 gate 记录：

```text
GATE WARN v182a_noop:
  base_lpips_fg=0.132146
  candidate_lpips_fg=0.150250
  delta=0.018104
```

结论：

```text
v182a render replay 通过，但 v182a no-op resume 没有通过严格验收。

这说明：
  1. 只要进入 train loop，即使所有主要 lr 设为 0，也会出现前景指标退化；
  2. 退化可能来自 schedule/local_iteration side effect、boundary tag refresh、训练态模块状态、验证 split/metric 选择差异或某些未完全冻结的内部状态；
  3. 后续不能把“resume 后退化”简单解释成 texture 学坏；
  4. 真正做 v182b/v183 前，必须先把 no-op resume 的不变性查清楚。
```

需要重点排查：

```text
boundary-tagged subset updated 是否在 no-op resume 中改变了训练/验证行为；
texture / converter 是否存在 train-mode stateful schedule side effect；
iteration_offset=100400 后 local_iteration 和 schedule_iteration 混用是否改变输出；
best_test_metrics 的 best_eval/test selection 是否和 v179c 原始记录完全一致；
optimizer step 虽然 lr=0，但 scheduler / weight_decay / latent decay group 是否仍有副作用；
validation 时 scene.train()/scene.eval() 切换是否引入非确定性。
```

### v182c texture-after-anchor 主实验结果

v182c_texture_after_anchor_6000：

```text
exp:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v182c_texture_after_anchor_v179c_faithful_20260507_234247_bjt

start:
  v179c best_ckpt.pth

train:
  texture/detail continuation
  geometry / opacity / scaling / pose / rigid / non-rigid lr = 0
  texture_lr = 0.000035
  tex_latent_lr = 0.000035
  local iterations = 6000
```

结果：

```text
iteration = 105400
psnr      = 32.513474
ssim      = 0.982643
lpips     = 0.024766
psnr_fg   = 21.440548
ssim_fg   = 0.878287
lpips_fg  = 0.147651
l1_fg     = 0.044067
```

相对 v179c：

```text
lpips_fg:
  0.132146 -> 0.147651
  worse by +0.015505

l1_fg:
  0.042668 -> 0.044067
  worse by +0.001399
```

结论：

```text
v182c 主实验没有超过 v179c。
texture/detail 续训能略改善全图指标，但不能改善 foreground perceptual quality。
```

### fill watcher 长训结果

为了跑满夜间时间，主队列结束后继续串行跑了 8 个 fill chunk。
每个 chunk 都从上一阶段 best_ckpt 继续。

fill 结果摘要：

```text
chunk1:
  iteration = 111400
  lpips_fg  = 0.148046
  l1_fg     = 0.044118

chunk2:
  iteration = 117400
  lpips_fg  = 0.147830
  l1_fg     = 0.044138

chunk3:
  iteration = 121400
  lpips_fg  = 0.148140
  l1_fg     = 0.044270

chunk4:
  iteration = 122400
  lpips_fg  = 0.147646
  l1_fg     = 0.044069

chunk5:
  iteration = 125400
  lpips_fg  = 0.148247
  l1_fg     = 0.044016

chunk6:
  iteration = 131400
  lpips_fg  = 0.147769
  l1_fg     = 0.043952

chunk7:
  iteration = 132400
  lpips_fg  = 0.147741
  l1_fg     = 0.044115

chunk8 final:
  iteration = 133400
  lpips_fg  = 0.147551
  l1_fg     = 0.044135
```

最终 v182c fill checkpoint：

```text
exp:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v182c_fill_chunk8_3000_20260508_074603_bjt

render:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v182c_fill_chunk8_3000_20260508_074603_bjt_render_quick

visual compare:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v182c_fill_chunk8_3000_20260508_074603_bjt_render_quick/inspect/v179c_vs_v182c_final_hard_crops.png

contour diagnostic:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v182c_fill_chunk8_3000_20260508_074603_bjt_render_quick/diagnostics/contour_summary.json
```

最终 checkpoint 对比：

```text
v179c best:
  psnr      = 32.707905
  ssim      = 0.983502
  lpips     = 0.021380
  psnr_fg   = 21.797573
  ssim_fg   = 0.884818
  lpips_fg  = 0.132146
  l1_fg     = 0.042668

v182c final:
  psnr      = 32.508621
  ssim      = 0.982988
  lpips     = 0.024633
  psnr_fg   = 21.401485
  ssim_fg   = 0.879987
  lpips_fg  = 0.147551
  l1_fg     = 0.044135

delta v182c_final - v179c:
  psnr      = -0.199284 worse
  ssim      = -0.000514 worse
  lpips     = +0.003253 worse
  psnr_fg   = -0.396088 worse
  ssim_fg   = -0.004831 worse
  lpips_fg  = +0.015405 worse
  l1_fg     = +0.001467 worse
```

### quick render 与 contour diagnostic

quick render 指标：

```text
v179c quick:
  psnr  = 32.474205
  ssim  = 0.982066
  lpips = 0.025829

v182a faithful replay:
  psnr  = 32.474205
  ssim  = 0.982066
  lpips = 0.025829

v182a noop render:
  psnr  = 32.480183
  ssim  = 0.982276
  lpips = 0.025573

v182c initial render:
  psnr  = 32.638107
  ssim  = 0.982917
  lpips = 0.024912

v182c final render:
  psnr  = 32.623344
  ssim  = 0.983208
  lpips = 0.024797
```

这组 quick render 看起来 v182c 全图指标略好。
但 foreground / hard contour 诊断不是这样：

```text
v179c quick contour:
  mean_fg_l1                 = 0.047433
  mean_boundary_l1           = 0.068378
  mean_interior_l1           = 0.035735
  mean_edge_symmetric_dist_px = 3.363018

v182c final contour:
  mean_fg_l1                 = 0.047867
  mean_boundary_l1           = 0.068907
  mean_interior_l1           = 0.035630
  mean_edge_symmetric_dist_px = 3.347795
```

同帧 foreground L1 复核：

```text
n = 30 quick samples
v179c mean fg L1  = 0.049227
v182c mean fg L1  = 0.049602
delta             = +0.000375 worse

v182c better count = 12
v179c better count = 18
```

视觉对比结论：

```text
v182c final 和 v179c 整体非常接近；
局部有些帧差异略小，但不稳定；
hard contour、手臂边缘、衣服/鞋边没有形成稳定更硬的结果；
部分样本 v182c 反而更糊或前景误差更大；
全图 PSNR/SSIM/LPIPS 的改善主要来自背景/低频区域，不能代表主体清晰度提升。
```

### 本轮有效信息

本轮真正确认了三件事：

```text
1. faithful render substrate 是可用的。
   v179c .hydra + v179c best_ckpt 在当前工程可以无损 render。

2. train-loop no-op resume 仍存在不变性问题。
   即使主要 lr 为 0，500 iter 后前景指标也会退。
   这说明 resume pipeline 仍需单独排查，不能把所有退化归因于新方法。

3. texture-only continuation 不是清晰度上限答案。
   从 v179c 继续 texture/detail 长训到 133400 iteration，仍没有超过 v179c foreground/hard contour。
```

### 本轮可以排除的方向

可以进一步降级或停止投入：

```text
1. v182c_texture_after_anchor 这种未做 alignment correction 的 texture-only 长训。
2. 继续把 texture_lr 从 3e-5 到 5e-5 附近小幅调参。
3. 继续单纯延长 v179c/v182c texture/detail 训练时间。
4. 只看全图 PSNR/SSIM/LPIPS 作为清晰度决策指标。
5. 不解决 no-op resume 不变性，就直接做更复杂分支。
```

特别是第 4 点很重要：

```text
v182c quick render 全图指标略好，
但 foreground LPIPS / foreground L1 / contour diagnostic / hard crop 视觉都不支持它成为新 anchor。
```

### 当前 anchor 判断

```text
继续保留 v179c 作为唯一主线 anchor。

不要使用：
  v181a
  v182a_noop
  v182c_texture_after_anchor
  v182c_fill_chunk*

作为后续主线底座。
```

当前可作为资料保留：

```text
v182a faithful replay render:
  用于证明当前工程可复现 v179c。

v182 contour diagnostics:
  用于定位 hardest camera/frame。

v182c fill final render:
  用于证明 texture-only 长训不解决 hard contour。
```

### 下一步更新后的决策

原计划中的 Gate 需要更新：

```text
Gate 0A:
  faithful render replay 是否通过？

  已通过。

Gate 0B:
  train-loop no-op resume 是否严格不退？

  未通过。
  下一步必须先查清 no-op resume 退化机制。

Gate 1:
  v182b alignment calibration 是否显著降低 contour/edge residual？

  未执行。
  只有 Gate 0B 修好后才值得做。
```

推荐下一步不是继续跑 v182c，而是先做两个工程/根因实验：

```text
v182a2_noop_resume_invariance_audit:
  目标：
    找出 no-op resume 500 iter 为什么退。

  做法：
    1. 不进入 optimizer.step，只跑 validation；
    2. 进入 train loop 但完全跳过 backward/step；
    3. 只跑 forward/backward，不 optimizer.step；
    4. 逐项打开 boundary tag update / schedule_context / train_sampler；
    5. 对比 checkpoint tensor hash 和 render 输出差异。

  验收：
    no-op resume 后 render 和 v179c bitwise 或近似一致；
    foreground metrics 不退。

v182b_aligncalib_camera_pose_residual:
  目标：
    在 no-op resume 可信后，真正训练 per-camera/per-frame alignment residual。

  做法：
    freeze texture/gaussian；
    只训练 camera/pose residual；
    用 silhouette distance / edge alignment / mask IoU；
    输出 corrected alignment cache。
```

如果 v182a2 不能修好：

```text
不要再通过 train.py 做续训型根因实验。
应改为：
  1. 离线构建 alignment diagnostic；
  2. 单独写校准脚本；
  3. 避免触发当前 train loop 的隐式 side effect。
```

如果 v182a2 修好但 v182b alignment calibration 无效：

```text
直接进入 v183 representation split。
不要再回到 texture-only continuation。
```

### 2026-05-08 结论

本轮不是白跑。
它把一个关键岔路排除了：

```text
“只要 faithful 继承 v179c，然后继续训 texture/detail，就能变清晰”
这个假设不成立。
```

当前更可信的根因仍然是：

```text
1. 局部 camera/pose/SMPL 对齐误差；
2. 多视角监督高频冲突；
3. shared texture 表示平均 hard contour；
4. train-loop resume 存在未解释的非 no-op 副作用。
```

下一步必须先把 no-op resume 不变性修掉，再做真正的 alignment calibration。
否则继续训练只会产生“看似有全图指标改善、但主体清晰度不提升”的假阳性结果。

## 2026-05-08 补充：v183 no-op / camera-geometry 实跑结果与清晰度根因更新

本轮按照上一节判断，先修实验可信度，再做真正的 alignment 方向探针。

新增/修正代码：

```text
scene/__init__.py:
  增加 resume.restore_converter_optimizer_state
  增加 resume.restore_converter_scheduler_state
  增加 resume.restore_converter_optimizer_preserve_config_lrs
  目的是让从 v179c best 续训时尽量恢复原 optimizer/scheduler 状态，
  同时允许保留当前实验配置中的 lr=0 / 新增 param group。

train.py:
  增加 opt.diagnostic_validate_at_start
  增加 opt.diagnostic_no_backward
  增加 opt.diagnostic_skip_optimizer_step
  修复 validation opacity_histogram 对 requires_grad tensor 直接 numpy 的问题。

models/gaussian_converter.py:
  新增 CameraGeometryCorrection
  每个 camera 学一个受限 rotvec + translation residual。
  rot_max_deg = 0.16
  trans_max   = 0.004
  camera_geometry_lr = 0.00035

tools:
  tools/start_377_stageA2_v183a_v179c_resume_invariance.sh
  tools/start_377_stageA2_v183b_v179c_camera_geometry_align.sh
  tools/run_377_v183_alignment_queue.sh
```

关键工程修正：

```text
v181/v182 早期脚本用 option stack 近似复现 v179c，结果会丢 v179c 的真实 texture/HF/structured trunk 结构。
v183 脚本改为直接继承：

  exp/377_multiview_explicit_hq_rootfix_resume_v179c_v178c_k6_camquality_tightphoto_screen3000/.hydra/config.yaml

只在这个真实 hydra config 上追加 v183 控制项。
这避免了“实验方法没问题，但配置近似导致结构不等价”的假失败。
```

### v183a：当前代码下的 no-op resume baseline

实验：

```text
exp:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v183a_v179c_resume_invariance_20260508_095815_bjt

render:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v183a_v179c_resume_invariance_20260508_095815_bjt_render_quick

log:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/logs/v183_alignment_20260508_095815_bjt/v183a_resume_invariance.log
```

设置：

```text
start = v179c best_ckpt.pth
restore converter optimizer = true
restore converter scheduler = true
all lr = 0
diagnostic_validate_at_start = true
diagnostic_skip_optimizer_step = true
iterations = 500
```

结果：

```text
v183a:
  iteration = 100900
  psnr      = 32.383667
  ssim      = 0.981858
  lpips     = 0.025712
  psnr_fg   = 21.592262
  ssim_fg   = 0.873688
  lpips_fg  = 0.150673
  l1_fg     = 0.043513
```

与历史 v179c 保存的 best_test_metrics 对比：

```text
v179c saved best_test_metrics:
  iteration = 100400
  psnr      = 32.707905
  ssim      = 0.983502
  lpips     = 0.021380
  psnr_fg   = 21.797573
  ssim_fg   = 0.884818
  lpips_fg  = 0.132146
  l1_fg     = 0.042668

v183a current validation:
  lpips_fg  = 0.150673
```

这个差异不能简单解释成“500 步 no-op 把模型训坏了”，因为：

```text
1. v183a 在 checkpoint load 后立即 validation，已经是同一组当前 validation 指标；
2. 500 步之后指标与 immediate validation 基本一致；
3. v183a 设置 diagnostic_skip_optimizer_step=true，没有 optimizer.step；
4. quick render 里 v179c / v183a 的全图指标和肉眼图像基本一致。
```

因此 v182 里“train-loop no-op resume 退化”的结论需要更新为：

```text
旧判断:
  train loop 本身会让 no-op resume 退化。

新判断:
  当前工程下 no-op train-loop 已经基本稳定；
  更大的差异来自历史 v179c saved metrics 与当前 validation/foreground metric substrate 不完全等价。
  后续 gate 不能再只拿历史 best_test_metrics 做硬比较，
  必须同时使用当前代码下的 v183a no-op baseline 和 quick render/contour baseline。
```

这点很关键：否则会把“指标口径漂移”误判成“新方法退化”。

### v183b：camera geometry alignment 探针

实验：

```text
exp:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v183b_v179c_camera_geometry_align_20260508_095815_bjt

render:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v183b_v179c_camera_geometry_align_20260508_095815_bjt_render_quick

visual compare:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/logs/v183_alignment_20260508_095815_bjt/v183_visual_compare_gt_v179c_v183a_v183b.jpg

summary:
  /remote-home/ming/semantic-editable-avatar-main/exp/stageA2/logs/v183_alignment_20260508_095815_bjt/summary.tsv
```

设置：

```text
start = v179c best_ckpt.pth
freeze:
  gaussian xyz/features/opacity/scaling/rotation
  texture
  rigid / non-rigid deformer
  pose correction
  camera affine

train:
  only camera_geometry for train cameras 1..20

loss:
  foreground L1
  boundary L1
  mask boundary
  silhouette outer/inner/shell
  no perceptual/detail/edge texture losses
```

结果：

```text
v183b:
  iteration = 100700
  psnr      = 32.545799
  ssim      = 0.982587
  lpips     = 0.026021
  psnr_fg   = 21.464815
  ssim_fg   = 0.878218
  lpips_fg  = 0.151868
  l1_fg     = 0.043673
```

相对 v183a 当前 no-op baseline：

```text
v183a:
  psnr_fg   = 21.592262
  ssim_fg   = 0.873688
  lpips_fg  = 0.150673
  l1_fg     = 0.043513

v183b:
  psnr_fg   = 21.464815
  ssim_fg   = 0.878218
  lpips_fg  = 0.151868
  l1_fg     = 0.043673

delta:
  psnr_fg   = -0.127447 worse
  ssim_fg   = +0.004530 better
  lpips_fg  = +0.001195 worse
  l1_fg     = +0.000160 worse
```

quick render 全图指标：

```text
v179c quick:
  psnr  = 32.474205
  ssim  = 0.982066
  lpips = 0.025829

v183a quick:
  psnr  = 32.471283
  ssim  = 0.982067
  lpips = 0.025835

v183b quick:
  psnr  = 32.684021
  ssim  = 0.982850
  lpips = 0.026132
```

contour diagnostic 同口径：

```text
v179c:
  mean_fg_l1                  = 0.047433
  mean_boundary_l1            = 0.068378
  mean_interior_l1            = 0.035735
  mean_edge_symmetric_dist_px = 3.363018

v183a:
  mean_fg_l1                  = 0.047417
  mean_boundary_l1            = 0.068390
  mean_interior_l1            = 0.035708
  mean_edge_symmetric_dist_px = 3.380929

v183b:
  mean_fg_l1                  = 0.047310
  mean_boundary_l1            = 0.067084
  mean_interior_l1            = 0.035334
  mean_edge_symmetric_dist_px = 3.287968
```

这组结果说明：

```text
1. v183b 对 contour/edge alignment 有弱正信号。
   mean_edge_symmetric_dist_px 从 v183a 的 3.3809 降到 3.2880；
   mean_boundary_l1 也从 0.06839 降到 0.06708。

2. 这个弱正信号没有转化为 foreground perceptual clarity。
   lpips_fg 从 0.150673 变成 0.151868，反而更差；
   视觉拼图中衣服、肩膀、手臂、脸、鞋边没有明显更清晰。

3. v183b 全图 PSNR/SSIM 变好，不能代表主体清晰度变好。
   这再次证明决策指标必须以 foreground / hard contour / visual crop 为主。
```

还要注意一个代码逻辑上的限制：

```text
v183b 的 camera_geometry_train_camera_ids 只包含训练相机 1..20。
quick render / validation 主要看 held-out camera 21..23。

在 GaussianConverter.forward 中：
  deformed_gaussians = self.camera_geometry(deformed_gaussians, camera, iteration=iteration)

CameraGeometryCorrection 对不在 train_camera_id_set 中的 camera 会直接 return gaussians。

所以 v183b 学到的 per-camera geometry residual 不会直接作用到 held-out camera 21..23。
held-out render 里的差异，更多来自训练过程中 boundary tag / live boundary state / schedule state 的间接变化，
而不是“训练相机的 camera residual 泛化到了测试相机”。
```

这意味着：

```text
v183b 不能证明 per-camera geometry calibration 已解决清晰度问题；
它只能证明 alignment 误差确实能影响 contour 指标，
但单纯在 train cameras 上学一个 tiny camera residual，不足以提升 held-out foreground clarity。
```

### v183 后可以更新/排除的判断

可以更新：

```text
1. no-op resume 问题已经从“train-loop 会改坏模型”变成“当前 validation/metric substrate 与历史 saved metrics 不一致”。
   后续实验必须建立 current-code baseline。

2. alignment 是真问题，但 v180/v183b 证明：
   只改 loss mask 或 tiny per-camera residual，不足以产生肉眼清晰度突破。

3. texture-only continuation 已经由 v182c/fill 排除；
   camera-affine low-frequency residual 已经由 v181a 降级；
   tiny camera-geometry residual 已经由 v183b 降级。
```

可以排除/降级：

```text
1. 继续从 v179c 做 texture_lr / tex_latent_lr 小幅调参。
2. 继续单独加 edge / boundary / luma loss 权重。
3. 继续把 alignment-aware mask 做复杂，但不改变数据/表示。
4. 只训练 per-camera affine 或 per-camera geometry 这种低自由度残差。
5. 只看全图 PSNR/SSIM/LPIPS 判断清晰度。
```

当前仍保留为事实的有效模块：

```text
1. HF source / structured trunk 高频通路是有效的；
   v175a 是第一次明显突破。

2. same-frame K2/K4/K6 dense supervision 是有效的；
   v176-v179 连续证明。

3. camera-quality + tight photometric 是有效辅助；
   v179b/v179c 证明可以叠加。

4. hardest contour diagnostic 是有效决策工具；
   它能发现全图指标的假阳性。
```

## 2026-05-08 根因重判：为什么清晰度一直卡住

结合 v165-v183 和当前代码逻辑，清晰度卡住不是单一参数问题，而是三个层面的结构性冲突叠加。

### 根因 1：当前模型仍在用单一 canonical 主输出平均多视角冲突

当前 texture 路径虽然已经有：

```text
structured trunk
detail_high_frequency
luma branch
face/local carrier
owner head
boundary head
camera affine
camera geometry
```

但核心输出仍是：

```text
同一组 canonical gaussian / texture feature
解释 20 个 train cameras 的颜色、曝光、边界位置和局部高频。
```

多视角数据里实际存在：

```text
相机颜色/曝光/白平衡差异；
局部 camera / SMPL / pose 对齐误差；
mask 边界误差；
训练相机质量差异；
view-dependent 细节和遮挡差异。
```

当这些差异进入同一个 shared texture 输出时，最稳定的优化解就是：

```text
低频对齐；
边界变软；
高频幅度降低；
hard contour 被平均；
衣服褶皱、肩膀、手臂、头发、鞋边不够锐。
```

v175-v179 能提升，是因为它们增强了 HF source 并提高同帧多视角监督密度；
但到 v179c 后收益变窄，说明 shared canonical 表示已经接近当前能承载的上限。

### 根因 2：boundary/head 路由命中了，但输出幅度长期接近零

多轮日志都显示 hardest contour 不再是完全找不到：

```text
boundary_focus    ~= 0.080
boundary_takeover ~= 0.048
owner_takeover    ~= 0.64 - 0.65
support           ~= 0.96
```

但真正画到最终颜色里的 boundary 输出仍很小：

```text
v183b typical:
  boundary_color ~= 0.0006 - 0.0012
  boundary       ~= 0.0002 - 0.0005
  hf_scale       = 0.240
  luma           ~= 0.021 - 0.024
  tiny           ~= 0.015 - 0.022
```

这说明：

```text
系统已经能把点标成 boundary / hard subset；
但 boundary branch 没有足够可信的高频目标，也没有足够自由度/幅度去画硬边界；
继续提高 boundary loss 或 topk_ratio，只会改变路由数字，不会自然产生硬边界颜色。
```

v173/v174/v180/v183 都支持这个结论：

```text
继续拧 boundary/head 不是主突破口。
```

### 根因 3：alignment 误差是真问题，但现在的修法只在监督侧或低自由度残差侧打补丁

v180：

```text
alignment-aware mask 确实命中 hardest contour，
但没有带来肉眼清晰度突破。
```

v183b：

```text
tiny camera geometry residual 让 contour 指标小幅改善，
但 foreground LPIPS 和视觉清晰度没有改善。
```

原因是：

```text
loss mask / uncertainty / tiny camera residual 只是在当前表示上重新分配梯度；
它没有重建相机/人体/轮廓的一致几何；
也没有把不可共享的多视角细节从 shared canonical texture 中分离出去。
```

更重要的是：

```text
per-camera residual 只对训练相机有效；
如果目标是 held-out view 清晰度，它不会自动泛化。
真正需要的是一个能影响所有视角的数据/几何层修复，
或者一个能在新视角也合理工作的 view-conflict 表示。
```

### 根因 4：当前实验曾被 metric substrate 和 config substrate 干扰

v181a 暴露：

```text
option stack 近似 v179c 会丢真实 texture/HF/structured trunk 模块；
partial load unexpected keys 会让实验不再是严格从 v179c 续训。
```

v183a 暴露：

```text
历史 v179c saved best_test_metrics 与当前 validation 指标不完全等价；
必须建立 current-code no-op baseline。
```

因此以后判断实验必须满足：

```text
1. 从真实 .hydra/config.yaml 继承；
2. 先跑 current-code no-op baseline；
3. 同时看 foreground metrics、quick render、contour diagnostic 和 hard crop；
4. 不再用旧 best_test_metrics 单独做 gate。
```

否则会继续产生“方法失败/成功”的假判断。

## 2026-05-08 下一阶段：不要再小修，进入三条大修路线

下一阶段不要再做：

```text
texture_lr 小调；
camera_affine_lr 小调；
boundary loss 小调；
mask width / topk_ratio 小调；
再拉长 v179c / v182c texture-only 训练；
只训练 tiny camera residual。
```

真正应该做的是把“多视角冲突”从 shared canonical texture 里拆出来。

### 大修路线 A：离线 alignment / calibration substrate，而不是 loss mask

目标：

```text
在训练前建立 corrected alignment substrate，
不要只在 train loop 里临时给 loss mask 降权。
```

做法：

```text
1. 用 v179c / v183 diagnostic 找 hardest camera-frame：
   c21/c22/c23 的 edge_symmetric_dist_px 高样本；
   shoulder / arm / waist / hairline。

2. 离线优化一个低维 alignment cache：
   per-camera small SE(3) / 2D residual；
   可选 per-frame root / SMPL global orient residual；
   用 silhouette edge distance、mask IoU、foreground boundary L1 做目标。

3. 输出 corrected camera/pose/mask cache：
   不在 texture train loop 里边训边改；
   先把几何/边界监督变成稳定输入。

4. 在 corrected substrate 上从 v179c 重新训练 HF/source 或 representation split。
```

关键区别：

```text
v180/v183b 是监督侧或低自由度补丁；
路线 A 是数据/几何 substrate 重建。
```

验收：

```text
current-code no-op baseline 不变；
corrected render contour:
  mean_edge_symmetric_dist_px 至少下降 0.3 - 0.5 px；
  hardest 10 samples 明显下降；
  foreground LPIPS 不应恶化。
```

如果离线 calibration 只能带来 0.05 - 0.10 px 的小变化，就不要继续；
说明最大瓶颈不是几何 substrate，而是表示/监督冲突。

### 大修路线 B：representation split，把不可共享多视角差异从 canonical texture 分离出去

目标：

```text
不是再给 shared texture 加一点 residual，
而是明确拆成：
  shared canonical base
  view/camera conflict residual
  robust gate / regularizer
```

建议结构：

```text
base branch:
  继承 v179c shared canonical texture；
  保持主体颜色和大结构。

residual branch:
  low-rank camera/view embedding；
  输入 view direction / camera group / region feature；
  输出小幅 RGB residual 或 HF residual；
  amplitude cap 分 region 控制，例如 0.015 - 0.035；
  不允许改 opacity/scaling/geometry。

gate:
  只在 foreground detail / cloth / skin high-error region 开；
  boundary 区域单独限制，防止漏色；
  test view 用 camera-independent view direction 分支，不直接依赖训练 camera id。

regularization:
  residual L2；
  view consistency；
  leave-one-camera validation；
  residual sparsity / smoothness。
```

这条路线要解决的是：

```text
同一个 canonical texture 同时解释所有相机，必然会平均。
必须允许模型表达“这些差异不是 canonical texture 本身”，
同时又不能变成记相机。
```

验收：

```text
相对 current-code v183a baseline 和 v179c quick:
  foreground LPIPS 必须下降；
  contour 不恶化；
  c21/c22/c23 held-out view 不能崩；
  residual 可视化必须低幅、局部、可解释。
```

### 大修路线 C：robust multi-view teacher-student，而不是平均所有视角

目标：

```text
不要让低质量/错位/偏色视角直接平均 shared texture。
先构建可靠视角 teacher，再让 student 学稳定高频。
```

做法：

```text
1. 为每个 camera-frame-region 估计可靠性：
   edge alignment score；
   mask confidence；
   photometric consistency；
   camera quality；
   local sharpness。

2. 生成 region/patch 级 teacher target：
   高可靠视角提供 high-frequency/detail 目标；
   不可靠视角只提供低频/轮廓约束，或降权。

3. student 训练：
   shared canonical base 学 consensus；
   HF/detail branch 学 teacher 高频；
   不让 outlier view 的高频梯度直接参与平均。
```

这条路线和 v165/v176-v179 的区别：

```text
v176-v179 是更密地采样所有视角；
路线 C 是先判断哪些视角/patch 可靠，再决定谁能贡献高频。
```

验收：

```text
衣服褶皱、鞋边、裤腿暗部必须比 v179c 更清楚；
肩膀/手臂/hairline 不能因为 teacher 错选而变形；
全图指标可持平，但 foreground / hard contour 必须改善。
```

### 推荐执行顺序

```text
Step 0: 固定 current-code baseline
  以后所有实验先跑：
    v179c render quick
    v183a current no-op validation
    contour diagnostic
  不再直接拿历史 saved best_test_metrics 做唯一 gate。

Step 1: 做路线 A 的离线 alignment diagnostic/calibration 小闭环
  不是小修 loss；
  是确认 corrected substrate 能否显著降低 contour distance。
  如果 contour distance 没明显降，立刻停止这条线。

Step 2: 如果路线 A 有效
  在 corrected substrate 上跑 representation split，而不是再 texture-only continuation。

Step 3: 如果路线 A 无效或收益很小
  直接进入路线 B/C：
    representation split
    reliable-view teacher-student

Step 4: 只有 B/C 出现正信号后
  再考虑把 K6 / camera-quality / tight photometric 叠回去。
```

### 当前最终判断

截至 v183：

```text
当前清晰度卡住的核心不是：
  训练步数不够；
  texture lr 不对；
  boundary loss 不够；
  单个 camera affine / camera geometry 没调好。

核心是：
  多视角颜色/曝光/局部对齐/边界冲突仍被同一个 shared canonical texture 平均；
  boundary/head 虽然能定位 hardest contour，但没有真实高频输出幅度；
  当前修法多数还停留在 loss 权重或低自由度 residual，没改变数据/表示层的冲突结构。
```

所以后续要提高清晰度，必须从以下至少一个层面动刀：

```text
1. 数据/几何 substrate：
   离线 corrected alignment cache。

2. 表示层：
   shared base + view/camera conflict residual + robust gate。

3. 监督层：
   reliable-view teacher-student，避免 outlier view 平均高频。
```

这三条才是根因级修复。
继续小幅调参只会重复 v180-v183 的结果：全图指标偶尔好一点，但主体清晰度不上去。

## 2026-05-08 补充：current-code v185-v193 可比实验闭环

本轮实验先修正了一个判断前提：历史 v179c 的 `best_test_metrics.json` 不能直接和当前代码/数据/验证入口下的新实验混用。

原因是 v183a / v185 replay 暴露出：

```text
历史 v179c saved best_test_metrics:
  FG_LPIPS = 0.132146314

当前代码环境 replay / quick validation:
  v185 replay / v179c quick 约在 0.150668 附近
```

因此 v185 之后的判断口径改为：

```text
1. 只在 current-code / current-data / current-validation 下横向比较；
2. 每个候选都看 best_eval FG_LPIPS；
3. 同步看 quick render 的 contour diagnostic；
4. 不再把历史 0.132146 当作本轮实验的直接目标线。
```

### v185-v193 指标汇总

本轮可比实验结果如下：

```text
v186a all-view temporal / texture-camera fit
  best_iter = 102800
  FG_LPIPS = 0.147299185
  L1_fg    = 0.042930
  PSNR_fg  = 21.6052
  SSIM_fg  = 0.878549
  结论：相对 current-code replay 有改善，但离清晰度目标仍远。

v187a heldout camera geometry / L1-contour probe
  best_iter = 100700
  FG_LPIPS = 0.157509387
  L1_fg    = 0.040252
  PSNR_fg  = 21.9519
  SSIM_fg  = 0.889198
  结论：L1 / PSNR 变好，但 perceptual clarity 明显变差；粗暴几何/L1 方向排除。

v187b heldout camera geometry + perceptual
  best_iter = 101600
  FG_LPIPS = 0.142890707
  L1_fg    = 0.041156
  PSNR_fg  = 21.7960
  SSIM_fg  = 0.880630
  结论：perceptual 约束比 L1/contour 几何更有效，是后续底座。

v188a geom perceptual + small texture restore
  best_iter = 103200
  FG_LPIPS = 0.141120255
  L1_fg    = 0.040700
  PSNR_fg  = 21.8323
  SSIM_fg  = 0.878818
  结论：有效；几何/perceptual 底座后接轻量 texture restore 有收益。

v189a geom + local pose probe
  best_iter = 104100
  FG_LPIPS = 0.140527174
  L1_fg    = 0.040551
  PSNR_fg  = 21.8250
  SSIM_fg  = 0.879171
  结论：小幅有效；局部 pose / geometry 能继续压一点 perceptual error，但收益有限。

v190a texture restore from v189a
  best_iter = 105150
  FG_LPIPS = 0.140662074
  L1_fg    = 0.040657
  PSNR_fg  = 21.7974
  SSIM_fg  = 0.878275
  结论：差于 v189a；普通 texture-only continuation 排除。

v191a geom/pose extend from v189a
  best_iter = 104700
  FG_LPIPS = 0.141139850
  L1_fg    = 0.040700
  PSNR_fg  = 21.7944
  SSIM_fg  = 0.878320
  结论：继续放开 structure / pose 会回退；排除。

v191b camera-only probe from v189a
  best_iter = 104250
  FG_LPIPS = 0.142950609
  L1_fg    = 0.041079
  PSNR_fg  = 21.7362
  SSIM_fg  = 0.875899
  结论：camera-only continuation 明显变差；排除。

v191c pose-only probe from v189a
  best_iter = 104250
  FG_LPIPS = 0.141591787
  L1_fg    = 0.040810
  PSNR_fg  = 21.7856
  SSIM_fg  = 0.878299
  结论：pose-only continuation 变差；排除。

v192a stable interior texture from v189a
  best_iter = 105150
  FG_LPIPS = 0.138978705
  L1_fg    = 0.040488
  PSNR_fg  = 21.8471
  SSIM_fg  = 0.882636
  结论：明显有效；说明边界错位区域正在污染 texture perceptual / patch 监督。

v193a adaptive stable texture from v192a
  best_iter = 106200
  FG_LPIPS = 0.138587683
  L1_fg    = 0.040455
  PSNR_fg  = 21.8699
  SSIM_fg  = 0.882742
  结论：当前 current-code 最好；自适应稳定内区过滤继续带来小幅收益。
```

### quick render / contour diagnostic

同一组 quick render 诊断结果：

```text
v179c current replay quick
  mean_fg_l1                 = 0.047993
  mean_boundary_l1           = 0.068778
  mean_interior_l1           = 0.036207
  mean_edge_symmetric_dist_px= 3.324702

v188a quick
  mean_fg_l1                 = 0.045072
  mean_boundary_l1           = 0.064986
  mean_interior_l1           = 0.033119
  mean_edge_symmetric_dist_px= 2.706178

v189a quick
  mean_fg_l1                 = 0.044755
  mean_boundary_l1           = 0.064616
  mean_interior_l1           = 0.032729
  mean_edge_symmetric_dist_px= 2.709678

v192a quick
  mean_fg_l1                 = 0.044722
  mean_boundary_l1           = 0.065595
  mean_interior_l1           = 0.032326
  mean_edge_symmetric_dist_px= 2.681046

v193a quick
  mean_fg_l1                 = 0.044671
  mean_boundary_l1           = 0.065710
  mean_interior_l1           = 0.032286
  mean_edge_symmetric_dist_px= 2.775026
```

对应渲染和对比图：

```text
v192a render:
  exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v192a_v189a_stable_interior_texture_20260508_v192a_stable_interior_texture_render_quick

v193a render:
  exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v193a_v192a_adaptive_stable_texture_20260508_v193a_adaptive_stable_texture_render_quick

v179c/v188a/v189a/v192a/v193a contact sheet:
  exp/stageA2/377_multiview_explicit_hq_rootfix_resume_v193a_v192a_adaptive_stable_texture_20260508_v193a_adaptive_stable_texture_render_quick/diagnostics/compare_v179c_v188a_v189a_v192a_v193a_contact.png
```

### 当前代码改动

本轮为 v192a / v193a 增加的有效机制：

```text
train.py:
  新增 _perceptual_stable_region_mask
  支持 perceptual_exclude_boundary_width
  支持 perceptual_stable_erode_kernel_size
  支持 perceptual_stable_min_pixels / fallback
  支持 patch_perceptual_min_mask_coverage
  支持 perceptual_adaptive_boundary_exclude_enable
  支持 perceptual_adaptive_boundary_width / error_threshold / edge_protect
  全局、face、shoulder_arm、upper_torso、upper_torso_core、waist 的 LPIPS / patch 感知监督都接入稳定内区 mask

models/pose_correction/pose_correction.py:
  DirectPoseOptimization 新增可训练参数开关
  支持 train_root_orient / train_pose_body / train_pose_hand / train_trans / train_betas
  支持 pose_body_train_joint_ids，只训练指定 SMPL body joints

models/gaussian_converter.py:
  pose correction optimizer 支持使用 pose_correction.trainable_parameters()

tools:
  新增 v189a-v193a 启动脚本
  v193a 脚本从 v192a best 继续，冻结几何/pose，只训练高频纹理头，并开启 adaptive stable texture mask
```

验证：

```text
python -m py_compile train.py render.py models/pose_correction/pose_correction.py models/gaussian_converter.py 通过
bash -n tools/start_377_stageA2_v193a_v192a_adaptive_stable_texture.sh 通过
v193a 1400 iter 完整训练完成，best checkpoint 可渲染
```

### 新结论：清晰度和边界已经分成两个问题

v192a / v193a 的关键结论：

```text
稳定内区 texture perceptual / patch 监督是有效方向。
v193a 已经把 current-code best FG_LPIPS 压到 0.138587683。
```

但 contour diagnostic 同时说明：

```text
v193a 的前景/内区 L1 是当前最好；
但边界 edge distance 从 v192a 的 2.681046 回退到 2.775026；
boundary_l1 也没有变好。
```

所以现在不能再把“清晰度”和“边界”混成一个 loss 继续调。

当前应拆成两条线：

```text
1. Texture clarity line:
   保留 v193a best；
   继续围绕 stable interior / adaptive reliable mask 做纹理清晰度；
   不让 misaligned boundary 区域直接参与 LPIPS / patch 高频监督。

2. Boundary geometry / mask line:
   不再指望 texture loss 修边界；
   单独做离线 alignment / silhouette calibration / mask substrate 修复；
   目标是先把 mean_edge_symmetric_dist_px 从 2.7 左右继续降下来。
```

### 对前面三条大修路线的修正

v193a 给路线选择增加了一个新判断：

```text
路线 C 的思想已经被部分验证：
  不可靠边界不应贡献高频/LPIPS；
  可靠内区可以继续提供清晰纹理监督。

但路线 A 仍然必要：
  如果不单独修边界几何/掩码，
  v193a 这类 texture clarity 会继续出现“内区更清楚、边界仍漂/软”的瓶颈。
```

后续优先级调整为：

```text
Step 1:
  以 v193a best 作为当前清晰度底座。

Step 2:
  单独做 boundary alignment substrate：
    per-camera / per-view silhouette edge correction；
    hardest c21/c22/c23 samples；
    用 contour_summary 的 top_hard_samples 做闭环。

Step 3:
  如果边界距离能明显降低，再把 corrected substrate 叠回 v193a texture clarity。

Step 4:
  如果边界 substrate 无法降低 edge distance，再进入 representation split：
    shared base + view-conflict residual；
    不再继续 texture-only 或 pose-only continuation。
```

### 当前可保留 / 可排除项

可保留：

```text
v187b:
  perceptual geometry 底座有效。

v188a:
  geometry + texture restore 有效。

v189a:
  小范围 pose / geometry 有效，但收益有限。

v192a:
  固定稳定内区 texture 监督有效。

v193a:
  自适应稳定内区 texture 监督当前最好。
```

可排除：

```text
v187a:
  单纯 L1 / contour geometry，perceptual clarity 变差。

v190a:
  普通 texture-only continuation。

v191a:
  继续放开 geometry / pose。

v191b:
  camera-only continuation。

v191c:
  pose-only continuation。
```

当前最终判断更新为：

```text
清晰度卡住的主因已经进一步定位：
  不是 texture capacity 完全不够；
  不是简单多跑 texture；
  不是单独 camera / pose continuation；
  而是 misaligned / unreliable boundary 区域污染了高频感知监督。

v193a 证明：
  把高频感知监督限制到更可靠的稳定内区，可以继续提高清晰度。

v193a 也证明：
  这不能自动修复边界几何。
  下一步必须把 boundary substrate 单独做出来，再和 v193a 的 texture clarity 合并。
```
