# @package _global_

# Narrow carrier relaxation sibling of v89a: keep the same supervision and
# schedule, but only reduce latent decay slightly instead of removing it.

opt:
  latent_weight_decay: 0.005
