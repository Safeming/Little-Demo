# @package _global_

texture_name: shallow_mlp_lownoise

model:
  gaussian:
    use_sh: false
    feature_dim: 32
  texture:
    name: mlp

    feature_dim: ${model.gaussian.feature_dim}
    use_xyz: false
    use_cov: false
    use_normal: false
    sh_degree: 3
    non_rigid_dim: 16
    latent_dim: 16

    cano_view_dir: true
    view_noise: 30

    mlp:
      n_neurons: 64
      n_hidden_layers: 2
      skip_in: []
      cond_in: []
      multires: 0

opt:
  feature_lr: 9e-4
  texture_lr: 9e-4
  tex_latent_lr: 9e-4
  latent_weight_decay: 0.05
